[
  {
    "id": "F-005",
    "verdict": "CONFIRMED",
    "reasoning": "Structural divergence verified on both sides. ROM SHRTCK (BZONE.MAC:2268-2327, .RADIX 16 region — before the :4878 flip to 10) forms TEMP3 = range(MOUTHI:MOUTLO) >> 2 with a hard range gate (LSR/BNE 22$ rejects when the high byte survives >>2, i.e. range >= ~1024), then builds threshold = 1.5*angular_term + $38(56) where angular_term is derived from |TANGLE - TANGLE+2| (ASL, abs, LSR/LSR, plus an extra LSR for a non-R2D3), and declares a hit at CMP TEMP3 / BCS 38$ (threshold >= range>>2). Base $38=56 verified byte-exact at :2324; the R2D3 term ADC I,$18=24 at :2319 is added to angular_term BEFORE the *1.5 (so it contributes ~+36 to the final threshold, not +24 — the finding's 'base + 24' phrasing is a minor imprecision, immaterial to the accept ruling). The clone (enemies.ts:514) is dx*dx+dz*dz < TANK_HIT_RADIUS^2 with TANK_HIT_RADIUS = PLAYER_RADIUS = 0x480 = 1152 (enemies.ts:147, movement.ts:66) — a fixed planar Euclidean circle with no range-scaling and no bearing term. The core claim (ROM = range+bearing view-space test with per-target-type constants 56/+24; clone = flat fixed-radius circle) is exactly right. Caveat: the editorial gloss 'a cone that narrows with distance' is imprecise — the threshold actually WIDENS with larger angular_term, so the directional feel is speculative — but that gloss is not the load-bearing claim, and the recommendation is 'accept' under the planar-sim ruling. Constants 56 and 24 are correct, so no numeric correction."
  },
  {
    "id": "F-006",
    "verdict": "CONFIRMED",
    "reasoning": "Verified on both sides. ROM OBJOBJ (BZONE.MAC:3600-3689) loads the tank-to-object distance into TEMP3 (MOUTLO:MOUTHI), indexes PROXTB by object-type*2 (LDA AY,PTBLO1 / ASL / TAX at :3632-3634), and compares PROXTB[type] directly against that raw centre distance (LDA AX,PROXTB / CMP TEMP3 ... SBC TEMP3+1 / BCS at :3652-3658) — one pre-combined per-type threshold. PROXTB (:3690, .RADIX 16) = .WORD 340,340 / 10 zero words / 400,0,0,3C0 = 832, 832, 1024, 960 units, byte-exact. R2D3 gets a 1.5x distance inflation (the '3/4 TANK RADIUS' block :3637-3648); tank-vs-tank collides on distance high-byte <=5 (1280) / <=3 (768). Clone (movement.ts:88-93) computes block = PLAYER_RADIUS(1152) + OBSTACLE_RADIUS[type] where OBSTACLE_RADIUS = {narrow-pyramid 512*sqrt2=724, wide-pyramid 800*sqrt2=1131, tall-box 512*sqrt2=724, short-box 640*sqrt2=905}, giving blocks 1876/2283/1876/2057 — a sum of two circumscribed radii, not a single stored threshold. Structural divergence (sum-of-radii vs single per-type PROXTB) is certain; the ~2.1-2.25x magnitude is caveated by the finding as dependent on the undecoded Math-Box output scaling, which is honest. The clone's own comment (movement.ts:71-72) already flags the $6955 obstacle-radius decode as deferred — PROXTB is that table. Size m is fair (decode a 3-value table + swap the formula)."
  },
  {
    "id": "F-007",
    "verdict": "CONFIRMED",
    "reasoning": "Verified on both sides. ROM COLCHK (BZONE.MAC:2408-2436) shifts the shell-to-object distance right twice — the two CMP I,80 / ROR / ROR TEMP3 pairs at :2423-:2428 are sign-preserving arithmetic >>1 done twice, yielding TEMP3 = distance>>2, with CMP I,0 / BNE 40$ rejecting anything still non-zero in the high byte (too far) — then indexes PRXTBL by object-type directly as a BYTE table (LDX AY,PTBLO1 / LDA AX,PRXTBL at :2432-2433) and explodes the shell when PRXTBL[type] >= distance>>2 (CMP TEMP3 / BCC 40$ misses). PRXTBL (:2510) = .BYTE 56.,88. / 10 zero bytes / 86.,0,0,0 — the trailing periods force DECIMAL inside the .RADIX 16 region, so 56/88/86 (effective radius PRXTBL*4 = 224/352/344 Math-Box units). This is a SEPARATE, smaller table than PROXTB (832/1024/960), and indexed by type*1 (byte) versus PROXTB's type*2 (word). The clone (firing.ts:83-88, r = OBSTACLE_RADIUS[o.type] at :85) reuses the tank-footprint circumscribed radius (724-1131) with a plain dx^2+dz^2 < r^2 and no >>2 scaling — a different table and a different formula, making the clone's shell-vs-obstacle radius roughly 2-3x too large. firing.ts:81 admits the $6139 projectile-diameter table is a deferred decode; PRXTBL is that table. Structural claim certain; absolute-unit ratio caveated on the undecoded Math-Box scaling. Size m fair."
  },
  {
    "id": "F-008",
    "verdict": "CONFIRMED",
    "reasoning": "Verified on both sides. ROM EXINIT (BZONE.MAC:2587-2611) seeds 6 pieces (LDX I,5 -> X=5..0, six DEX iterations), each getting ZVELOC = IZVEL[i], position = tank position, orientation = PRAND, EXPOSZ = 0. IZVEL (BZMTNS.MAC:837) sits in that file's .RADIX 10 block (:501->:839), so the bytes are DECIMAL. EXPLDE (BZONE.MAC:1756-1854) runs once/frame: EXPOSZ += ZVELOC*4 (sign-extended <<2 via ASL/ROL at :1783-1794), ZVELOC += GRAVTY (verified GRAVTY=-4. decimal at BZONE.MAC:380) at :1799, XY drift from EXPTBX/EXPTBY (BZMTNS.MAC:833/835), a per-piece orientation spin (:1801-1816), and the sequence resets only when ALL six pieces have EXPOSZ high-byte negative (:1841-1848). Duration is physics-driven off max V0=88 and gravity -4 (~3s), not a constant. The clone (enemies.ts:155) is EXPLOSION_DURATION = 1.5, a single scalar with its own comment admitting 'PROVISIONAL -- no quarry citation for blast length yet'; the exploding phase carries only a phaseAge timer, no debris positions/velocities/gravity in the pure core. Both divergences (half-length duration; zero debris physics) are real; the ~3s figure is an estimate the finding shows arithmetic for, and the fixed-timer-vs-particle-physics gap is certain.",
    "corrections": [
      "Size: m -> l. This is not a table decode or constant swap (unlike F-005/006/007) but a new deterministic-core subsystem: 6 pieces each carrying position (X,Y,Z), Z-velocity, and orientation; per-frame gravity integration (EXPOSZ += ZVELOC*4, ZVELOC += -4); XY drift tables (EXPTBX/EXPTBY); per-piece orientation spin; and a physics-driven termination (all six below ground) — plus threading that debris state through state/cloneState and rendering the flying/spinning/falling fragments in shell/. The coverage review's m->l flag is correct.",
      "IZVEL has SIX launch velocities, not five. The cited line BZMTNS.MAC:837 '.BYTE 55,40,70,88,40' is byte-exact, but a 6th byte '.BYTE 66' follows at BZMTNS.MAC:838 (still decimal, .RADIX 10), and EXINIT seeds 6 pieces (IZVEL[0..5]). So the six per-piece velocities are 55,40,70,88,40,66. The max is still 88, so the ~3s duration estimate is unchanged."
    ]
  }
]
