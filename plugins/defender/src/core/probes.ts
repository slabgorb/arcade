// plugins/defender/src/core/probes.ts
//
// Story df4-5 — RED stub (O'Brien / TEA). Deliberately EMPTY: this is the sanctioned
// brand-new-module RED seam (the df4-3/df4-4 landers/mutants/ufo precedent). The contract
// tests/helpers/df4-5-enemies-contract.ts namespace-imports this module and casts it to
// Partial<…>, so `npm run lint` stays green while every reducer assertion fails with a
// self-describing "not built yet" message. GREEN (Julia) replaces this file with the real
// reducer — see the loader in the contract helper for the exact exports required.
export {}
