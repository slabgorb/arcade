id: rb1
type: epic
title: Red Baron — Quarry & Foundation
description: "Foundation epic for Red Baron (Atari 1980, first-person WWI vector dogfight — Battlezone's Math Box/AVG hardware twin). Quarries the historicalsource/red-baron disassembly into reference/ and extracts mechanics/timing/sound/object data into a fidelity spec; scaffolds the red-baron subrepo (Vite/TS/Vitest, pinned port 5277, base /red-baron/); consumes @arcade/shared@v0.5.0 (math3d/rng/loop) as the first native shared-lib consumer; builds the roll/pitch flight camera + tilting horizon on shared math3d. Exit: runnable banking cockpit over vector terrain + fidelity spec seeding rb2-rb5. See docs/superpowers/specs/2026-07-08-red-baron-design.md."
priority: p1
status: ready
repos: red-baron
stories:
  - id: rb1-1
    title: Subrepo bootstrap — Vite/TS/Vitest scaffold, pinned port 5277, base /red-baron/, wire @arcade/shared@v0.5.0 (math3d/rng/loop), register (repos.yaml + justfile serve + cloudflared ^/red-baron→:5277 + lobby tile)
    points: 2
    priority: p1
    status: done
    started: '2026-07-08'
    repos: red-baron
    workflow: tdd
    acceptance_criteria:
      - 'red-baron/ subrepo initialized (git init -b develop, no GitHub remote): Vite/TS/Vitest scaffold, TS strict, black-canvas index.html booting src/main.ts'
      - Pinned dev port 5277 (NOT 5270/5273/5274/5275/5276) and base '/red-baron/', with strictPort + allowedHosts on both server and preview in vite.config.ts
      - '@arcade/shared pinned as github:slabgorb/arcade-shared#v0.5.0; a shared import (e.g. /math3d) resolves under vite build + vitest (first native shared-lib consumer)'
      - Registered in .pennyfarthing/repos.yaml in the star-wars entry shape (no port key; port authority is vite.config strictPort + cloudflared)
      - 'justfile: red-baron added to games/subrepos vars AND the serve trap (→ http://localhost:5277/red-baron/); green lobby tile → /red-baron/'
      - 'cloudflared ingress: ^/red-baron → :5277 placed ahead of the lobby catch-all'
      - No push/remote; orchestrator + lobby edits left uncommitted for review (orchestrator 'commit only when asked'); subrepo work on chore/rb1-1-subrepo-bootstrap
    completed: '2026-07-08'
    type: chore
    review_verdict: approved
  - id: rb1-2
    title: Source findings / fidelity spec — quarry historicalsource/red-baron into reference/; distill mechanics (flight, enemy behavior, ground seq), timing (frame cadence — watch ÷N ROM tick), sound (RBSOUN.MAC POKEY tables), object data (PLANE POINTS DB, 036xxx/037xxx picture ROMs, RBGRND.MAC)
    points: 3
    priority: p1
    status: done
    started: '2026-07-09'
    repos: red-baron
    workflow: trivial
    completed: '2026-07-09'
    type: chore
    review_verdict: approved
  - id: rb1-3
    title: Flight-camera foundation — roll/pitch/yaw camera on shared math3d, tilting horizon, vector terrain/horizon render substrate, runnable empty cockpit
    points: 5
    priority: p1
    status: backlog
    repos: red-baron
    workflow: tdd
