sprint:
  name: "TO Sprint 2634"
  number: 2634
  jira_sprint_id: 5
  jira_sprint_name: "TO Sprint 2634"
  goal: Smoothing out the game factory (Millipede ml1)

completed_epics: []
completed_stories:
  - id: ml1-5
    epic: ml1
    title: 'Four-file scaffold + three registrations -> boots black canvas at /millipede/: copy plugins/joust/ (index.html, plugin.ts, package.json, tsconfig.json extends ../../tsconfig.json); add millipede to justfile games and vitest.config.ts GAMES; npm run gen:registry. Green npx vitest run --project millipede + npm run lint; visual boot check at http://127.0.0.1:5270/millipede/ (compare against a nonsense control path, not just a 200).'
    points: 5
    completed: '2026-08-11'
  - id: sw11-2
    epic: sw11
    title: "Trench wall guns render cantilevered off the wall instead of flush, facing the wrong way. TRENCH_TURRET is placed on BOTH side walls (trench-obstacles.ts streamPanelSlots: pos = [depth, wallX = +/-W, slotHeight]) but drawn with a single fixed orientation — render.ts:585 uses `modelMatrix(o.pos, TRENCH_ORIENT)` and TRENCH_ORIENT was retired to IDENTITY by sw10-1. The model's `.WP WGA` base is a plate in the horizontal (x-z) plane with a single barrel direction (models.ts:726; the doc comment says 'ORIENTATION is the shell's job (render.ts)'), so with no per-wall rotation it cannot sit flush on a vertical side wall and its barrel points the wrong way on at least one wall. The ROM proves the two walls are MIRROR images: BSGUN mounts the barrel at `M.Y0 = -380 ;GUN BARREL ON LEFT WALL` (FRPLGN) vs `M.Y0 = +380 ;GUN BARREL ON RIGHT WALL` (FRPRGN), WSBASE.MAC:1251/1295, and PANLIN gives the shell MOV$PL vs MOV$PR so left/right guns are handled as mirrors throughout. Fix in the render/placement layer (core is pure): seat the turret base against the vertical wall and mirror/rotate per wall from the sign of o.pos[1] (right axis) so the barrel points INTO the channel on both walls. Render-only orientation change — MUST be eyeballed on the dev server (port 5270, /star-wars/); structural tests can't catch orientation (render.ts:168). Apply the same audit to TRENCH_SQUARE/TRENCH_CATWALK while here — they share the IDENTITY-orient placement but read less obviously wrong."
    points: 3
    completed: '2026-08-11'
  - id: sw11-1
    epic: sw11
    title: "Trench return-fire is grossly too lethal — port the ROM's THREE base-gun mechanisms the sw10 basis unification newly exposed. Every difficulty CONSTANT is already faithful; these are MECHANISM omissions (cf. asteroids-rom-retune). Our stepTrench fires every 'turret' within TRENCH_GUN_FIRE_RANGE (0x6000) each opening on a flat `nextInt(rng,256) > threshold` roll, spawns a PERFECT analytic lead (trenchGunFireVelocity), and caps concurrent shots at MAX_FIREBALL_SLOTS (6). The cabinet does all three very differently. (1) AIM — biggest lever. The ROM never leads: PANLIN (WSGUNS.MAC:409-432) spawns the shell at the gun's OWN panel position with a semi-random Z angle (`LDA P.RND1 / LSRA ;45 DEGREES`), top-panel guns level; then MOVPL/MOVPR (WSGUNS.MAC:788-846) move it with a heavily DAMPED, DIRECTIONALLY-LIMITED heat-seek — Z drifts toward the player at ASRD4 (>>4 = 1/16 of the gap per frame); Y only heat-seeks the player from WV.HRD>=1 (`CMPA #1 / IFHS ... LDD M$TY ;HEAT SEEK`), and below that just drifts to +/-180 (channel centre), and a LEFT-wall shell may ONLY move right, a RIGHT-wall shell ONLY left. Net: authentic shells scatter and usually miss; ours never do. Replace the closed-form lead with a random-angle spawn + damped per-frame heat-seek matching MOVPL/MOVPR. (2) CONCURRENCY — GNBSAVAIL (WSGUNS.MAC:210-244) caps base-gun shells by the per-difficulty TGNBS table `GUNZ-<N*G$IZE>` = N airborne, N = [1,1,2,2,3,3,3,4] over WV.HRD 0..7. Wave-1 trench (WV.HRD 0) allows just ONE base shell in the air; our flat 6 is 6x. Cap concurrent base shots at TGNBS[gunDiff], not MAX_FIREBALL_SLOTS. (`GNAVAIL`/'FIRE EVERYTHING' is the port-in-view end-run only.) (3) ELIGIBILITY — BSGUN (WSBASE.MAC:1236-1341) fires a gun only when the player is ABOVE the bunker (`SUBD M.Z0 / IFGE ?PLAYER ABOVE BUNKER?`) AND within the near height band (`SUBD #400 / IFLT`) at full prob, OR within one lower band at the SQUARED prob `P.RND1 * P.RND1` (two uniforms => skewed low); never below, and never point-blank (`ADDA #20+4 ;(DONT SHOOT IF TOO CLOSE)`). #400 is one wall-panel height (Z walks -0200 down by #400 x4); map to our WALL_SLOT_Y spacing. Port the vertical-alignment gate + squared far-branch + too-close exclusion into the stepTrench gun loop. Pin all three with deterministic tests (fixed seed + fixed player height: assert only the aligned subset fires, a point-blank gun never fires, concurrent base shots <= TGNBS[gunDiff], and a spawned shell's angle is the random/level value not a lead). Verify playability by hand on the dev server."
    points: 8
    completed: '2026-08-11'
  - id: ml1-1
    epic: ml1
    title: 'Citation gate + src/core purity test FIRST (TDD): port centipede tools/audit/check-citations.mjs to a single-sided millipede checker (drop the ours side) + citations.test.ts that fails on any uncovered prose citation + the src/core purity scan (fetch/canvas/Date/Math.random). Committed before the first game constant.'
    points: 5
    completed: '2026-08-12'
  - id: jt11-1
    epic: jt11
    title: 'Start experience: thread playerCount from createGame (game.ts:318, currently dropped) into createWaveDemo (demo.ts:1229-1236, hardcodes PLAYER1_ID+PLAYER2_ID) so a 1P game spawns ONE knight - downstream (lives=NSHIP game.ts:322, death clamp game.ts:257, respawn game.ts:479-499) already keys off ledger count and self-aligns. Attract page gains a visible PRESS 1 OR 2 TO START prompt (attract exits only on Digit1/Digit2 main.ts:422-426,500-503; START labels render only on the select screen selectScreen.ts:20-22, unreachable without knowing the key). Boot/attract self-play keeps two mounts (main.ts:354) - count enters via select->startPlaying (cabinet.ts:99-101) only.'
    points: 5
    completed: '2026-08-12'
