---
story_id: "tp1-9"
jira_key: "tp1-9"
epic: "tp1"
workflow: "tdd"
---
# Story tp1-9: THE CAMERA — the far ring is built about the projected vanishing point, and FAR_RATIO is per-well, not a single 0.2

## Story Details
- **ID:** tp1-9
- **Jira Key:** tp1-9
- **Workflow:** tdd
- **Stack Parent:** none

## Workflow Tracking
**Workflow:** tdd
**Phase:** review
**Phase Started:** 2026-07-14T18:07:03Z

### Phase History
| Phase | Started | Ended | Duration |
|-------|---------|-------|----------|
| setup | 2026-07-14T17:13:09Z | 2026-07-14T17:15:33Z | 2m 24s |
| red | 2026-07-14T17:15:33Z | 2026-07-14T17:45:40Z | 30m 7s |
| green | 2026-07-14T17:45:40Z | 2026-07-14T18:07:03Z | 21m 23s |
| review | 2026-07-14T18:07:03Z | - | - |

## Sm Assessment

**Setup complete — routing to TEA (O'Brien) for the RED phase.**

- **Story:** tp1-9, 6 points, p1 bug, epic tp1 (Cluster C5; subsumes DB-006/007/008/009/016). Blocks tp1-10 (the dive camera needs the movable eye this story creates).
- **Repos:** tempest (gitflow; branched `fix/tp1-9-camera-per-well-far-ratio` from origin/develop at bad7f12, working tree was clean).
- **Workflow:** tdd (phased: setup → red → green → review → finish). Routed per SETUP_RESULT: next_agent = tea.
- **Jira:** skipped — no Jira in this project; jira_key is the story id (tp1-9).
- **Merge gate:** clear — no open PRs on slabgorb/tempest at setup time.
- **Context:** story + epic context generated by sm-setup at sprint/context/. No pre-existing Architect-enriched context existed for tp1-9, so nothing was clobbered.
- **Coordination notes for TEA:**
  - Check recent tempest archives (`sprint/archive/tp1-6-session.md`, `tp1-27`, `tp1-12`) for Delivery Findings — prior TEAs have pre-extracted ROM quarry for follow-on stories.
  - AC-7 pins `npm test -- citations`; cited files shift pinned lines on ANY edit (comments included) — re-anchor citations after edits.
  - The story is an API change, not a constant swap: project/boundaryRail/laneWidth/flipPivot currently close over module-level FAR_RATIO.

## TEA Assessment

**Tests Required:** Yes
**Reason:** p1 fidelity bug with seven ACs — an API change to the core projection, new sim state, and a shell law swap.

**Test Files:**
- `tempest/tests/core/tp1-9.per-well-camera.test.ts` — AC-1/2/4/5 + AC-3 data half: `perspectiveDepth(tube, depth)` derives R = (16+H)/(240+H) per well (all 16 pinned to ROM-byte literals, range 26/250..44/268); far rings built about the projected vanishing point from HOLEZL (VP relation on every rim point of every level, the 36%-displacement literal on the circle, the figure-8 as the one concentric well, the flat-plane horizon cue); `tube.eye`/`tube.screenZ` data pins; per-well divide through project/laneWidth/flipPivot; `makeCircleTube` derives a consistent eye (60/300 → H=40) so every synthetic-tube sibling keeps its numerics; FAR_RATIO export + identifier deletion.
- `tempest/tests/core/tp1-9.camera-slide.test.ts` — AC-3 behaviour half: `s.camera.screenZ` snaps at game start, eases ~⅛-per-frame over ~8 ROM frames on wave advance (1→2 upward, 7→8 downward across a sign change), monotone, no overshoot, deterministic.
- `tempest/tests/shell/tp1-9.starfield-law.test.ts` — AC-6: `starPlaneScale(z) = 40/(z+24)` (DSTARF's eye −24 / YDEUNI 40), hyperbola signature, 15%-visible spawn, linear-ramp refutation; `?raw` scans pin the render wiring (starPlaneScale call, linear law gone, camera.screenZ consumed).

**Tests Written:** 40 tests covering 7 ACs (AC-7 is the citations gate — verified green this phase)
**Status:** RED (38 failing across the 3 new files — verified by testing-runner RUN_ID tp1-9-tea-red; the 2 pre-GREEN passes are audited intended guards: the near-ring keep-behaviour pin, and the uniform-ratio refutation which fails exactly under the wrong-GREEN shared-R implementation)

**Sibling re-seats (TEA-owned):**
- `tests/core/rom-clock.test.ts` — the FAR_RATIO negative-control test removed with its import (its subject is deleted by this story; its own comment predicted tp1-9).
- `tests/rom-clock-sources.test.ts` — stale comment bullet naming geometry.ts:18 updated (comment-only; the RATE_60 scan never matched `60 / 300`).
- Everything else survives BY DESIGN: `makeCircleTube` deriving the consistent eye keeps the whole 10-12 perspective suite and 6 other synthetic-tube suites byte-identical, and the near ring not moving keeps claw/rim pins green.

### Rule Coverage

| Rule | Test(s) | Status |
|------|---------|--------|
| TS #4 null/undefined + divide poles | `finiteness — per-well divides stay clear of the pole across the roster`; starfield `shrinks monotonically... stays finite/positive` | failing (RED) |
| TS #2 readonly interface fields | tsc-enforced via `npm run build` (vitest strips types — no runtime pin possible; see arcade-shared lesson) | n/a (compile gate) |
| TS #8 test quality (vacuous asserts) | self-checked: determinism/trace tests carry `Number.isFinite` guards so undefined traces cannot pass vacuously; no `let _`, no `assert(true)` | done |
| TS #1/#3/#5/#6/#7/#9 | no type escapes, enums, module tricks, JSX, async, or config changes in scope | n/a |

**Rules checked:** 2 of 2 applicable lang-review rules have test coverage (rest n/a)
**Self-check:** 1 potential vacuous pass found and fixed pre-commit (undefined-trace `toEqual` in the determinism test — now guarded by a finiteness assert)

**Quarry verified first-hand** (primary source `~/Projects/tempest-source-text`; the CRLF sibling is NOT citable): WELSEQ/HOLEYL/HOLEZL/HOLZAD/HOLZDH at ALDISP.MAC:1384-1388; INIWLS eye negation + YDEUNI=16+H + ZADJ snap/ease at 2464-2508; the slide driver at ALWELG.MAC:75-84; DSTARF's star eye at 2931-2936. All four audit findings' claims (DB-006/007/008/016) reproduced independently before pinning.

**Handoff notes for Julia (Dev):**
- The API the tests pin: `Tube.eye: { distance, z }` (raw ROM bytes), `Tube.screenZ` (canvas-y ring units, −ZADJ·RING_SCALE), `perspectiveDepth(tube, depth)` tube-first like every geometry function, `GameState.camera.screenZ`, `starPlaneScale(z)` exported from the pure starfield module.
- `makeCircleTube` must DERIVE its eye from its radii — H = (240q − 16)/(1 − q), q = far/near — or seven sibling suites break; the tests pin H=40 for 60/300.
- HOLEYL/HOLEZL/ZADJ are indexed by the REMAPPED shape id (WELSEQ = our ROM_REMAP — same table, verified), i.e. attach the data in `makeRingTube(shapeIndex)`; level 9 → stair is the remap tripwire test.
- Don't forget `cloneState` (sim.ts:23) for the new camera field, and `initialState`.
- Citations: mark DB-006, DB-007, DB-008, DB-016 `remediated_by: tp1-9` — but NOT DB-009 (see Delivery Findings). DB-005 needs a RE-QUOTE, not a stamp. Run `node tools/audit/reanchor-citations.mjs --write` after editing geometry.ts/render.ts/starfield.ts/state.ts/sim.ts (all carry live citations). The FAR_RATIO src-scan is comment-inclusive: geometry.ts's doc comments at lines 15-18/27/34/189 must lose the identifier too.

**Handoff:** To Julia (Dev) for GREEN.

## Dev Assessment

**Implementation Complete:** Yes
**Files Changed:**
- `tempest/src/core/geometry.ts` — `Tube` gains `eye: WellEye {distance, z}` + `screenZ`; HOLEYL/HOLEZL/ZADJ tables (ALDISP.MAC:1385-1388) with citations; `perspectiveDepth(tube, depth)` derives R = (16+H)/(240+H); far rings built about the projected vanishing point; FAR_RATIO deleted (identifier fully gone from src/); `makeCircleTube` derives the consistent eye (H = (240q−16)/(1−q)).
- `tempest/src/core/state.ts` — `CameraState { screenZ, slidePerFrame }` on GameState; initialState snaps to the level-1 target.
- `tempest/src/core/sim.ts` — cloneState carries camera; startGameAtLevel SNAPS (CNWLF2 branch); advanceLevel arms the ⅛-per-frame ease (ZADEST); stepCamera applies it each playing frame and clamps at the target.
- `tempest/src/shell/starfield.ts` — `starPlaneScale(z) = 40/(z+24)` exported (DSTARF's eye −24 / YDEUNI 40).
- `tempest/src/shell/render.ts` — drawStarfield sizes dots by the hyperbolic law and re-anchors at the scene origin (fixes a latent post-phosphor half-screen displacement); the whole projected scene translates by `s.camera.screenZ`.
- `tempest/src/core/modelView.ts` + 3 test files — mechanical Tube-literal updates (neutral legacy eye H=40).
- `tempest/docs/audit/findings/*.json` — DB-006/007/008/016 stamped `remediated_by: tp1-9` (DB-009 deliberately NOT, per TEA's finding); DB-005 and WD-013 re-quoted (lines rewritten, findings still live); 161 citations re-anchored clean.
- `tempest/tests/core/tp1-9.*.test.ts` — screenZ unit-conversion literals corrected (see the blocking Dev Delivery Finding); slide fixture stages the parked camera.
- `tempest/tests/core/geometry.cycle.test.ts` — far~60 pin re-seated to the ROM-faithful far ring (it pinned the DB-006 divergence itself).

**Tests:** 1134/1134 passing (GREEN, 96 files — testing-runner RUN_ID tp1-9-dev-green-2); `npx tsc --noEmit` and `npm run build` clean; citations gate green (AC-7).
**Branch:** fix/tp1-9-camera-per-well-far-ratio (pushed, 2 commits: RED 63d2647, GREEN 5a9fcc8)

**Handoff:** To the next phase (review) — note the blocking Delivery Finding: Dev corrected TEA's screenZ unit literals; the Reviewer should verify the −ZADJ·(16+H)·S/256 derivation against WORSCR, and eyeball the rendered well offset + star rush on a dev server (render is scan-pinned only).

## Subagent Results

| # | Specialist | Received | Status | Findings | Decision |
|---|-----------|----------|--------|----------|----------|
| 1 | reviewer-preflight | Yes | clean | none (1134/1134 green, build clean, 0 smells, tree clean) | N/A |
| 2 | reviewer-edge-hunter | Skipped | disabled | N/A | Disabled via settings |
| 3 | reviewer-silent-failure-hunter | Skipped | disabled | N/A | Disabled via settings |
| 4 | reviewer-test-analyzer | Skipped | disabled | N/A | Disabled via settings |
| 5 | reviewer-comment-analyzer | Skipped | disabled | N/A | Disabled via settings |
| 6 | reviewer-type-design | Skipped | disabled | N/A | Disabled via settings |
| 7 | reviewer-security | Skipped | disabled | N/A | Disabled via settings |
| 8 | reviewer-simplifier | Skipped | disabled | N/A | Disabled via settings |
| 9 | reviewer-rule-checker | Skipped | disabled | N/A | Disabled via settings |

**All received:** Yes (1 returned, 8 disabled via workflow.reviewer_subagents — analytical review performed directly by Reviewer)
**Total findings:** 3 confirmed, 0 dismissed, 0 deferred (plus the SUPERSEDED verdict driver below)

## Reviewer Assessment

**Verdict:** REJECTED — SUPERSEDED, not defective. tp1-9 was completed and merged by a parallel checkout mid-flight (tempest PR #113, `d1df2ab`, merged to origin/develop at 13:48; orchestrator `5d3386c` archived the story and `9b33569` filed **tp1-31** for the deferred DB-008 screen-Z work). Merging this branch would double-implement the story with a conflicting API (`Tube.eye` vs upstream's `Tube.farRatio`). The code itself is sound (see observations) — the rework directive is RE-SCOPE, not fix.

### The supersession facts

- Upstream #113 delivered AC-1/2/5/6: per-well `farRatio` on the Tube, VP far rings (same formula ours derived: VP = (0, (0x80−EZ)·S)), per-well divide through project/laneWidth/flipPivot, hyperbolic `starReachFraction(z) = 40/(z+24)` (faithful 15%-at-spawn variant), DB-006/007/016 stamped, DB-005 re-pointed. 20 RED tests (`tp1-9.camera-perspective.test.ts`, `tp1-9.starfield-perspective.test.ts`).
- Upstream explicitly deferred AC-3 → **story tp1-31** ("DB-008 screen-Z translate + slide, deferred from tp1-9"), and DB-008/DB-009 remain unstamped upstream (consistent with TEA's DB-009 finding here).
- THIS branch's unique, unmerged value = exactly tp1-31 plus three extras:
  1. **AC-3 complete**: ZADJ port with the POST-DIVIDE unit conversion (−ZADJ·(16+H)·S/256, verified against WORSCR's header formula ALDISP.MAC:2049-2051 — I re-derived it independently this phase), `CameraState` snap/ease slide, whole-scene render translate.
  2. **Starfield anchor fix**: develop's drawStarfield still anchors at (W/2, H/2) on the centre-origin phosphor scene — upstream wired the hyperbolic law into a field that is displaced half a screen. Our fix (scene-origin, per DSTARF's PXL=PZL=0x80) is needed regardless.
  3. **Eye as data** (`tube.eye.{distance, z}`): upstream's `farRatio` scalar cannot feed tp1-10's movable eye (H and EZ are needed per-frame); upstream keeps them as module tables, so tp1-10 can still reach them — but AC-4 ("the eye is a movable parameter") is at best half-met upstream. Planning input for tp1-10.

### Observations (own analysis; specialists disabled)

1. `[VERIFIED]` The screenZ unit correction is right — WORSCR header `SCREEN Z = [FACTOR/(PY-EY)]*(PZ-EZ)+SZCENT` (ALDISP.MAC:2049-2051), numerator loaded into the math-box HIGH byte (×256, `STA MZLH` ALDISP.MAC:2243/2268), `ADC ZADJL` post-divide at :2274; rim-relative invariant −ZADJ·(16+H)·S/256 is scale-free. Checked against lang-review TS #4: the divisor (16+H) is a positive ROM byte on all 16 wells, no pole.
2. `[VERIFIED]` VP far-ring construction matches upstream's independent derivation formula-for-formula ((0, (0x80−EZ)·S); far = VP + (near−VP)·R) — two clean-room implementations agreeing is strong evidence both read the ROM right.
3. `[MEDIUM]` Stale VP-glow anchor at `src/shell/render.ts:275` (`ctx.arc(0, 0, …)` under the comment "closed tubes converge on the center") — after tp1-9 the far ring converges on the per-well VP, not the origin; the decorative glow now floats ~109 ring units off the level-1 far ring. Present in OUR branch AND on develop (upstream has the same gap). Fold into tp1-31.
4. `[MEDIUM]` Develop's starfield is displaced half a screen (upstream kept `cx = W/2` on the centre-origin scene). Live visual defect on develop; our anchor fix should ride tp1-31.
5. `[VERIFIED]` The slide state machine: snap on `startGameAtLevel` (covers demo seeding via the same path), ⅛-fixed-step ease armed in `advanceLevel`, per-call stepping matching the qframe convention, direction-aware clamp (sim.ts stepCamera). `cloneState` carries camera — checked against the RNG-cursor clone precedent.
6. `[VERIFIED]` Purity boundary intact: geometry/state/sim additions are table lookups + arithmetic; no DOM/time/Math.random (preflight smell scan 0; rom-clock-sources RATE_60 scan green in the suite).
7. `[LOW]` `makeCircleTube` with farRadius ≥ nearRadius produces a nonsensical derived eye (q ≥ 1 → division by ≤0) — documented "every caller passes q < 1", no runtime guard. Acceptable for a test-only constructor; note if it ever gains prod callers.

### Rule Compliance (lang-review typescript)

| Check | Result |
|---|---|
| #1 type escapes | Compliant — no `as any` / `@ts-ignore` / non-null assertions in the diff (grep-verified) |
| #2 generics/readonly | Compliant — `WellEye`/`Tube` new fields readonly; `CameraState` deliberately mutable matching GameState's existing mutable-substate convention (player, warp) |
| #3 enums | N/A — none added |
| #4 null/undefined & divide poles | Compliant — pd denominator ∈ [R,1]; per-well R from positive ROM bytes; finiteness suite pins all 16 wells; `??`/`||` not introduced |
| #5 modules | Compliant — `import type` used in tests; no re-export changes |
| #6 React/JSX | N/A |
| #7 async | N/A — no async in the diff |
| #8 test quality | Compliant — vacuous-trace guards present (Number.isFinite before toEqual); no .skip/.only added |
| #9 build/config | N/A — no config changes |

### Devil's Advocate

Suppose this review is wrong and the branch should merge anyway. The argument would be: our implementation is a superset — richer eye data, correct screenZ units, the anchor fix — so develop should take OURS and tp1-31 dies at birth. But walk the consequences: develop already carries `Tube.farRatio` consumed by upstream's 20 new tests and two new test files; merging our `Tube.eye` API would either break their suites or force carrying BOTH spellings of one ROM fact — precisely the two-spellings drift (`PLAYER_RIM_DEPTH`/`RIM_DEPTH`) that tp1-27 existed to kill. Conflict resolution across geometry.ts, render.ts, starfield.ts, three findings JSONs, and two parallel test suites for the same ACs would be hand-merged under review pressure — the exact failure mode the epic-11-6 lesson warns about ("prefer upstream over hand-merging duplicate impls"). Now suppose instead we quietly re-point this branch at tp1-31 and merge it ourselves without re-scoping: the sprint record would show tp1-31 done by a branch named `fix/tp1-9-…` whose session file describes a different story — the audit trail (which this epic treats as load-bearing) would lie. What would a stressed maintainer conclude in six months seeing two tp1-9 test suites with different file names pinning the same ACs through different APIs? That one of them is dead weight. And the subtlest risk: our corrected screenZ conversion contradicts the literals TEA's session text still describes (−ZADJ·S) — if the re-scoped tp1-31 RED inherits the session prose rather than the corrected tests, the 6.4× error resurrects. The re-scope must carry the corrected derivation and its WORSCR citation, not the assessment prose. Devil's advocate surfaced no reason to merge; it strengthened the SUPERSEDED verdict and added a warning for the re-scope.

**Data flow traced:** ROM byte tables (HOLEYL/HOLEZL/HOLZAD:HOLZDH) → makeRingTube (build-time, per shape) → Tube data → perspectiveDepth/project (per-frame, pure) → render scene transform (translate only) — no user input touches the projection; the only runtime input is `s.level`, clamped by `tubeForLevel`'s mod-16 wrap (level 17 test).
**Pattern observed:** two-spellings-of-one-ROM-line risk handled correctly at `src/core/geometry.ts` (single ZADJ table, screenZ derived once) — the tp1-27 lesson applied.
**Error handling:** pure math paths; poles excluded by construction; finiteness pinned across the roster.
**Handoff:** NOT relayed — the tdd workflow has no arm for "story completed by a parallel session"; surfacing to the user/SM for the re-scope decision (recommend: re-scope the delta as tp1-31).

## Delivery Findings

Agents record upstream observations discovered during their phase.
Each finding is one list item. Use "No upstream findings" if none.

**Types:** Gap, Conflict, Question, Improvement
**Urgency:** blocking, non-blocking

<!-- Agents: append findings below this line. Do not edit other agents' entries. -->

### TEA (test design)

- **Conflict** (blocking): **the story description says tp1-9 "subsumes DB-009", but DB-009 must NOT be stamped `remediated_by: tp1-9`.** DB-009 is the eye TRAVELLING down the well during the warp dive (CHKDEP/DSPENL exist to cope with it); tp1-9 delivers only the movable PARAMETER (AC-4 — eye as tube data the projection honours). Stamping a compound finding a story half-closes writes a phantom fix into the audit (the W-030 lesson, repeated by tp1-27). Stamp DB-006, DB-007, DB-008, DB-016 only; DB-009 stays live for tp1-10.
  Affects `tempest/docs/audit/findings/pair-4-aldisp-b-well-projection.json` (Dev's remediated_by pass at GREEN).
  *Found by TEA during test design.*

- **Gap** (non-blocking): **DB-005's `ours` verbatim breaks when Dev rewrites perspectiveDepth, and the fix is a RE-QUOTE, not a stamp.** DB-005 (CONFIRMED — "the curve is right") quotes geometry.ts:38 `return (FAR_RATIO * depth) / (FAR_RATIO * depth + (1 - depth))`. The curve stays correct after tp1-9 — nothing is remediated — but the line's text changes, so `reanchor-citations.mjs` will report LOST. Update DB-005's `ours` verbatim/line to the new body.
  Affects `tempest/docs/audit/findings/pair-4-aldisp-b-well-projection.json` (DB-005).
  *Found by TEA during test design.*

- **Improvement** (non-blocking): **tp1-10 quarry pre-extracted (the travelling eye).** Driver: ALWELG.MAC:85-91 — `LDA EYL / CLC / ADC I,18` (+0x18 = 24 world units per frame toward EYLDES), starfield cut off via PLAGRO once the eye passes 0xFC (ALWELG.MAC:91-95). CHKDEP (ALDISP.MAC:2766-2788): once EYH == 0 (eye inside the well), any point closer than 0x0C to the eye is nudged to EYL+0x0F, clamped 0xF0. DSPENL aborts spike drawing entirely once EYL >= 0xF0 (ALDISP.MAC:3000-3006). Crucial subtlety: INIWLS freezes YDEUNI at 16+H — the ROM does NOT recompute it as the eye moves, so the dive's scale law is (16+H)/(PY − EY_now). tp1-10 will need an exported per-frame ring projection; `tube.eye` (this story) is the data seam it should consume.
  Affects `tempest/src/core/geometry.ts` (tp1-10's design input).
  *Found by TEA during test design.*

- **Question** (non-blocking): DSTARF also owns the star COLOUR rule — BLUE while CURWAV < 5, colour-cycling after (ALDISP.MAC:2949-2960). That is DB-017, the starfield half of the recolouring debt tp1-12 deferred; my AC-6 suite deliberately pins the SIZE law only. Whoever picks up the tp1-12 follow-up should note the two laws live in the same ROM routine but are separate findings.
  Affects `tempest/src/shell/render.ts` (DB-017 follow-up scope).
  *Found by TEA during test design.*

### Dev (implementation)

- **Conflict** (blocking — for the Reviewer to adjudicate): **TEA's RED screenZ literals carried a unit error, and Dev corrected the tests.** ZADJ is added to SZ POST-divide (WORSCR header: `SCREEN Z = [FACTOR/(PY-EY)]*(PZ-EZ)+SZCENT`, ALDISP.MAC:2049-2051; `ADC ZADJL` at :2274) — a ROM SCREEN-unit quantity, where the rim spans FACTOR·112/(16+H) with FACTOR = 256 (the numerator rides the math-box high byte). The RED draft converted ZADJ as a WORLD-unit quantity (−ZADJ·S), 6.4× too large on the circle: +514 canvas units of a 720-unit viewport — the well mostly off-screen. Implemented and re-pinned the rim-relative, scale-invariant port `screenZ = −ZADJ·(16+H)·S/256` (+80.36 circle, −93.75 stair). The tests' INTENT (a faithful per-well translate in ring units) is unchanged; only the conversion factor moved. This is the exact trap DB-007's own CORRECTION note flagged ("absolute pixel figures depend on an unverified '256' scale"). Affects `tempest/tests/core/tp1-9.per-well-camera.test.ts`, `tempest/tests/core/tp1-9.camera-slide.test.ts` (Dev edits to TEA-owned files — review the unit derivation, not just the diff).
  *Found by Dev during implementation.*

- **Gap** (non-blocking): **the warp starfield had been drawn off-centre since the phosphor refactor (#53)** — drawStarfield anchored at (W/2, H/2) on a context beginScene had already translated to centre-origin, displacing the field by half a screen. Fixed here (the ROM anchors the pictures at the world centre, PXL = PZL = 0x80 = our scene origin) because the hyperbolic law is unobservable at the wrong anchor — but no test pins star POSITIONS even now (render cannot run in the node env; the suite pins the law + wiring scans). DB-014/DB-015 (other starfield findings) remain live. Affects `tempest/src/shell/render.ts` (a future harness could pin the anchor).
  *Found by Dev during implementation.*

- **Improvement** (non-blocking): **the arcade's ABSOLUTE rim size varies per well and ours does not.** WORSCR's rim scale is 256/(16+H) per world unit, so the cabinet's rim radius runs 651.6 (square) → 716.8 (circle) → 1146.9 (plane) screen units, while RING_SCALE pins every rim at 300 ring units. No audit finding covers per-well rim scale (the DB series covers ratio/VP/translate); if the epic wants it, it is a NEW finding + story, and it interacts with viewport framing. Affects `tempest/src/core/geometry.ts` (RING_SCALE).
  *Found by Dev during implementation.*

- **Question** (non-blocking): mid-slide death→respawn keeps the slide running — our respawn does not re-run well init, while the ROM's CNWLF2 new-life path snaps ZADJL immediately. Deferred per TEA's deviation; tp1-10 (which reworks the dive/camera) or the Reviewer should rule whether respawn should snap `s.camera`. Affects `tempest/src/core/sim.ts` (respawn).
  *Found by Dev during implementation.*

### Reviewer (code review)

- **Conflict** (blocking): **tp1-9 was completed and merged by a parallel checkout mid-flight** — tempest PR #113 (`d1df2ab`) landed on origin/develop at 13:48 while this session's RED/GREEN ran; the orchestrator archived tp1-9 (`5d3386c`) and filed **tp1-31** for the deferred DB-008 screen-Z work (`9b33569`). This branch must NOT merge as tp1-9 (duplicate implementation, conflicting API: our `Tube.eye` vs upstream's `Tube.farRatio`). Recommended: re-scope this branch's unique delta (AC-3 camera/slide with the corrected −ZADJ·(16+H)·S/256 units, the starfield anchor fix, the corrected tests) as tp1-31 on top of origin/develop, adopting upstream's API. Affects `tempest` (branch disposition) + `sprint/` (tp1-31 pickup).
  *Found by Reviewer during code review.*

- **Gap** (non-blocking): **develop's warp starfield is displaced half a screen** — upstream #113 wired the hyperbolic law into drawStarfield but kept the pre-phosphor `(W/2, H/2)` anchor on the centre-origin scene context, so the faithful law renders off-centre in production code today. Our scene-origin fix (DSTARF anchors at the world centre, PXL=PZL=0x80) should ride tp1-31. Affects `tempest/src/shell/render.ts` (drawStarfield anchor).
  *Found by Reviewer during code review.*

- **Gap** (non-blocking): **the decorative vanishing-point glow still draws at the origin** (`ctx.arc(0, 0, …)`, "closed tubes converge on the center") on develop and on this branch — after tp1-9 the far ring converges on the per-well VP, so the glow floats ~109 ring units off the level-1 far ring. One-line fix (anchor at the far-ring centroid); fold into tp1-31. Affects `tempest/src/shell/render.ts` (drawTube VP glow).
  *Found by Reviewer during code review.*

- **Question** (non-blocking): **upstream's AC-4 is at best half-met** — `Tube.farRatio` is a derived scalar; the movable eye tp1-10 needs (H and EZ per-frame) lives only in module-private tables upstream. When tp1-10 is picked up, its TEA should decide whether to surface eye DATA on the tube (this branch's `WellEye` design + tests are ready-made input). Affects `tempest/src/core/geometry.ts` (tp1-10 design).
  *Found by Reviewer during code review.*

- **Improvement** (non-blocking): **parallel checkouts raced the same backlog story** — the merge-gate checks open PRs on the game repo, but a sibling checkout's in-flight work is invisible until it merges; both sessions ran the full tdd pipeline on tp1-9 (~4 hours of duplicated work). A story CLAIM committed to the orchestrator's main at setup (status → in_progress pushed immediately, not just locally) would make the race visible at `pf sprint work` time. Affects `.pennyfarthing` (sm-setup / merge-gate design).
  *Found by Reviewer during code review.*

## Design Deviations

Agents log spec deviations as they happen — not after the fact.
Each entry: what was changed, what the spec said, and why.

<!-- Agents: append deviations below this line. Do not edit other agents' entries. -->

### TEA (test design)
- **Concrete API names pinned where the ACs leave the seam open**
  - Spec source: context-story-tp1-9.md, AC-1/AC-3/AC-4/AC-6
  - Spec text: "perspectiveDepth() takes the tube and derives R = (16+H)/(240+H) per well" (no field/function names given)
  - Implementation: tests pin `Tube.eye.{distance,z}`, `Tube.screenZ`, `perspectiveDepth(tube, depth)` tube-first, `GameState.camera.screenZ`, `starPlaneScale(z)` in `src/shell/starfield.ts`
  - Rationale: RED must compile against something; names follow existing conventions (tube-first geometry args, starfield.ts as the pure star module per the 10-4 precedent)
  - Severity: minor
  - Forward impact: Dev implements these names or renegotiates through TEA — the tests are the contract.
- **makeCircleTube derives a consistent eye from its own radii (60/300 → H = 40)**
  - Spec source: context-story-tp1-9.md, AC-1
  - Spec text: "the module-level FAR_RATIO = 0.2 is deleted" (silent on synthetic tubes)
  - Implementation: synthetic circle tubes keep the legacy R = 0.2 curve via H = (240q − 16)/(1 − q)
  - Rationale: an arbitrary default eye would silently re-curve seven sibling suites; the derived eye is the value CONSISTENT with the rings the constructor builds, and the tests pin the derivation so it can't decay into a hardcoded default
  - Severity: minor
  - Forward impact: none for ROM wells; synthetics are test/debug-only (modelView).
- **tube.screenZ exposed canvas-oriented (−ZADJ·RING_SCALE), not as the raw ROM word**
  - Spec source: DB-008 (pair-4 findings), AC-3
  - Spec text: "signed 16-bit screen offsets: well 0 0xFF40 = -192 …"
  - Implementation: tests pin `screenZ = −ZADJ × 300/112` in canvas-y ring units
  - Rationale: the near/far rings on the same interface are canvas-space; exposing the translate in ring units keeps one unit system per consumer and the shell needs no access to the private RING_SCALE
  - Severity: minor
  - Forward impact: the shell applies it directly inside the existing viewport transform.
- **The slide is pinned loosely (monotone, no overshoot, arrival ≤ 10 frames), not byte-exact ROM fixed-point**
  - Spec source: DB-008; INIWLS ALDISP.MAC:2492-2505 + ALWELG.MAC:75-84
  - Spec text: "ZADEST = (target − current) >> 3 per step" (16-bit with a fractional accumulator)
  - Implementation: tolerances admit a float port (fixed step delta/8 per ROM frame, clamp at target); an exponential ease (⅛ of REMAINING) still fails the arrival bound
  - Rationale: the fractional accumulator is a hardware artifact; the player-visible contract is the ~8-frame linear slide
  - Severity: minor
  - Forward impact: none.
- **New-life snap pinned at game start only; the mid-slide death→respawn snap is not covered**
  - Spec source: DB-008 claim
  - Spec text: "on a new life it snaps to the table value"
  - Implementation: initialState/early-frame snap pinned; death during the 8-frame slide unpinned
  - Rationale: our respawn does not re-run well init (the ROM's CNWLF2 path does), so the faithful behaviour is genuinely ambiguous in our architecture; not worth inventing a contract in RED
  - Severity: minor
  - Forward impact: flagged for tp1-10/Reviewer — the dive rework touches the same camera state.
- **Render APPLICATION of the translate and the star law is pinned by source-scan only**
  - Spec source: AC-2/AC-3/AC-6
  - Spec text: "the far ring is built about the projected vanishing point … the per-well screen-Z translation … the starfield reuses the same hyperbolic law"
  - Implementation: geometry/law pinned behaviourally in pure modules; render wiring pinned via `?raw` scans (`starPlaneScale(plane.z)`, linear law absent, `camera.screenZ` consumed)
  - Rationale: render() cannot run in the node env (phosphor scratch canvas needs `document`) — the 10-4/6-17 seam; the tp1-12 Reviewer showed scans are gameable, so the strong pins live in the pure halves
  - Severity: minor
  - Forward impact: Reviewer should eyeball the rendered well offset + star rush on the dev server.
- **DB-016 ruling: star pictures scale by 40/(z+24) with the 15%-visible spawn**
  - Spec source: DB-016 recommendation (offers two variants)
  - Spec text: "r = reach * (40/(z + 24) - 0.1515)/(1 - 0.1515), or just scale the picture by 40/(z+24) directly"
  - Implementation: tests pin the direct picture scale (spawn visibly at 15% reach), refuting the renormalised-to-zero variant; the cosmetic alpha/size ramps stay linear (out of scope — DB-016 is the radius law)
  - Rationale: the AC says "the SAME hyperbolic law", and DB-016's claim text calls out the arcade's 15%-at-spawn as part of the divergence
  - Severity: minor
  - Forward impact: none.

### Dev (implementation)
- **Corrected the RED suites' screenZ unit conversion (a Dev edit to TEA-owned tests)**
  - Spec source: DB-008 (pair-4 findings) + WORSCR, ALDISP.MAC:2049-2051 / :2274
  - Spec text: "SCREEN Z = [FACTOR/(PY-EY)]*(PZ-EZ)+SZCENT" — ZADJ is added POST-divide, in ROM screen units (FACTOR = 256)
  - Implementation: tube.screenZ = −ZADJ·(16+H)·RING_SCALE/256 (rim-relative), replacing the RED draft's −ZADJ·RING_SCALE (a world-unit misread, 6.4× too large on the circle); the corrected literals are commented and attributed in both test files
  - Rationale: the RED value put the well 71% of the viewport off-centre — unfaithful and unshippable; the tests' intent (faithful per-well translate) is preserved, only the conversion factor moved. Test edits are normally TEA's, so this is logged as a blocking Delivery Finding for the Reviewer to adjudicate
  - Severity: major
  - Forward impact: TEA's assessment text above still describes the −ZADJ·RING_SCALE form (append-only rule — not rewritten); the tests and code carry the corrected derivation.
- **Starfield re-anchored to the scene origin and reach re-based to scene units**
  - Spec source: DB-016; DSTARF ALDISP.MAC:2945-2948
  - Spec text: "PXL = PZL = 0x80 ('CENTER OF WORLD')"
  - Implementation: drawStarfield now draws about (0,0) with reach = 720·0.6 scene units; the old (W/2, H/2) anchor + hypot(W,H) reach predated the phosphor centre-origin transform and displaced the field half a screen
  - Rationale: no test pins the anchor (render cannot run in node), but the hyperbolic law the AC demands is unobservable at the wrong anchor; leaving a seen defect in the exact lines under edit would be dishonest
  - Severity: minor
  - Forward impact: star positions remain untested; flagged in Delivery Findings.
- **The slide steps once per stepGame call, not dt-scaled**
  - Spec source: DB-008; ALWELG.MAC:75-84
  - Spec text: "ZADJL += ZADEST every frame"
  - Implementation: stepCamera advances one fixed step per stepPlaying call (the qframe convention: one call = one ROM frame under the fixed-step loop), clamping at the target
  - Rationale: matches the sim's existing per-call frame cadence (s.qframe += 1) rather than inventing a dt-proportional ramp; TEA's tolerances admit either
  - Severity: minor
  - Forward impact: none — the shell loop drives stepGame at SIM_STEP where ROM_FPS·SIM_STEP = 1.
- **Sibling re-seat + fixture staging completed by Dev**
  - Spec source: tests/core/geometry.cycle.test.ts (Story 3-2/6-7 era) + tp1-9.camera-slide fixture
  - Spec text: "keeps the original circle size: near rim radius ~300, far ~60"
  - Implementation: the far~60 pin was the OLD 300·0.2 far ring — the very DB-006 divergence this story removes; re-seated to the ROM-derived literal (VP displacement + 300·R ≈ 154.55). The slide fixture now stages the parked camera a mid-level board has (playingState is a level-1 state; direct level mutation must carry the camera)
  - Rationale: both were missed by the RED sweep and turned red only once the implementation landed; intents preserved (size pin → ROM-faithful size pin; staging → complete staging)
  - Severity: minor
  - Forward impact: none.

### Reviewer (audit)

Verdict context: the branch is SUPERSEDED (see Reviewer Assessment) — these stamps travel with the tp1-31 re-scope, where the deviations' content survives.

- **TEA: Concrete API names pinned** → ✓ ACCEPTED by Reviewer: sound at the time; now moot for tp1-9 (upstream's `farRatio` API won on develop). The eye-DATA design question moves to tp1-10 (see Reviewer Delivery Finding).
- **TEA: makeCircleTube derives a consistent eye** → ✓ ACCEPTED by Reviewer: correct instinct — upstream independently made the same move (`farRatio: farRadius / nearRadius`), which corroborates the derived-consistency principle.
- **TEA: tube.screenZ canvas-oriented** → ✓ ACCEPTED AS AMENDED by Reviewer: the orientation/unit-system choice was right; the conversion factor was wrong and Dev's correction is verified (see next stamp).
- **TEA: slide pinned loosely** → ✓ ACCEPTED by Reviewer: the tolerances exclude an exponential ease and admit the ROM's fixed step — the right looseness.
- **TEA: new-life snap pinned at game start only** → ✓ ACCEPTED by Reviewer: carry the open respawn question into tp1-31/tp1-10.
- **TEA: render application scan-pinned only** → ✓ ACCEPTED by Reviewer: correct given the node-env constraint; the two visual defects I found (glow anchor, starfield displacement) are exactly the class scans cannot catch — noted as findings, not as a deviation failure.
- **TEA: DB-016 direct picture scale** → ✓ ACCEPTED by Reviewer: upstream's independent implementation chose the same faithful variant (40/(z+24), 15% at spawn) — two clean-rooms agree.
- **Dev: corrected the RED screenZ unit conversion** → ✓ ACCEPTED by Reviewer (this was the blocking finding): independently re-derived from WORSCR — header formula ALDISP.MAC:2049-2051, FACTOR=256 via the high-byte load (`STA MZLH`, :2243/2268), `ADC ZADJL` post-divide (:2274). The corrected −ZADJ·(16+H)·S/256 is the scale-invariant rim-relative port; the RED draft's −ZADJ·S was a world-unit misread (6.4× on the circle). Dev editing TEA-owned tests was handled correctly: attributed in-file, logged as deviation + blocking finding, intent preserved. The tp1-31 re-scope MUST carry the corrected derivation (see Devil's Advocate).
- **Dev: starfield re-anchored to scene origin** → ✓ ACCEPTED by Reviewer: verified against DSTARF (PXL=PZL=0x80) and beginScene's centre-origin transform; develop still has the displaced anchor, so this fix gains value in tp1-31.
- **Dev: slide steps per call** → ✓ ACCEPTED by Reviewer: matches the qframe per-call convention; ROM_FPS·SIM_STEP=1 makes it exact under the shell loop.
- **Dev: sibling re-seat + fixture staging** → ✓ ACCEPTED by Reviewer: both intents preserved; the geometry.cycle far~60 pin was itself the DB-006 divergence.