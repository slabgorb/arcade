# Story jt12-3 Context

## Title
Wire the transporter player-side service queue + SELARE occupancy safety: route respawnPlayerProcess (sim.ts:630) through takePlayerNumber + nextServed arbitration (players before enemies), and gate serveEnemy on spawnProceeds(scanAreas(occupantYs), tier) so a materialisation defers into an already-crowded third. Assert the now-live 'players first' branch and that a crowded third defers a spawn. JOUSTRV4.SRC:5615-5676

## Metadata
- **Story ID:** jt12-3
- **Type:** story
- **Points:** 8
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Joust — wire the unwired RV4 fidelity mechanics: baiter anti-farming, smart-enemy budget refund, transporter player queue

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach

**SM-verified facts (measured against the current tree before handoff — trust these over the title's locators):**

1. **This is a WIRING story, not a build story.** Every pure function it wires ALREADY EXISTS
   in `plugins/joust/src/core/transporter.ts`, shipped by the now-`done` jt12-1/jt12-2:
   - `scanAreas(occupantYs)` — `transporter.ts:139`
   - `spawnProceeds(occ, tier)` — `transporter.ts:149`
   - `takePlayerNumber(q)` — `transporter.ts:179`
   - `serveEnemy(q)` — `transporter.ts:207`
   - `nextServed(q)` — `transporter.ts:216`
   - SELARE empty-third boundaries + `selectArea` — `transporter.ts:95-141`
2. **Line-anchor drift:** the title's `respawnPlayerProcess (sim.ts:630)` is actually
   **`plugins/joust/src/core/sim.ts:644`** today (14-line drift). The ROM anchor
   `JOUSTRV4.SRC:5615-5676` is correct and is the fidelity source.
3. **Partial wiring already present:** `sim.ts` already imports `serveEnemy` (line 121) and
   calls it (line 1440). The work COMPLETES the arbitration (route `respawnPlayerProcess`
   through `takePlayerNumber` + `nextServed`, players-before-enemies) and ADDS the occupancy
   gate (`spawnProceeds(scanAreas(occupantYs), tier)`). Confirm the existing call site rather
   than assume nothing is wired.

_Approach hints to be refined by TEA/Dev. The story title above defines the intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story jt12-3` from the sprint YAML._
