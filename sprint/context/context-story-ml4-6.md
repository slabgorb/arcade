# Story ml4-6 Context

## Title
Extract the shared BEE-family helpers (BEEMV1 mushroomsNeeded, BEEMV2 spawnH, BEEOFF, COMP) into one millipede core helper module. The ml4-1 third-consumer trigger is met: dragonfly/mosquito/bee/earwig now hold byte-identical module-local copies (MILLI.MAC:166-171, :179-194, :228-234) and the four suites pin the behaviour, so extraction is a pure refactor under an existing net. Filed by the ml4-3 review (TEA Delivery Finding routed the decision).

## Metadata
- **Story ID:** ml4-6
- **Type:** refactor
- **Points:** 2
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Millipede — the menagerie + DDT (phase 4b): one cited reducer per critter, and the DDT bomb

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria

> ⚠ **Citation cohesion:** The four BEE-family functions (`mushroomsNeeded`, `spawnH`, `beeOff`, `comp`) currently have inline citation-tag comments that differ per file (e.g., `spawnH` is tagged `BE-39` in `bee.ts`, `DF-34` in `dragonfly.ts`, and bare in `mosquito.ts`). When extracted to a shared helper module, select one canonical citation form for each function.

> ⚠ **Core boundary:** The extracted helper module must live under `src/core/` (not `src/shell/`) so it remains within the sim purity boundary. The millipede core-purity guard (`plugins/millipede/tests/core-boundary.test.ts`) scans `src/core/` source text.

> ⚠ **Scope note on `comp`:** The story title mentions `comp` (two's-complement). Four consumers use `comp`: beetle, dragonfly, earwig, AND inchworm. The extracted module's sweep should explicitly decide whether to fold beetle/inchworm into the extraction too, or leave them as module-local. Do not silently miss either.

_No explicit acceptance criteria in sprint YAML — TEA to define test specifications during the RED phase. The nuances above capture constraints from the verified premise._

---
_Generated by `pf context create story ml4-6` from the sprint YAML._
