# Story mc8-4 Context

## Title
Parametric drones + full event wiring (bonusTick, whoop, end-game, bonus-city, low-ammo, cruise/Sputnik drone sweep)

## Metadata
- **Story ID:** mc8-4
- **Type:** story
- **Points:** 5
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Missile Command — authentic audio (W3SOUN + POKEY)

## Background

**ALREADY WIRED by mc8-2** (do NOT write tests expecting these to fail first time):
- `bonusTick` → audio.play('bonus-tick') [audio-dispatch.ts:43]
- `ammoEmpty` (low-ammo klaxon) → audio.play('no-fire') [audio-dispatch.ts:40]
- Cruise/Sputnik drone SUSTAINED VOICE lifecycle: start/stop only, silenced at phase='over' [audio-dispatch.ts:62-73]
- SoundEvent union is EXACTLY 6 kinds: launched, detonated, icbmKilled, structureDestroyed, ammoEmpty, bonusTick [sound-events.ts:63-69]

**BOUNDARY (core/shell purity contract)**
- New cues must be surfaced as PURE DATA on GameState (new SoundEvent kind OR edge-detectable state field), NEVER a callback or clock read
- Phase = 'play' | 'between' | 'over' [core/state.ts:13]
- CRITICAL: game.ts:129 returns `soundEvents: []` when phase === 'over' → end-game cue CANNOT use the SoundEvent stream; it must edge-detect the phase transition (same pattern as drone-stop)
- Purity.test.ts must stay green (source-text scanner enforces core/shell boundary)

## Problem

Core audio wiring is incomplete: the new-wave cue (whoop), game-over voice (end-game), city-bonus award (bonus-city), and parametric pitch sweep of the cruise drone are missing. mc8-2 shipped lifecycle-only drone silencing; this story adds the descending-pitch sweep.

## Technical Approach

1. **Wave transitions (whoop):** Edge-detect 'between'→'play' or waveNo increment on each frame; emit whoop cue to audio shell (new SoundEvent kind or state field)
2. **Game-over (end-game):** Edge-detect phase → 'over'; emit end-game cue via state (NOT via SoundEvent stream, which is empty on over-frame)
3. **Bonus city (bonus-city):** Expose reserve bonus-city award as pure data on GameState; shell detects edge and plays cue
4. **Parametric drone sweep:** Implement descending pitch ramp (from base POKEY frequency → silence at game end); modulate AUDF1 frequency in audio.ts proportional to time-remaining-in-phase-'play'
5. **Stale citation fix:** Update audio.ts:46 comment from "mc8-3" to "mc8-4" (renumber drift correction)

## Scope
- **In scope:** new-wave whoop, game-over voice, bonus-city cue, parametric drone pitch sweep, stale-comment fix
- **Out of scope:** asset baking or live verification (defer to mc8-3); orchestration of multiwave sequences; city/base destruction cues (already wired via SoundEvent)

## Acceptance Criteria
1. **Whoop cue for wave entry:** When a new wave starts (waveNo increments or phase='between'→'play'), audio-dispatch emits a 'whoop' sound; a test edge-detects the transition and asserts the cue fires exactly once per wave, surfaced as pure state not a callback
2. **End-game cue isolation:** When phase transitions to 'over', audio-dispatch emits 'end-game'; the cue is edge-detected from state change (not attempted via soundEvents stream); a test proves the stream is empty on the over-frame and the shell still voices the cue
3. **Bonus-city cue for reserve award:** GameState exposes a new field tracking the bonus-city award (or reuses a pure-data SoundEvent kind); shell edge-detects the value change and plays 'bonus-city'; test confirms the cue fires at the correct reserve threshold (matching mc4-5's citiesLost/reserve logic)
4. **Parametric drone sweep:** The sustained 'drone' voice exhibits descending pitch (parametric AUDF1 frequency modulation); shell modulates frequency proportional to time-remaining-in-play-phase; audio seam test confirms the pitch ramps smoothly and silences at phase='over' (same as existing drone-stop test shape)
5. **SoundEvent boundary preserved:** core/sound-events.ts remains EXACTLY 6 kinds; any new cues are surfaced as pure state fields (not new SoundEvent kinds) OR the enum is expanded and documented; purity.test.ts stays green
6. **Stale citation corrected:** audio.ts:46 comment is updated from "mc8-3" to "mc8-4" (citation gate will verify the renumber)

---
_Generated by `pf context create story mc8-4` from the sprint YAML._
