# Story sw7-21 Context

## Title
R-LOCK The green lock-on circle is a non-ROM aim-assist, not authentic — rule remove vs keep-as-labelled; the code overclaims cabinet provenance (Object_12 = exhaust port, not a lock box)

## Metadata
- **Story ID:** sw7-21
- **Type:** chore
- **Points:** 2
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** star-wars
- **Epic:** Star Wars — primary-source fidelity: the ruling sheet from the 2026-07-15 audit

## Problem & Ruling

NEW fidelity finding (NOT in the 2026-07-15 audit; surfaced 2026-07-17 while reviewing sw7-17). The green LOCK-ON CIRCLE that rings the TIE under the reticle (isLocked / lockedEnemy / LOCK_RADIUS_NDC=0.12, story 8-14; drawn by render.ts drawLockOn) is a NON-ROM predictive aim-assist, and the code claims cabinet provenance it does not have.

**EVIDENCE:**
1. No `lock` or `lock-on` anywhere in the 1983 source — grep hits are only inside the word BLOCK in the DIVTST/MATEST/SND test files.
2. The one object ever cited as a targeting-reticle / lock-on box — Object_12 (three concentric squares) — is actually .WP PORT (THERMAL EXHAUST PORT) per WSOBJ.MAC; that guess was SUPERSEDED in sw5-4.
3. The ROM gives ONLY the site crosshair (WSSITE.MAC) plus a POST-hit flash (LZ.HIT, the still-unported G-008) — feedback AFTER a hit, never a predictive "you-have-this-shot."

**RULING (made 2026-07-17 — NOT OPEN):**
The Jedi has decided: **REMOVE the predictive lock ring for authenticity.** The false cabinet-provenance claim in gameRules.ts LOCK_RADIUS_NDC docstring must be fixed. The story is TDD (not trivial) because the old 8-14 lock-on tests must be removed or refactored.

**IRONY TO PRESERVE:** The QUANTITY the ring is computed from (the nearest object under the site) IS authentic — it is CLSLZ / min(CL.GDS, CL.ADS), the exact hitscan targeting sw7-17 ported. The cabinet simply never draws a ring around it. Preserve the hitscan targeting; remove only the drawn ring and the false provenance claims.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story sw7-21` from the sprint YAML._
