# Story cp2-11 Context

## Title
Remove the LEVEL indicator — the arcade HUD shows no level text

## Metadata
- **Story ID:** cp2-11
- **Type:** bug
- **Points:** 1
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** centipede
- **Epic:** Centipede — the train (MOTION/OBSTAC/NEWHD/EXPLOD, poisoned dives, death + RESTOR) + cp1 escape fixes

## Problem
USER-REPORTED (2026-07-19, live play + reference screenshot of the real cabinet): our clone draws a 'LEVEL n' text label (cp1-6 HUD); the arcade shows NO level indicator anywhere. ROM ANCHORS (verified 2026-07-19, vendored tree reference/atari-source/centipede/revision.v4/): a full-tree grep proves no LEVEL/WAVE/ROUND/STAGE string or digit-writer exists — those words appear only in comments (DIP bonus-level CENTI4.MAC:240; wave-colour CENIR4.MAC:311). The COMPLETE display set is the message table MESG (CENIR4.MAC:16, strings MESS0..MES10 at :67-110 — player/coinage/game-over/enter-initials/bonus-every/high-scores/great-score/credits/2-credit-minimum) plus copyright (CENTI4.MAC:12), drawn via MESS (CENIR4.MAC:111), CHAR (:203), DIGIT2/DIGITZ (:227/:237), and the HUD routines UPSCRE (CENTI4.MAC:2629), DLIVES (:920), SCORES (:1893), BONUS (:225). The negative claim ('no level text in the display set') is net-new — no existing claims entry covers it; add one citing the enumeration. Remove the LEVEL glyph run from the shell HUD; adjust/retire any render pins asserting it; nothing on screen the cabinet doesn't draw. Pairs with cp2-12 (same HUD area/test files — land adjacent or together).

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

Current implementation (as of 2026-07-19, pre-fix): the offending draw call is
`src/shell/render.ts:100` — `drawText(ctx, atlas, 'LEVEL 1', TILE_W, LOGICAL_H - TILE_H)`,
sitting directly below the SCORE draw (`render.ts:99`). It was added under cp1-6
(HUD) with no ROM anchor; this story's quarry (see Problem above) proves no such
string/digit-writer exists anywhere in the rev-4 tree. The fix is a deletion, not
a rewrite: remove the `drawText(..., 'LEVEL 1', ...)` line (and any now-unused
locals it alone required) so nothing is drawn in that HUD slot the cabinet
doesn't draw.

No existing test currently pins the literal string `'LEVEL'` against
`src/shell/render.ts` — `tests/render.test.ts` imports the module's source text
via the repo's `render.ts?raw` idiom (see `tests/render.test.ts:24`, used for
source-wiring assertions like "no `@arcade/shared/font` import", "uses
`layoutText`", "colours come from the palette module") but has no assertion
on the LEVEL text itself, so TEA is writing new RED coverage, not fixing a
currently-green false pin. (The only other `'LEVEL'` hits in the repo are
unrelated: `tests/layout.test.ts:123-124` exercises `layoutText('LEVEL', ...)`
purely as a 5-glyph string fixture for the text-layout algorithm, and
`tests/centipede.test.ts:279` uses "LEVEL" as an English word in a test
description, not a HUD string.) TEA should add a RED assertion that
`render.ts?raw` source text contains no `LEVEL` glyph run / HUD draw for it,
plus a state-scan test across attract/play/death/game-over confirming no
`'LEVEL'` text is ever emitted, per AC-1.

## Scope
- In scope: deleting the `'LEVEL 1'` HUD draw in `src/shell/render.ts`;
  adding/adjusting render source-text scans and HUD render-pin tests so the
  absence is pinned (not just incidentally true); adding the net-new negative
  claim (no LEVEL/WAVE/ROUND/STAGE text in the display set) to the claims
  dossier per schema, citing the MESG enumeration (MESS0..MES10,
  CENIR4.MAC:67-110) and copyright (CENTI4.MAC:12) as the complete positive
  display set.
- Out of scope: any other HUD element (SCORE, lives/DLIVES gun icons, per-round
  colours) — those are governed by their own stories/citations and must not be
  touched here.
- **Pairs with cp2-12** (same HUD area / same test files, per-round colours) —
  land adjacent to or together with cp2-12 to avoid two agents fighting over
  `src/shell/render.ts` / `tests/render.test.ts` diffs in flight.

## Acceptance Criteria
- No LEVEL text renders in any state (attract, play, death, game-over); the render source-text scans and HUD pins are updated, none weakened.
- Suite + citations + build green; core untouched.

---
_Generated by `pf context create story cp2-11` from the sprint YAML._
