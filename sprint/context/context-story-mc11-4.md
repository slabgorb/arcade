# Story mc11-4 Context

## Title
Retire the redundant dead twins (delete or route the live path through them, per each): the mainline/stateCode MAINLINE-dispatch skeleton (state.ts:92, bypassed by stepGame), applyPointerMotion (shell/input.ts:42, superseded by placeCursor), launchFromKey (input.ts:69, superseded by fireFromKey), cruiseKillPoints (core/score.ts:61, inlined at game.ts:585), computeLetterbox (shell/viewport.ts:53, twin of applyLetterbox), and INITIAL_ATTRACT (state.ts:121). Keep any symbol still asserted by a claim test; only its dead runtime role goes.

## Metadata
- **Story ID:** mc11-4
- **Type:** story
- **Points:** 3
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Missile Command — wire the post-mc5 unwired seams: sputnik cruise-borrow, bonus count-up cue, name-entry timeout; retire dead twins

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach

**Measured Dead Twins (verified against current tree):**

| Dead twin (cited) | Live path that replaced it | Test(s) referencing it |
|---|---|---|
| `mainline` (state.ts:92) + `stateCode` (state.ts:77) | `stepGame` (game.ts:344) is the live per-frame reducer; `mainline` has no src caller, `stateCode` is called ONLY by `mainline` itself | tests/state-mainline.test.ts (surface/claim test), tests/pause.test.ts (behavioral: mainline/stateCode via togglePause) |
| `applyPointerMotion` (shell/input.ts:42) | `placeCursor` (core/cursor.ts:91), wired at main.ts:89 | tests/input.test.ts (behavioral) AND **tests/place-cursor.test.ts:206 — a CLAIM TEST that pins it retained** |
| `launchFromKey` (shell/input.ts:69) | `fireFromKey` (shell/input.ts:83, wired at input.ts:144) | tests/fire.test.ts, tests/fire-ammo.test.ts (behavioral) |
| `cruiseKillPoints` (core/score.ts:61) | live scoring uses `scoreKills` + `CRUISE_SCORE_MULT` (score.js), consumed in game.ts | tests/cruise.test.ts (behavioral) |
| `computeLetterbox` (shell/viewport.ts:53) | `applyLetterbox` (viewport.ts:75), wired at main.ts:37 | tests/mc10-5-letterbox-aspect.test.ts (behavioral) |
| `INITIAL_ATTRACT` (state.ts:121) | `INITIAL_PHASE` (state.ts:120), wired at game.ts:180; INITIAL_ATTRACT is FULLY dead (only its definition + the claim test reference it) | tests/state-mainline.test.ts (claim) |

**Critical nuance — symbol retention per claim-test assertion:**

`applyPointerMotion` is ALREADY pinned-as-retained by an existing claim test: `tests/place-cursor.test.ts:206` asserts `/export function applyPointerMotion\b/` must match, with the comment "The story keeps it even though main.ts stops calling it." Per the story rule — "Keep any symbol still asserted by a claim test; only its dead runtime role goes" — `applyPointerMotion` MUST NOT be deleted; its runtime role is already dead. The story explicitly anticipates per-symbol judgment: "delete or route the live path through them, per each". Each referencing test must be assessed: is it a "keep the symbol defined" claim test (symbol stays) or a behavioral test that moves/deletes with the symbol?

## Scope
- In scope: the per-symbol judgment of six dead twins (delete or route), respecting claim-test retention assertions.
- Out of scope: unrelated changes.

## Acceptance Criteria
- All six dead twins are assessed per their test references (claim vs behavioral).
- Symbols retained by claim tests are kept (their runtime role removed).
- Symbols not protected by claim tests are deleted.
- Dead runtime roles are removed or the live path is routed through them.
- Purity.test.ts and other test suites remain green.
- REV-01 citations remain intact.

---
_Generated by `pf context create story mc11-4` from the sprint YAML._
