# Story ml13-1 Context

## Title
Segment kills leave a mushroom (MUSHER) at the dead cell — both shot-kill and DDT-kill paths

## Metadata
- **Story ID:** ml13-1
- **Type:** bug
- **Points:** 2
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Millipede segment-kill scoring & mushroom fidelity (ml12-3 follow-ups)

## Problem
Follow-up to ml12-3 (DDT-kill path now working): neither the shot-kill nor the DDT-kill path plants a mushroom (MUSHER) at the dead segment's cell as the ROM does. In the ROM, SHOOT2's millipede branch (around MILLI.MAC:2148-2176) leaves a mushroom via `JSR MUSHER` (MILLI.MAC:2157) for BOTH kill paths (shot and DDT both route through SHOOT2 142$). This applies to both:
- Shot-kill path: plugins/millipede/src/core/sim.ts lines 177-185 (`segments.filter(...)` + `score += SEGMENT_PTS` + `segment-killed` event) — no mushroom write.
- DDT-kill path: plugins/millipede/src/core/sim.ts step 8b, lines 282-295 — no mushroom write.

Without the mushroom plant, killed trains fail to thicken the mushroom field as the ROM does. The mushroom stamp helper `FULL_MUSHROOM` already exists in plugins/millipede/src/core/mushroom.ts (line 74: `field[addr] = FULL_MUSHROOM | (cell & BACKGROUND_BIT)` preserves the grey background bit).

## Technical Approach
Both kill paths should plant `FULL_MUSHROOM` at the killed segment's cell, mirroring the ROM's MUSHER behavior. Watch the cell math:
- The DDT kill reads the OCCUPIED cell via obstacOffset(s.h, s.v, 0).
- The shot kill matches on the shot position.
Keep the core/shell purity intact.

## Scope
- In scope: Both shot-kill and DDT-kill paths planting a mushroom at the dead segment's cell.
- Out of scope: Scoring (that is ml13-2); live playtest verification (that is ml13-3); other mushroom behaviors.

## Acceptance Criteria
1. A shot kill of a millipede segment plants a full mushroom at the dead segment's cell (core test: kill a segment, assert the field cell now reads FULL_MUSHROOM), cited to the millipede ROM (MUSHER, MILLI.MAC) and gated by citations.test.ts where a new constant is introduced.
2. A DDT-cloud kill of a segment likewise plants a full mushroom at the dead cell (same observable), verified distinct from and not regressing the ml12-3 kill/score behaviour.
3. No regression: the existing millipede suite (kill removal, scoring, mushroom-field counts, wave-clear) stays green. NOTE (corrected during review vs the original assumption): the ROM plants at OBSTA0's cell — the cell 8px AHEAD in the segment's travel direction (`obstacOffset(h, v, dh<0?-1:1)`, MLSUB.MAC:834-839/860-863), NOT the segment's own cell; and BOTH the shot-kill and DDT-cloud kill route through the same SHOOT2 142$ → MUSHER tail, so both plant (at the cell-ahead, when that cell is empty).

---
_Generated by sm-setup from the sprint YAML and ground-truth ROM notes._
