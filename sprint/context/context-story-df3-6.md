# Story df3-6 Context

## Title
VISUAL playtest — the ship flying a scrolling, wrapping world: screenshot http://127.0.0.1:5270/defender/ showing the ship over the moving starfield, thrust scrolling the world, reverse flipping facing + slide, a laser in flight — compared against a nonsense control path (must DIFFER, not just 200 — the canonical-serve lesson). Confirm scroll direction, ship-leads offset, vertical clamp and colour BEFORE enemies (playbook sec 4).

## Metadata
- **Story ID:** df3-6
- **Type:** story
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender — the ship + the scheduler (phase 4a)

## Problem

df3-1..df3-5 make the ship fly on a live world, but green unit tests do not prove
the thing *looks* right — scroll direction, the ship-leads offset, the vertical
clamp and colour are exactly the orientation traps the playbook says to catch with
a **visual playtest before physics grows further** (`docs/playbooks/next-sprite-game.md`
§4). This story is that gate: drive the running app and look, then carry the
accessibility ruling forward so downstream epics inherit it.

## Technical Approach

- **Serve this checkout.** `just serve` (or `npx vite --port 5290 --strictPort` if
  5270 is held by another checkout — verify whose tree answers, per `CLAUDE.md`
  "prove whose server answers"). Defender is at `/defender/`.
- **Drive and capture.** Playwright MCP headless (`claude-in-chrome` is not
  connected in this environment — memory `arcade-visual-playtest-via-playwright`).
  Screenshot the ship over the moving starfield; capture frames showing: **thrust
  scrolls the world**, **reverse flips facing and the ship-leads slide**, **a laser
  in flight**, and the **vertical clamp** (ship can't leave the strip).
- **DIFFER, not 200.** Compare `/defender/` against a **nonsense control path**
  (e.g. `/defender-xyzzy/`) and assert the rendered output **differs** — an all-200
  sweep proves nothing because the lobby SPA fallback answers 200 to everything
  (`CLAUDE.md`; the mechanical DIFFER check already runs in
  `tests/canonical-serve.test.mjs`).
- **Confirm the orientation facts** from df3-2 by eye: scroll direction matches
  thrust, the ship sits *ahead* in its facing direction (the `$20`/`$70` lead),
  vertical is clamped `[YMIN+1,238]`, colours resolve through the palette (no
  invented hex).
- **Carry the accessibility note forward:** record that `df4`/`df5` must render
  smart-bomb/hyperspace as freeze/fade, **no full-screen strobe** — `df3` itself
  shows none.

## Scope

- **In scope:** a visual playtest of the running `/defender/` — screenshot proof of
  ship + scroll + wrap + laser + vertical clamp; the DIFFER-vs-control check; the
  accessibility forward-note.
- **Out of scope:** any code fix beyond trivial orientation tweaks surfaced by the
  playtest (file a follow-up if a real bug appears); enemies/HUD/attract (df4+).

## Acceptance Criteria

- [ ] A screenshot of `http://127.0.0.1:5270/defender/` (or the verified alt port)
      shows the ship over the **moving** starfield, captured across thrust (world
      scrolls), reverse (facing + slide flip), and a laser in flight.
- [ ] The vertical **clamp** is visible: the ship cannot leave `[YMIN+1,238]`.
- [ ] `/defender/` output is compared against a **nonsense control path** and
      **DIFFERS** (not merely both 200 — the canonical-serve lesson).
- [ ] Scroll direction, ship-leads offset and colour (palette-resolved, no invented
      hex) are confirmed by eye and recorded.
- [ ] The `df4`/`df5` accessibility ruling is carried forward as a note (freeze/fade,
      no full-screen strobe); `df3` shows no strobe.
- [ ] Any orientation bug found is either fixed inline (trivial) or filed as a
      follow-up story (non-trivial) — not silently left.

## Scope dependencies

Runs **last** in df3, after df3-1..df3-5 land. Pure verification/gate story.

## References

- **Epic context:** `sprint/context/context-epic-df3.md` — sequencing (last), accessibility forward-note.
- **Design spec:** `…-df3-ship-scheduler-design.md` §4 (df3-6), §1 (playbook §4 gate).
- **Serve + DIFFER:** `CLAUDE.md` (Serving the arcade; "prove whose server answers"), `tests/canonical-serve.test.mjs` (the mechanical DIFFER check).
- **Playtest tooling:** memory `arcade-visual-playtest-via-playwright` (Playwright MCP headless; claude-in-chrome not connected).
- **Sibling precedent:** `df2-6` (the df2 still-frame visual gate).

---
> **Architect note:** this mirrors `df2-6`'s visual gate one epic later — the difference is `df3` is *moving*, so the capture must show scroll/wrap/facing across frames, not a single still. No new ROM constants; nothing to enroll in `claims/*.json` unless an inline fix adds one.

_Generated by `pf context create story df3-6`; body authored by Architect (df3 design spec)._
