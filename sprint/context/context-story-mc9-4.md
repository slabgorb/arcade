# Story mc9-4 Context

## Title
Authentic stroke-font HUD and screen layout

## Metadata
- **Story ID:** mc9-4
- **Type:** story
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Missile Command — render fidelity (real graphics)

## Problem
Retire the monospace HUD. Replace render.ts's `${hud}px monospace` score/ammo readouts with the cabinet's stroke-font glyphs and the authentic on-screen layout (score position, per-base ammo, wave/multiplier if mc4 has landed). Reuse @shared/font if it fits; otherwise a missile-command-only glyph module in src/shell (extract to src/shared only if a second game proves the duplication — the CLAUDE.md eligibility bar). Cite CLEAR SCREEN / the text-draw path (W3DSUP.MAC:1712 and the alphanumeric draw). The HUD-figure rule still holds: the score drawn is the core's state.score verbatim. Shell-only. Depends on mc3 (and reads mc4's wave/multiplier if present).

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- the HUD renders in the cabinet stroke font (not monospace) at the authentic layout; the score drawn is the core state.score verbatim (HUD-figure rule), never a re-derived copy.
- the text path cites its W3DSUP source; the glyph source reuses @shared/font or is a documented missile-command-only module (no premature src/shared extraction).
- render tests assert the HUD content against the canvas-mock harness; shell-only, purity.test.ts stays green.

---
_Generated by `pf context create story mc9-4` from the sprint YAML._
