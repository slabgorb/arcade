# Story df7-6 Context

> ⚠ **DO NOT REGENERATE THIS FILE.** The Technical Approach, Scope, Dependencies and
> Design Notes are Architect-enriched from the df7 epic plan. `pf context create`
> refills Technical Approach/Scope with placeholder text and would overwrite them.

## Title
2P alternating handoff (df5 Decision D lands here): the death->next-player transition for a two-player game (ST2 *TWO PLAYER START defender/DEFA7.SRC:1112; PLAYER START PROCESS :1206; player-switch P1SW/P2SW :1179). Each player keeps their own men/score/world; on death the phase machine hands off to the other player until both are out. The COCKTAIL screen-flip is DOCUMENTED-NOT-PORTED (desktop-only, upright — Decision D). Consumes df7-1 phase machine + df5-3 score/men.

## Metadata
- **Story ID:** df7-6
- **Type:** story
- **Points:** 2
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender phase machine + attract + wiring + HUD + showcase (df7, phase 6-7): turn df1-df6's pure cores into a played-start-to-finish cabinet — the attract->setup->play->death->game-over phase machine (pure-first, pm4/mc6 model), the self-playing attract demo, the hall-of-fame name-entry flow, the HUD + scanner render, 2P alternating handoff, lobby showcase, and the full-lifecycle visual playtest

## Problem
df5 Decision D deferred 2P alternating handoff to df7 as phase-machine territory. Two players alternate on death, each with their own men/score/world snapshot; the machine cycles P1->P2->P1 until both reach game-over. The ROM cocktail screen-flip (P1SW/P2SW drive it) is documented in the design spec but NOT ported — the arcade is desktop-only/upright. Single-player (df7-1..df7-5) is unaffected; 2P is an additive path.

## Technical Approach
df5 Decision D deferred 2P alternating handoff to df7 as phase-machine territory.

- **Alternation.** Two players take turns on death, each with their own men / score / world
  snapshot. The df7-1 machine cycles `P1 → P2 → P1` on each death until both reach
  game-over, then the mainline enters `game-over`. Single-player (df7-1..df7-5) is an
  unchanged special case; 2P is an **additive** path.
- **Cite the ROM.** `ST2 *TWO PLAYER START DEFA7.SRC:1112`; `PLAYER START PROCESS :1206`;
  the player-switch `P1SW/P2SW :1179`. Constants → `claims/*.json`.
- **The cocktail flip is documented-not-ported (Decision D).** `P1SW/P2SW` drive the ROM's
  cocktail screen-flip; the arcade is desktop-only/upright, so the flip is **intentionally
  omitted** — a design note records the citations and the omission; no flip code ships.
- **Purity.** Per-player state snapshots are pure data; purity.test.ts green.

## Scope
- **In scope:** the 2P alternating-on-death handoff with independent per-player men/score/
  world, the `ST2`/`PLAYER START PROCESS`/`P1SW-P2SW` citations + `claims/*.json`, the
  documented-not-ported cocktail-flip note, a test that 1P is unaffected.
- **Out of scope:** the cocktail screen-flip itself (documented, not built); any new HUD for
  2P beyond reusing df7-5; the shared hall-of-fame board (df7-4 owns it).

## Acceptance Criteria
- AC1: a two-player game alternates on death — each player keeps their own men/score/world, and the df7-1 phase machine cycles P1->P2 on each death until both are out, then game-over; a test pins the alternation and the independent per-player men/score.
- AC2: the transition is cited to the ROM (ST2 *TWO PLAYER START defender/DEFA7.SRC:1112; PLAYER START PROCESS :1206; P1SW/P2SW :1179) with claims/*.json under the df1-1 gate.
- AC3: the COCKTAIL screen-flip is DOCUMENTED-NOT-PORTED (Decision D) — a design note records the P1SW/P2SW flip citations and states the arcade is desktop-only/upright so the flip is intentionally omitted; no flip code ships.
- AC4: single-player play (df7-1..df7-5) is unchanged by the 2P path (a test pins the 1P mainline still holds); purity.test.ts stays green.

## Dependencies
- **df7-1** — the phase machine that cycles P1→P2 on death.
- **df5-3** — `score.ts` (per-player men/score).
- **df3** — `sim.ts` (per-player world snapshots).
- **Blocks:** nothing hard; df7-7's playtest is single-player-focused (2P is additive).

## Design Notes
- **Decision D:** the 2P handoff lands here (deferred from df5); the **cocktail screen-flip
  is documented-not-ported** — desktop-only, upright. Record the `P1SW/P2SW` citations and
  the omission; ship no flip code.
- Single-player must stay unchanged (a test pins it); citations gated; purity green.

---
_Generated by `pf context create story df7-6` from the sprint YAML._
