# Story cp2-14 Context

## Title
Global orientation flip — cellScreenX + input sign chain match the upright cabinet

## Metadata
- **Story ID:** cp2-14
- **Type:** bug
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** centipede
- **Epic:** Centipede — the train (MOTION/OBSTAC/NEWHD/EXPLOD, poisoned dives, death + RESTOR) + cp1 escape fixes

## Problem
REVIEWER-SURFACED (2026-07-19, cp2-12 review), USER-RULED same day: the clone renders the playfield as the HORIZONTAL MIRROR of the real upright cabinet. Derivation (cp2-12 Reviewer Assessment, byte-verified): ROM playfield addr decodes h=(addr-0x400)>>5, v=addr&0x1F with CK* mirror vars zeroed upright; MAME centiped is ROT270 over TILEMAP_SCAN_ROWS with no tilemap flip (centiped_v.cpp:93/:414), so the 90-degree-CCW rotation puts ROM h=0 at display LEFT (P1 score left, high score center — the classic layout; the same transform independently confirms the clone's known-correct vertical axis). The clone's cellScreenX(h)=LOGICAL_W-(h+1)*TILE_W (layout.ts:49, from cp1-6) puts h=0 at display RIGHT. Invisible until cp2-12's asymmetric HUD because the mushroom field is statistically symmetric and the input sign chain was tuned device-right-to-screen-right. USER RULING on cp2-12: mirror accepted there (HUD consistent with field); THIS story does the global flip — cellScreenX direction, the input sign chain (device-right-to-screen-right must still hold AFTER the flip), gunScreenX, and every h-anchored render site flip TOGETHER, so the cp2-12 HUD lands on the cabinet's true sides with no HUD special-casing. Re-pin sign-chain.test.ts, the cp2-7 hitbox pins, and the cp2-12 order-agnostic HUD tests (reviewer LOW: multiset pins cannot catch a mirror — add absolute left/right pins). SHOULD LAND BEFORE cp3's asymmetric entrants (flea drop columns, spider side-entry, scorpion crossing) make the mirror visible in live play.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- cellScreenX maps ROM h=0 to screen LEFT, cited to the cp2-12 derivation (MAME ROT270 + TILEMAP_SCAN_ROWS, no tilemap flip; CK* zeroed upright); the HUD renders score h0-5 LEFT, lives h6-11, high score h12-17 center with NO HUD special-casing; screenshot from THIS checkout committed.
- Input feel preserved: device-right still moves the gun screen-right after the flip; sign-chain and cp2-7 hitbox tests re-pinned, none weakened.
- Orientation becomes testable: at least one render test pins ABSOLUTE left/right field placement (not order-agnostic multisets); suite + citations + build green.

---
_Generated by `pf context create story cp2-14` from the sprint YAML._
