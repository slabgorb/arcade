# Story df7-3 Context

## Title
Self-playing attract demo: a deterministic seeded auto-player drives the ship (thrust/fire/reverse) during phase attract so the field plays itself; ANY player input -> setup (start a real game). Pure attract driver in plugins/defender/src/core (seeded RNG, no clock — the mc6-4 analog). Cite the ROM attract display HALDIS defender/AMODE1.SRC:375 (reached HALL13 :230). Consumes df7-1 phase machine + df3 sim.ts (drives the real sim, no separate demo path).

## Metadata
- **Story ID:** df7-3
- **Type:** story
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender phase machine + attract + wiring + HUD + showcase (df7, phase 6-7): turn df1-df6's pure cores into a played-start-to-finish cabinet — the attract->setup->play->death->game-over phase machine (pure-first, pm4/mc6 model), the self-playing attract demo, the hall-of-fame name-entry flow, the HUD + scanner render, 2P alternating handoff, lobby showcase, and the full-lifecycle visual playtest

## Problem
The mc6-4 / pm4 self-play analog. During attract the phase machine runs the REAL df3 sim driven by a pure seeded auto-player (the joust jt13 demoInput lesson: drive the real sim, do not fork a demo path) so the attract screen shows actual gameplay. Any input exits attract to setup. Pure, seeded, clock-free. Colour by df2 palette index.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- AC1: a PURE, seeded, clock-free attract driver in src/core produces ship inputs (thrust/reverse/fire/smart-bomb) that drive the REAL df3 sim during phase attract (not a forked demo path — the jt13 lesson); purity.test.ts stays green and the same seed replays the same attract bit-for-bit.
- AC2: ANY player input during attract transitions attract->setup (df7-1); a test pins that the first real input exits the demo.
- AC3: the attract display is cited to the ROM (HALDIS defender/AMODE1.SRC:375, HALL13 :230) with claims/*.json under the df1-1 gate; no un-cited constant; colour by df2 palette index only.
- AC4: the attract demo obeys ADR-0005 (Decision B) — no full-frame strobe; the df4-2 guard stays green with the demo (including its smart-bomb visuals) live.

---
_Generated by `pf context create story df7-3` from the sprint YAML._
