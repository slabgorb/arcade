# Story ml10-1 Context

## Title
Wire checkOverlap — the millipede segment-vs-segment OVRLAP turn — into stepMillipede

## Metadata
- **Story ID:** ml10-1
- **Type:** bug
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Millipede — unwired-code findings (wire the built-but-dead seams)

## Problem
checkOverlap (core/millipede.ts:405) is fully implemented, ROM-cited (OVRLAP, MLSUB.MAC:896-912) and unit-tested (millipede-split-death.test.ts), but has ZERO production callers — the head only ever reacts to the OBSTAC (mushroom) turn. The confession at core/millipede.ts:300 spells out the gap: the ROM's no-turn branch (13$, MILLI.MAC:1541) runs JSR OVRLAP FIRST — the segment-vs-segment overlap turn — before the free-space/edge handling, so a head that overlaps a live segment ahead of it should turn (drop a row) just as it does on a mushroom. Wire checkOverlap into the head reaction path in stepMillipede at the ROM's ordering: OBSTAC (mushroom) turn → OVRLAP (overlap) turn → edge/free-space. Reuse checkOverlap AS-IS; its dead-floor (colour ≥ OVRLAP_DEAD_MIN 0xC0) and parked-score (0xFF) guards already exist. Test through the real stepMillipede — the existing data test on checkOverlap passes today while the turn is dead, so it proves nothing about wiring. Seam: core/millipede.ts (the head reaction sequence).

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- stepMillipede consults checkOverlap in the head reaction sequence at the ROM ordering (mushroom/OBSTAC turn, then OVRLAP overlap turn, then edge/free-space): a marching head that overlaps a live in-front segment turns and drops a row.
- The overlap turn is pinned to the ROM (the 13$/MILLI.MAC:1541 JSR OVRLAP ordering + MLSUB.MAC:896-912) with citations, and a parked score (0xFF) or dead segment (colour ≥ 0xC0) does NOT trigger the turn — checkOverlap's existing semantics are preserved.
- A test drives the behaviour through stepMillipede (the real producer), not a direct checkOverlap call, and fails if the call is removed; no regression to the mushroom-turn, split-on-turn, edge-turn, or player-collision paths.

---
_Generated by `pf context create story ml10-1` from the sprint YAML._
