# Story ml4-4 Context

## Title
DDT bombs: DDTS add DDT bombs (MLSUB.MAC:420), BOMBS bomb player with bee/mosquito/dragonfly (MILLI.MAC:446), explosion clouds (CLOUD MLDEF.MAC:202, DDT stamps :203). NDDT=4 (MLDEF.MAC:191). Cited.

## Metadata
- **Story ID:** ml4-4
- **Type:** story
- **Points:** 3
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Millipede — the menagerie + DDT (phase 4b): one cited reducer per critter, and the DDT bomb

## Problem
Millipede's DDT bombs have no core owner yet. The story title cites the three ROM
routines to port: DDTS (add DDT bombs, MLSUB.MAC:420), BOMBS (bomb interaction with
player/bee/mosquito/dragonfly, MILLI.MAC:446), and the explosion-cloud stamps
(CLOUD MLDEF.MAC:202, DDT stamps :203), with NDDT=4 (MLDEF.MAC:191).

**Title-only story:** per project convention, verify each cited line against the
vendored tree BEFORE writing tests — the title is the spec and it rots like one.
Quarry: in-repo `reference/original-source/millipede` (CRLF line endings — if grep
comes back empty, use awk; see millipede quarry memory).

## Inherited findings this story MUST absorb (routed promises)

- **From ml3-5 (blocking for this story's scope):** the DDT-bomb halves of
  SCROLD/SCROLU (`MLSUB.MAC:1231-1295` and `:1379-1399` — DDTADD pointer moves,
  off-screen clears, one-per-scroll top-row bomb seeding; claims SC-49/SC-50 in
  `docs/rom-study/claims/12-scroll.json`) were descoped from ml3-5 and routed to
  ml4-4: scroll-time DDT-table maintenance lands HERE. See the TEA Gap finding in
  `sprint/archive/ml3-5-session.md` (Delivery Findings).
- **Seam inversion carry-over (ml4-1/2/3 precedent):** DDTEXP/DDTEX1, OBSTAC,
  PLAY player collision, MUSHER and sound-channel writes stay caller-side; the
  reducers expose pure decisions. Wiring order lands with ml3/ml7.

## Technical Approach (pointers, for TEA/Dev to refine)

- House style: pure cited reducers in the `conway.ts` style — in-place mutation,
  byte semantics 0..255, upright cabinet only (cocktail is fleet-wide out of scope,
  filed ml8-3). New modules land in `plugins/millipede/src/core/` and enter the
  ml1-1 purity sweep automatically.
- Claims JSON: GENERATE from the vendored lines (scratchpad script), never
  hand-transcribe — the ml4-2/ml4-3 pattern; the ml1-1 citation gate sweeps it.
- Prior-story cribs: `sprint/archive/ml4-3-session.md` (contract shape, RED/GREEN
  structure), `sprint/archive/ml3-5-session.md` (scroll seams, boundary-fixture
  convention: pin thresholds AT the boundary or a `>=`→`>` mutant survives).
- Existing neighbours: `scroll.ts` (SCROLC/field shape this story's table
  maintenance hooks into), `bee.ts`/`dragonfly.ts`/`mosquito.ts` (the fliers BOMBS
  interacts with; note the bee's two-hit rule holds even inside a DDT cloud).

## Scope
- In scope: DDTS, BOMBS, CLOUD/DDT explosion stamps, NDDT=4, and the ml3-5-routed
  scroll-time DDT-table maintenance (SC-49/SC-50).
- Out of scope: caller-side wiring (ml7), OBSTAC/SCROL0 (ml3-3), cocktail (ml8-3),
  extraction of shared helpers (ml4-6), spider feature (ml4-5).

## Acceptance Criteria
_TEA defines during RED. Must cover: the four title citations verified against the
vendored tree; the SC-49/SC-50 scroll-maintenance halves; byte-anchored claims JSON
generated not transcribed; boundary fixtures pinned AT thresholds._

---
_Generated by `pf context create story ml4-4` from the sprint YAML._
