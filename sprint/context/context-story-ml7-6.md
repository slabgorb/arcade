# Story ml7-6 Context

## Title
Decode the real ship + enemy/mushroom sprites

> ⚠ **SM CORRECTION** — The story title contains partially stale language. Current facts (measured 2026-08-14):
>
> 1. `plugins/millipede/src/shell/gfx-rom.ts` EXISTS — the gfx ROM decoder is real.
> 2. The ROT90 tile/sprite asymmetry (pac-man precedent) is ALREADY HANDLED in `render.ts` — ml7-3 shipped "char-tile map + vertical-monitor rotation" with a 90° CCW turn (render.ts:63-95, 123-128). Do NOT re-derive the rotation trap; sprite work must FOLLOW that same CCW convention.
> 3. **STALE:** Enemies and millipede segments are NO LONGER "red-triangle pic guesses." They are already drawn as DECODED sprite tiles via `drawSprite(h,v,pic)` → `drawStampAtPx(2*pic, ...)` in `plugins/millipede/src/main.ts:94-111`. The open question is whether each entity's `pic` → tile mapping is CORRECT (visual-playtest fidelity), not that they are "undecoded."
> 4. **LIVE / CORE DELIVERABLE:** The PLAYER SHIP is STILL a placeholder blue square — `main.ts:114-118` draws `c.fillStyle='#4cf'; c.fillRect(pxx+2, pyy+2, 4, 4)` with comment "Player ship (exact sprite decode is a follow-up)." This is the clearest undone item and the story's primary deliverable: decode & render the real player ship sprite from the gfx ROM, replacing the blue square.
> 5. Spider-death "parked score in lieu of its sprite": the parked-score hold lives in core (`plugins/millipede/src/core/millipede.ts:512-514`). Treat this as a to-verify item at visual playtest, not an established defect.

## Overview
Decode sprite bitmaps from the gfx ROM and wire them into the shell render pipeline. The player ship is the primary item (currently a placeholder blue square); enemy / millipede-segment / mushroom sprite `pic` → tile mappings are secondary (already drawn, but correctness must be validated by visual playtest). Respect the ml7-3 CCW rotation convention and maintain the core/shell boundary.

## Acceptance Criteria
- **AC1:** The player ship renders as its real decoded gfx-ROM sprite (not the `#4cf` blue-square placeholder), following the ml7-3 CCW rotation convention.
- **AC2:** Enemy / millipede-segment / mushroom sprite `pic`→tile mappings are validated to decode to the correct shapes (visual playtest), and any mismatch is corrected.
- **AC3:** A visual playtest confirms the frame is correct to the eye. Routing tests pass while a flipped/mis-scaled sprite ships; pin output coordinates in a core unit test if needed. Do NOT introduce full-screen flashes (accessibility constraint from the epic — freeze/fade only).
- **AC4:** core/shell boundary preserved — decode/render work stays in `src/shell/`; the purity/core-boundary test (`plugins/millipede/src/core/<boundary-test>`) must stay green.

## Metadata
- **Story ID:** ml7-6
- **Epic ID:** ml7
- **Repo:** arcade
- **Points:** 5
- **Workflow:** tdd (RED → GREEN → review → finish)

## Background

### Preconditions (ml7-3 and prior stories)
- ml7-3 shipped HUD + rotation infrastructure (`render.ts:63-95, 123-128`) with a 90° CCW turn for the vertical monitor.
- ml6-2 wired the core→shell event dispatch and frame loop (`main.ts` frame loop, `drawSprite` call site).
- ml5 established the game structure and sprite indices (`pic` field in entities).
- The gfx ROM has been available and decoded in `gfx-rom.ts`.

### Known Trap
**Routing != geometry:** vitest routing tests can pass while a flipped, mis-scaled, or mis-positioned sprite ships. Reference: `render.ts:7` comment and PAC-MAN's `pac-man-rot90-tile-sprite-asymmetry.md` memory note. A coordinate unit test is the backstop.

### Accessibility Gate
The epic holds a standing exception to ROM-always-wins: **NO full-screen strobe/flash**. This was baked into the phase machine in ml7-4 (delivered via ml6-2). Sprite renders must not introduce flashing; freeze or fade only (e.g., if a sprite-render path affects death/wave transitions).

### Spider-Death Parked Score
The spider-death score hold is logged in core (`millipede.ts:512-514`). The comment says "parked score in lieu of its sprite because sprites are undecoded." With this story, sprites are decoded — verify at playtest whether the parked score is still needed or can be retired.

---
_Generated by `pf context create story ml7-6` from the sprint YAML and curated with SM corrections._
