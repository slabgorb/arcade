# Story mc1-3 Context

## Title
Trackball/mouse crosshair cursor with clamp

## Metadata
- **Story ID:** mc1-3
- **Type:** story
- **Points:** 2
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Missile Command — plugin skeleton: boot the cabinet on REV-01 ground truth

## Problem
Trackball/mouse-driven crosshair cursor that integrates input delta and clamps to the play area. Core: cursor.ts integrates a per-frame delta and clamps (W3MAIN:424 PROCESS CURSOR MOTION, :546 ADD TBALL TO CURSOR, :587 UPDATE CURSOR). Shell: input.ts maps mouse/trackball motion to the delta. Desktop-only (repo rule: no touch/narrow-viewport).

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- cursor.ts (core, pure) is a reducer: (cursor, delta) -> clamped cursor, bounded to the field. Deterministic and unit-tested; no shell imports.
- input.ts feeds mouse/trackball motion as the delta; the crosshair moves on screen at /missile-command/ and cannot leave the play area.

## Background — Measured Facts

**Artifact Location & Scope**

The missile-command plugin already exists with `plugins/missile-command/src/core/{field.ts,game.ts}` and `src/shell/render.ts`. This story ADDS two new files: `core/cursor.ts` (pure reducer) and `shell/input.ts` (delta source). No cursor.ts/input.ts exists yet.

**Cabinet Source & Citations**

The cabinet source is at `plugins/missile-command/reference/source/W3MAIN.MAC`. The story/design cites:
- W3MAIN:424 (PROCESS CURSOR MOTION)
- W3MAIN:546 (ADD TBALL TO CURSOR)
- W3MAIN:587 (UPDATE CURSOR)

**CRITICAL CITATION-CONVENTION NOTE** (carry this forward, it will save the next agent a wrong-line trap): **W3MAIN.MAC is DOUBLE-SPACED** (a blank line after every source line). The rom-study/design citation numbers are LOGICAL (non-blank) line numbers, roughly `physical_line / 2`. The three cited routines are at these PHYSICAL line numbers:

- `:424` "PROCESS CURSOR MOTION" → physical line 847 (`.SBTTL PROCESS CURSOR MOTION`)
- `:546` "ADD TBALL TO CURSOR" → physical line 1091 (`.SBTTL ADD TBALL TO CURSOR POSITION`)
- `:587` "UPDATE CURSOR" → physical line 1173 (`.SBTTL UPDATE CURSOR POSITION`)

Also relevant labels: `UPCURS` (JSR at physical 555/679), `UPDCUR` (JSR physical 879), `ADCURS` (JSR physical 1183). **Opening the raw physical line 424/546/587 lands in the WRONG routine** — always map cite → physical via ×2 or grep the `.SBTTL` label.

**Desktop-Only & Input Binding**

Repo rule: missile-command is DESKTOP-ONLY. No touch/narrow-viewport acceptance criteria. `input.ts` maps mouse/trackball motion (pointer delta) to the cursor delta; fire keys are Z/X/C per `docs/design/mc1-skeleton.md`.

**Design Reference & Boundary Enforcement**

Design reference: `plugins/missile-command/docs/design/mc1-skeleton.md` and `docs/rom-study/brief.md` define cursor.ts's role. The plugin enforces the core/shell boundary with a purity test (`plugins/missile-command/tests/purity.test.ts`) — cursor.ts must live in `src/core/` and import nothing from shell.

---
_Generated by `pf context create story mc1-3` from the sprint YAML. Background measured facts added by sm-setup._
