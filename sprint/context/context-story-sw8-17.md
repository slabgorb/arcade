# Story sw8-17 Context

## Title
The Death Star's off-centre wander belongs to the STATION, not the camera — port its own lateral motion into deathStarPlacement. The epic's §1 evidence anchor is a real cabinet observation (longplay ~wave 4, score 352,171: mid space-combat the Death Star is entirely out of frame) that our port cannot reproduce: deathStarPlacement seats the station at x=0 and moves it only in DEPTH. sw8-1 explained it with a viewer slide off ST.UX; sw8-8 retired that camera (ST.UX is the starfield's register — sole reader VWSTAR WSSTAR.MAC:98-102, and no space code writes the player's world position at all), so the observation is now unexplained. Find the station's OWN ROM mechanism (WSBASE.MAC / the DS site handling in WSSITE.MAC) and port it as a lateral term in deathStarPlacement. MUST NOT be re-derived from ST.UX or reintroduced as a camera slide: tests/shell/render.space-camera.test.ts pins camera-contributes-no-lateral-offset and will fail loudly on the shortcut

## Metadata
- **Story ID:** sw8-17
- **Type:** bug
- **Points:** 3
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Cabinet feel: the flight and combat loop — render/experiential fidelity vs the cabinet longplay

## Problem
The Death Star placement currently remains at x=0 and only varies in depth, but the real cabinet shows the station moving laterally (off-centre) during space combat. The evidence comes from a real cabinet longplay (~wave 4, score 352,171) where the Death Star is entirely out of frame mid-combat — a situation our port cannot reproduce because the camera mechanism that was explored in sw8-1 (ST.UX viewer drift) has been retired as of sw8-8. The observation now lacks explanation.

## Technical Approach
This is a ROM reverse-engineering story:

1. **Audit the ROM mechanism:** Examine WSBASE.MAC and the Death Star site handling in WSSITE.MAC to identify the station's own lateral motion logic (not a camera/viewer term).
2. **Identify the lateral term:** Determine what ROM register or computation drives the Death Star's x-position offset during space combat.
3. **Port to deathStarPlacement:** Implement the lateral motion term in the `deathStarPlacement` function (plugins/star-wars/src/core/). The term must account for the station's own movement, independent of viewer/camera position.
4. **Guard against shortcuts:** The test `tests/shell/render.space-camera.test.ts:camera-contributes-no-lateral-offset` must remain passing. Do NOT re-derive the motion from ST.UX or reintroduce a camera slide — the observation must be explained by the station's own ROM-ported mechanism.

## Scope
- **In scope:** 
  - Reverse-engineering the Death Star's ROM lateral-motion logic (WSBASE.MAC, WSSITE.MAC).
  - Porting that mechanism into `deathStarPlacement` as a lateral term.
  - Ensuring the longplay's out-of-frame observation is reproducible.
  - Verifying camera-test passes and no camera-slide shortcut is used.

- **Out of scope:** 
  - Camera/viewer position changes (camera is post-sw8-8 retired for this purpose).
  - Other Death Star properties (rotation, scale, depth).
  - Unrelated rendering or simulation changes.

## Acceptance Criteria
TEA to define during RED phase. Expected behaviors:

1. The Death Star's x-position is driven by a ROM-ported lateral mechanism (WSBASE.MAC or WSSITE.MAC), not camera drift.
2. During space combat (especially wave progression), the station moves off-centre, matching the cabinet longplay observation (~wave 4, score 352,171).
3. `tests/shell/render.space-camera.test.ts` (especially `camera-contributes-no-lateral-offset`) passes without modification.
4. The lateral term is tested and verified to be non-vacuous.

---
_Generated by `pf context create story sw8-17` from the sprint YAML._
