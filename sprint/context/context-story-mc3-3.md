# Story mc3-3 Context

## Title
Scoring (25/ICBM wave-1) and the play->game-over phase

## Metadata
- **Story ID:** mc3-3
- **Type:** story
- **Points:** 3
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Missile Command — core combat loop (the playable milestone)

## Problem
Plan tasks 5-6 (docs/superpowers/plans/2026-08-07-missile-command-mc3-core-loop.md). (a) src/core/score.ts + claims/score.json — ICBM_KILL_POINTS=25 and scoreKills(score,killed)=score+killed*25. The ROM scores 25 x wave number per downed ICBM (ADC I,25 under 'INCREASE POINTS FOR DOWNING ICBMS', W3MAIN ICBPTL); mc3 pins the wave-1 value 25, the x-wave ramp is mc4. New claim MC-ICBPTS(25), byte-exact verbatim (grep 'ADC I,25' for the exact physical line). (b) src/core/state.ts — Phase='play'|'over', allCitiesDead(cities), nextPhase(phase,cities) flips to 'over' once every city is dead ('over' is terminal). The full attract/setup/pause machine is mc6. Depends on mc3-1 (City).

## Technical Approach

> ⚠ **SM VERIFIED-FACTS (measured against the current tree 2026-08-07 — the Problem/ACs
> above are copied verbatim from the epic YAML and use SHORTHAND; these are the true
> facts. Do not edit the ACs; read these first.):**
>
> 1. **Claims path is `docs/rom-study/claims/score.json`, NOT a top-level `claims/`.**
>    The AC/Problem text says "claims/score.json" — that is shorthand. missile-command's
>    claims live at `plugins/missile-command/docs/rom-study/claims/` (existing siblings:
>    config.json, cursor.json, field.json, explosion.json, anchors.json, prose.json,
>    external.json). Create a NEW `docs/rom-study/claims/score.json` for MC-ICBPTS(25).
>    Do NOT create a top-level `claims/` dir (the dup-id trap) and do NOT move claims
>    between existing files — the AC3 guard is value-based and file-agnostic.
> 2. **Citation `ADC I,25` is at `plugins/missile-command/reference/source/W3MAIN.MAC`
>    PHYSICAL line 4091.** Surrounding context — 4083 `;ICBM PTS X WAVE NUMBER` (block
>    header), 4087 `LDA ICBPTL ;INCREASE POINTS FOR DOWNING ICBMS`, 4089 `CLC`,
>    4091 `ADC I,25`, 4093 `STA ICBPTL`. W3MAIN.MAC is **double-spaced** (a blank line
>    between every source line), so physical 4091 ≈ logical 2045 — the story asks for the
>    **physical** line, so cite 4091. The x-wave multiply implied by the block header is
>    deferred to **mc4**; mc3 pins ONLY the wave-1 constant `25`.
> 3. **Dependency mc3-1 (stateful cities) is DONE.** `allCitiesDead`/`nextPhase` build on
>    the city model it shipped — see `plugins/missile-command/src/core/` (damage.ts,
>    field.ts, game.ts, icbm.ts, etc.). Neither `score.ts` nor `state.ts` exists yet;
>    both are new files, no collision.

Both modules live in `plugins/missile-command/src/core/` and must be **pure** (no clock,
no DOM, no I/O) — the `purity.test.ts` guard scans core source text. `citations.test.ts`
must stay green: the new claim's `verbatim`/`source.line` pair has to match W3MAIN.MAC
byte-for-byte at the physical line.

- (a) `score.ts`: `ICBM_KILL_POINTS = 25`; `scoreKills(score, killed) => score + killed * 25`.
- (b) `state.ts`: `Phase = 'play' | 'over'`; `allCitiesDead(cities)`; `nextPhase(phase, cities)`
  stays `'play'` while any city lives, flips to `'over'` when all cities dead, `'over'` terminal.

## Scope
- In scope: score.ts (+ docs/rom-study/claims/score.json) and state.ts, per the ACs.
- Out of scope: the x-wave scoring ramp (mc4); the full attract/setup/pause machine (mc6).

## Acceptance Criteria
- score.ts exports ICBM_KILL_POINTS=25 and scoreKills(score,killed) adding 25 per kill onto the running score; claims/score.json carries MC-ICBPTS(25) with byte-exact verbatim; the literal 25 passes the AC3 guard.
- state.ts exports Phase 'play'|'over', allCitiesDead(cities), and nextPhase(phase,cities): stays 'play' while any city lives, flips to 'over' when all cities are dead, and 'over' is terminal.
- both modules are pure; score.test.ts and state.test.ts pass; purity.test.ts and citations.test.ts stay green.

---
_Generated by `pf context create story mc3-3` from the sprint YAML._
