# Story ml6-2 Context

## Title
Wire sfx to core events + gate audio on first user gesture (autoplay policy): map millipede/critter/DDT/bonus-life (CHAN11 bonus-life sound MLDEF.MAC:323) events to the driver; verify audio actually plays in a real browser (silent-feature trap), not just a green vitest.

## Metadata
- **Story ID:** ml6-2
- **Type:** story
- **Points:** 5
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Millipede — sound (phase 5): the two-POKEY, eight-channel driver and the SOUNDS routine

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria

**AC1:** All sound event families (millipede segment, critter, DDT, bonus-life/CHAN11) are wired to core events and routed to the synthesizer driver (ml6-1).

**AC2:** Audio playback is gated on first user gesture, per browser autoplay policy (no unmuted audio before interaction).

**AC3:** Audio output is verified as audible in a real browser (via Playwright MCP headless testing). Static vitest render alone is insufficient; the sweep IS the sound (silent-feature trap).

**AC4:** Any asset-based sounds (if used) pass live 200 fetch verification (not just CI green; silent-degrade trap).

---
_Generated by `pf context create story ml6-2` from the sprint YAML._
