# Story mc11-3 Context

## Title
Wire the name-entry timeout + start-abort: add a per-frame countdown to the 'entry' step branch that calls abortNameEntry (core/game.ts:274) on 30-second expiry, and map the start switch to abort during entry, returning to attract with the buffer cleared and the ladder unchanged. Assert both the timeout and start-abort paths. REV-01 W3DSUP.MAC:4076/:4086 TAKE INITIALS abort

## Metadata
- **Story ID:** mc11-3
- **Type:** story
- **Points:** 5
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Missile Command — wire the post-mc5 unwired seams: sputnik cruise-borrow, bonus count-up cue, name-entry timeout; retire dead twins

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria

Derived from the title/description:

**AC1 (timeout path):** A per-frame countdown is added to the `'entry'` step branch (currently at `plugins/missile-command/src/core/game.ts:368`, which today only does `frame + 1`). On expiry, it calls `abortNameEntry`.

**AC2 (start-abort path):** The start switch, pressed during `'entry'`, calls `abortNameEntry`.

**AC3 (post-abort state):** Both paths return to `phase: 'attract'` with the initials buffer cleared and the high-score ladder UNCHANGED (no insert — a timeout on the last letter discards it). `abortNameEntry` (game.ts:274) already implements this pure result; this story WIRES it.

**AC4 (tests):** Assert BOTH the timeout path and the start-abort path in the test suite.

### Fidelity Note
**TEA must confirm** the exact countdown duration/frame count from REV-01 W3DSUP.MAC:4076 (start-switch abort) and :4086-:4088 (timeout). The code comment at game.ts:267-270 already cites these lines. **Do NOT hard-code 30s into an AC as gospel — cite the ROM.**

## Background

**Verified Premises:**

- `abortNameEntry` is defined at game.ts:274 and is called NOWHERE in missile-command/src (genuinely unwired).
- The `'entry'` phase machinery (enterNameEntry, keydown, commit, abort) all exists and is pure/tested.
- **This is a WIRING story in the shell/step seam**, not new sim logic.
- This is the deferred **O-7b** input-mapping item named in the abortNameEntry comment.
- The ROM ground truth is **REV-01 W3DSUP.MAC** (TAKE-INITIALS timeout and start-switch abort).

---
_Generated by `pf context create story mc11-3` from the sprint YAML._
