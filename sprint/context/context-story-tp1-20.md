# Story tp1-20 Context

## Title
HUD & MESSAGES — fixed field colours and the ROM's actual strings; drop the three invented captions

## Metadata
- **Story ID:** tp1-20
- **Type:** bug
- **Points:** 5
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** tempest
- **Epic:** Tempest — ROM fidelity against Theurer's original 1981 source

## Problem
Cluster C14. Subsumes V-018, V-033, V-034, V-035, V-036. Depends on tp1-12 for the palette.

## Critical Notes for TEA

### Unverified Subsumes List
The "Subsumes V-018, V-033, V-034, V-035, V-036" label is an **unverified guess** from the epic. Before RED, TEA MUST:
1. Pull each finding's actual claim and reasoning from `tempest/docs/audit/findings/*.json`
2. Check if the reasoning has `[REFUTATION]/[CORRECTION]` which may flip the claim
3. Check if any findings already carry `remediated_by` (already fixed by prior stories)
4. Carve any misfiled findings out to their real story — do not assume the subsumes list is accurate

### Prior Story Context (tp1-12 Palette)
Story tp1-12 (THE PALETTE) ships the COLTAB banks and the palette mechanism. The per-level HUD/frame/warp accent currently derives from `wellColor(s.level)` with a white fallback on invisible-well waves 65-80 (tempest/src/shell/render.ts around lines 939-940 at time of tp1-12 archive). tp1-12's Reviewer noted a non-blocking nit: the authentic HUD/LETTERS colour is palette slot 5/7, not white. **tp1-20's fixed per-field colours (green score, yellow lives, green hi-score, blue level) supersede that accent-driven HUD colouring.** COLTAB_BANKS, paletteBank, paletteColor, wellColor already exist and are cited.

### Citation Gate Traps
tempest/tests/audit/citations.test.ts pins file+line to exact locations. **ANY edit in a cited file — even a comment-only change — shifts pinned lines and breaks the test.** After editing any cited file, re-anchor with:
```bash
node tools/audit/reanchor-citations.mjs --write
```

`remediated_by` is ONLY for defects actually removed by this story. Do not use it on unrelated changes or as a workaround to silence the gate.

### Purity Scanner Reads Comments
tempest/tests/audit/purity.test.ts scans pure-core boundary source text **including comments** for forbidden globals. Avoid the words `window.`, `document.`, `canvas`, `FontFace` even in comments — a sentence like "the window dimension" will fail the scanner.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- HUD field colours are FIXED per field, per the ROM: green score, yellow lives, green hi-score, blue level.
- The three invented captions are removed.
- The ROM's actual strings ship, verbatim: 'ENTER YOUR INITIALS', 'RANKING FROM 1 TO n', 'SPIN KNOB TO CHANGE'.
- Depends on tp1-12 (palette).
- npm test -- citations stays green.

---
_Generated by `pf context create story tp1-20` from the sprint YAML._
