# Story df2-5 Context

## Title
Terrain + mini-terrain (BLK71) transcription + static planet surface. BLK71 "TERRAIN, MINI-TERRAIN DATA AND PLAYER EXPLOSION", banked block 7 (defender/BLK71.SRC:1-8; TLEN EQU $100, defender/BLK71.SRC:18). Transcribe the terrain vectors under the gate; render a STATIC planet surface across the framebuffer bottom (the roadmap "static planet + text early"). No scroll — the scrolling world is df3.

## Metadata
- **Story ID:** df2-5
- **Type:** story
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender — framebuffer render + image-table transcription (phase 2): static pixels on screen before any physics

## Background

**ROM structure (vendored at `reference/original-source/defender/`; SM-verified against the current tree — cite these, do not re-derive):**

`BLK71.SRC` (723 lines) is titled "TERRAIN , MINI-TERRAIN DATA AND PLAYER EXPLOSION" (`defender/BLK71.SRC:1-8`) and is **mostly terrain-generation CODE** — `BGINIT` (`:95`), `BGOUT` (`:152`), `BGALT` (`:372`), `BGERAS` (`:402`), plus the left/right terrain functions (`:411-506`). That code consumes the terrain data **bit-serially** (`LTBYTE`/`RTBYTE` "TERRAIN DATA BYTE", `LTCNT`/`RTCNT` "TERRAIN BIT COUNTER", `:40-43`). The `RMB` blocks (`ALTTBL`, `TERTF0/1`, `:70-72`) are RAM reservations, not data.

The **only transcribable DATA in this block is two labels** — everything else is code or RAM:

- **`TDATA`** — "TERRAIN DATA TABLE" (header comment `defender/BLK71.SRC:507`; `FCB` data rows `:509-524`). `FCB` byte stream, exactly **`TLEN` = `$100` = 256 bytes** (16 bytes × 16 lines; `TLEN EQU $100` at `:18`). This is a **packed bit-stream** (bit-per-column terrain profile), read bit-serially by the `BG*` routines and referenced as `#TDATA+TLEN` at `:423,:445,:470,:496`. It is **not a raster** — same streams-are-not-rasters discriminant as df2-4/SAMEXAP7.
- **`MTERR`** — "MINI TERRAIN" (`defender/BLK71.SRC:527+`). `FCB` byte stream, referenced by the block's jump table at `:87` (`FDB MTERR  MINI TERRAIN DATA`). Appears to be periodic `(altitude, x, x)` triples (the scanner/mini-map terrain silhouette).

**Out of scope (do NOT transcribe):** BLK71 also carries **PLAYER EXPLOSION** data/equates (`PXPOST/PYPOST/PXVELT/PYVELT`, `:21-24`) and the terrain-generation routines themselves. This story is **terrain + mini-terrain DATA only** (`TDATA` + `MTERR`). The generation/scroll engine is df3.

**Design pattern (from df2-3/df2-4 precedent — copy the shape, do NOT invent):**
- `scripts/transcribe-<module>.mjs` — transcribe/codegen tool (model on `scripts/transcribe-charset.mjs`; note: `scripts/transcribe-pictures.mjs` does **not** exist despite epic prose)
- `plugins/defender/src/core/<module>-data.ts` — GENERATED data module (model on `objects-data.ts`)
- `plugins/defender/src/core/<module>.ts` — pure reader/decoder (model on `objects.ts`)
- `plugins/defender/tests/<module>-gate.test.ts` — INDEPENDENT gate: re-derives bytes from source, records an `encoding` discriminant per block, refuses to raster a non-raster stream
- static render/blit proof (model on `objects-blit.test.ts`)

**Citation gate (df1-1):** every new `src/core` constant needs audit coverage — follow the exact enrollment the df2-3/df2-4 constants used (claims live under `plugins/defender/docs/rom-study/claims/`; confirm the enrollment path against how `charset`/`objects` did it, per the "df1-1 claims/*.json isn't the real gate" caution).

**Purity boundary (the single most important rule):** terrain DATA transcription + the pure decoder live in `plugins/defender/src/core/` (must stay clock-free — keep `purity`/core-boundary test green). The **static planet-surface RENDER** is `shell`. `TDATA` is the world terrain; a STATIC planet surface = decode a fixed slice and paint the mountain silhouette across the framebuffer bottom. **No scroll** — the scrolling world is df3.

**Colour rule (settled fleet-wide):** any ground/surface fill uses the palette index (`indexToRgba`/`colours[0]`), never a hex literal — see the "colours never invented" lesson; df2-1 regressed it once.

## Technical Approach
_TEA/Dev to refine. The Background above defines the sourced facts and the precedent artifacts to model on; follow the df2-3/df2-4 transcription pattern._

## Scope
- **In scope:** transcribe `TDATA` + `MTERR` from BLK71 under the citation/encoding gate (INERT data); render a STATIC planet surface across the framebuffer bottom (shell).
- **Out of scope:** PLAYER EXPLOSION data, terrain-generation routines, ANY scrolling (df3), animation, collision, physics.

## Acceptance Criteria
_No acceptance criteria in the sprint YAML (epic df2 convention). TEA to derive and pin them during the RED phase, faithful to the Background above — no scope beyond terrain/mini-terrain transcription + static planet render._

---
_Generated by `pf context create story df2-5`, then enriched by SM (Thrawn) with SM-verified BLK71 facts and df2-3/df2-4 precedent pointers._
