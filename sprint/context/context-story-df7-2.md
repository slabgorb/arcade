# Story df7-2 Context

> ⚠ **DO NOT REGENERATE THIS FILE.** The Technical Approach, Scope, Dependencies and
> Design Notes are Architect-enriched from the df7 epic plan. `pf context create`
> refills Technical Approach/Scope with placeholder text and would overwrite them.

## Title
Setup->play start-of-game wiring: a start/coin action advances attract|game-over -> setup -> play, RESEEDING the sim (createSim with the df5-10 humanoid ground population, the df5-8 wave-1 WVTAB spawn, men=STARTING_MEN=3 per df5-3). Wire the df7-1 phase machine into plugins/defender/src/main.ts so the shell actually starts a game. Cite ST1 *ONE PLAYER START defender/DEFA7.SRC:1100 (credit check :1103). Consumes df7-1 + df3 sim.ts + df5-8/df5-10.

## Metadata
- **Story ID:** df7-2
- **Type:** story
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender phase machine + attract + wiring + HUD + showcase (df7, phase 6-7): turn df1-df6's pure cores into a played-start-to-finish cabinet — the attract->setup->play->death->game-over phase machine (pure-first, pm4/mc6 model), the self-playing attract demo, the hall-of-fame name-entry flow, the HUD + scanner render, 2P alternating handoff, lobby showcase, and the full-lifecycle visual playtest

## Problem
The mc6-2 analog: the start-of-game transition. A coin/start input advances the phase to setup then play and reseeds a fresh game — humanoids on the ground (df5-10), wave 1 spawned (df5-8), men=3 (df5-3 STARTING_MEN, NSHIP ROMC8.SRC:802). This is where main.ts stops booting straight into a bare sim and instead boots into attract and starts on input. Purity of the reseed logic preserved; the wiring is shell.

## Technical Approach
The mc6-2 analog — the start-of-game transition and the `main.ts` boot rewire.

- **The transition.** A start/coin input advances `attract | game-over → setup → play`
  (via df7-1's machine) and **reseeds** a fresh game: `createSim(seed)` with the df5-10
  humanoid ground population placed, the df5-8 wave-1 `WVTAB` attackers spawned, and
  `men = STARTING_MEN` (3, df5-3, `NSHIP ROMC8.SRC:802`). Cite `ST1 *ONE PLAYER START
  DEFA7.SRC:1100` (credit check `:1103`).
- **`main.ts` boots into attract.** Today `main.ts` boots a bare running sim; rewire it to
  boot into df7-1's `attract` phase and start `play` only on the start/coin input. The
  reseed logic stays **pure** (a `createSim`-shaped function); only the input binding is
  shell.
- **Determinism.** Same seed → same fresh game (df3 seeded RNG); a test pins the
  fresh-game invariants (humanoids placed, wave 1 present, men=3) at the `setup→play` edge.
- **ADR-0005.** No full-frame strobe on the transition (Decision B); the df4-2 guard stays
  green.

## Scope
- **In scope:** the `attract|game-over → setup → play` start transition, the deterministic
  reseed (humanoids/wave-1/men=3), the `main.ts` boot-into-attract rewire, the `ST1`
  citation + `claims/*.json`, the fresh-game invariant tests.
- **Out of scope:** the attract driver that plays during attract (df7-3); HUD render
  (df7-5); the 2P `ST2` path (df7-6). This story starts a **single-player** game from
  attract.

## Acceptance Criteria
- AC1: a start/coin action advances attract|game-over -> setup -> play and reseeds a fresh game via createSim — the df5-10 humanoid ground population placed, the df5-8 wave-1 WVTAB attackers spawned, men=STARTING_MEN (3, df5-3, cited); a test pins the fresh-game invariants at the setup->play edge.
- AC2: plugins/defender/src/main.ts boots into the df7-1 attract phase (not a bare running sim) and starts play only on the start/coin input; the ST1 credit/start path is cited (*ONE PLAYER START defender/DEFA7.SRC:1100, credit :1103) with claims/*.json under the df1-1 gate.
- AC3: the reseed is deterministic under a seed (df3 seeded RNG) — the same seed yields the same fresh game; purity.test.ts stays green (the reseed logic is pure; only the input binding is shell).
- AC4: no full-frame strobe on the setup->play transition (Decision B, ADR-0005) — the df4-2 render guard stays green; any transition flash is the safe freeze/fade.

## Dependencies
- **df7-1** — the phase machine this wires into `main.ts`.
- **df3** — `sim.ts` `createSim` (the reseed) + seeded RNG.
- **df5-8** — wave-1 `WVTAB` spawn; **df5-10** — humanoid ground population; **df5-3** —
  `STARTING_MEN`.
- **Blocks:** df7-3 (attract runs before this start), df7-5 (HUD shows the started game).

## Design Notes
- The mc6-2 analog. `main.ts` stops booting a bare sim and boots into attract (Decision A's
  machine); the reseed stays pure, only the input binding is shell.
- No full-frame strobe on the transition (Decision B); citations gated; RASM radix.

---
_Generated by `pf context create story df7-2` from the sprint YAML._
