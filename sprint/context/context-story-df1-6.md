# Story df1-6 Context

## Title
Harden the claims-JSON load path in the fleet's citation checkers: a malformed docs/rom-study/claims/*.json currently throws an uncaught SyntaxError (raw stack) from loadClaims/the checker CLI in defender, millipede and centipede; wrap the parse in a controlled per-file error naming the file. Filed from df1-1 review (rule-checker R3, inherited from millipede).

## Metadata
- **Story ID:** df1-6
- **Type:** chore
- **Points:** 2
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender — ROM source study + scaffold + fidelity harness (phase 1): the ground-truth dossier every later df* story cites

## Problem
Two halves, one function, three games. (1) As titled: a malformed docs/rom-study/claims/*.json currently throws an uncaught SyntaxError (raw stack) from loadClaims/the checker CLI in defender, millipede and centipede — wrap the parse in a controlled per-file error naming the file. (2) EXTENSION routed from df1-2 review (round-1 [TYPE] finding, routing discharged at df1-3 setup 2026-08-14): loadClaims() casts JSON.parse output without runtime shape validation, so a well-formed-JSON-wrong-shape claims file (e.g. an object instead of an array, or entries missing source.file/line/verbatim) flows into the sweep unchecked — add a shape assertion in loadClaims or an unconditional schema test (df1-2 archive names plugins/defender/tests/audit/dossier-sweep.ts loadClaims as the defender site). One edit per game closes both halves; both are on the same load path. WHY THIS IS NOT df1-3: df1-3 is the dossier-authoring story and its suite scope is enrollment + the skipped-on-CI prose model + floor derivation; the load-path hardening is fleet-wide (three games) and belongs with the fleet-wide story.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Scope Widening Notice

> ⚠ **SCOPE WIDENED (SM ruling, user-approved 2026-08-14)**: This story now covers FOUR games — the three named in the title (defender, millipede, centipede) PLUS pac-man (plugins/pac-man/tests/audit/dossier-sweep.ts:77), which carries the byte-identical vulnerable loadClaims. Every acceptance criterion that says 'three games' applies to all four; every per-game edit/test is replicated for pac-man. The epic YAML title still says three — that is the original filing, not current scope. The acceptance criteria below (when defined by TEA) are reproduced verbatim from the sprint YAML and have NOT been edited.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story df1-6` from the sprint YAML._
