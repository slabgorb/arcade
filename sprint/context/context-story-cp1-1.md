# Story cp1-1 Context

## Title
Scaffold — Vite/TS/Vitest on port 5278 + core/shell purity guard + CI caller + orchestrator registration

## Metadata
- **Story ID:** cp1-1
- **Type:** chore
- **Points:** 2
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** centipede
- **Epic:** Centipede — foundation slice (scaffold, citations, picture ROMs, playfield, player+shot, render)

## Problem
Mirror red-baron's config (the newest scaffold): Vite 8, TypeScript, Vitest 4, strictPort 5278, base '/'. src/core and src/shell skeletons with the boundary guard: a source-text scan test forbidding shell globals and wall-clock (window/document/Date.now/Math.random and friends) in src/core/ — word test prose to avoid the comment-scanner trap (tempest purity scanner matches forbidden names INSIDE COMMENTS). Ten-line CI deploy caller of slabgorb/arcade/.github/workflows/deploy-r2.yml@main targeting bucket arcade-centipede. Orchestrator side: add centipede to justfile games/subrepos lists and serve recipe, flip the CLAUDE.md port row from reserved to live.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- npm install && npm run build && npm test green from a fresh checkout; npm run dev serves on 5278 and fails loudly if the port is taken (strictPort).
- Purity guard test exists, passes, and demonstrably fails when a forbidden global is introduced in src/core/ (prove with a temporary red run in the story notes).
- CI workflow caller committed; a push to main would deploy dist/ to arcade-centipede (workflow-lints clean; no release cut in this story).
- just serve launches centipede alongside the fleet; just install-all covers it; CLAUDE.md port table row 5278 is live, not reserved.

---
_Generated by `pf context create story cp1-1` from the sprint YAML._
