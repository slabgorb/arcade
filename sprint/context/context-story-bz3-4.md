# Story bz3-4 Context

## Title
PER-TYPE COLLISION TABLES — the clone reuses one circle where the ROM has distinct proximity tables

## Metadata
- **Story ID:** bz3-4
- **Type:** bug
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** battlezone
- **Epic:** Battlezone — ROM fidelity against the original 1980 Atari source

## Problem
Cluster C4. Subsumes F-006, F-007. Depends on bz3-1 (units). The refuter confirmed F-005/006/007 are one architectural pattern: the clone flattens the ROM's per-type proximity tables to a single reused Euclidean tank-circle. (F-005, the shell-vs-tank view-space range+bearing cone, is ruled ACCEPT under the planar-sim descope and is NOT in this story.) Tank-vs-obstacle: the ROM uses per-type PROXTB radii (832/832/1024/960, i.e. .WORD 340/340/400/3C0 hex) where the clone sums two radii (~1876-2283), so obstacles block from ~2x too far (F-006). Shell-vs-obstacle: the ROM uses a distinct, smaller table PRXTBL (56/88/86, trailing-period decimals inside the hex block) with a >>2 range shift the clone omits, approximating it with the big tank-footprint radius (F-007).

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- Tank-vs-obstacle collision uses the per-type PROXTB radii (832/832/1024/960) rather than the summed-radii approximation.
- Shell-vs-obstacle collision uses the distinct PRXTBL table (56/88/86) with the ROM's >>2 range scaling, not the tank-footprint radius.

---
_Generated by `pf context create story bz3-4` from the sprint YAML._
