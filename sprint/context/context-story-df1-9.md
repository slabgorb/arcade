# Story df1-9 Context

## Title
Close the games-roster staleness cycle (routed from df1-7 review): reconcile the remaining stale roster surfaces AND pin the counts so they cannot silently desync again. (1) README.md:127 just build-all comment still says 'nine games + the lobby' — true count is eleven [DOC, comment-analyzer, high conf]. (2) CLAUDE.md R2 hosting table (~lines 203-212) lists nine games and omits the defender and millipede rows [RULE/DOC, rule-checker]. (3) No guard pins the roster total/raster numerals against the wired game set, so the prose rotted once (df1-5) and df1-7 fixed it by hand without closing the mechanism; add a guard deriving total===GAMES.length (and, where feasible, the raster/vector split) from tests/monorepo-topology GAMES so a future 12th game reddens instead of silently desyncing [TEST, test-analyzer, medium].

## Metadata
- **Story ID:** df1-9
- **Type:** chore
- **Points:** 3
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender — ROM source study + scaffold + fidelity harness (phase 1): the ground-truth dossier every later df* story cites

## Problem
The wired game set is **eleven** (`tests/monorepo-topology.test.mjs:96` `GAMES` =
tempest, star-wars, asteroids, battlezone, red-baron, centipede, joust,
missile-command, pac-man, millipede, defender). Several human-facing surfaces still
say "nine". df1-5 rotted these once; df1-7's review re-found two survivors and hand-fixed
them without closing the mechanism that lets prose numerals drift from the roster. This
story reconciles the last stale surfaces **and** installs the guard that df1-5/df1-7
never added.

**Verified against the current tree (SM, 2026-08-16 — all three claims LIVE):**
1. `README.md:127` reads ``just build-all            # build every app (nine games + the lobby) into dist/`` — still "nine games".
2. `CLAUDE.md` R2 hosting table, lines **203–212**, lists nine game rows and is **missing** a `defender/` row and a `millipede/` row (lobby + 9 games present; defender + millipede absent).
3. Existing guards pin the `plugins/` **directory** count (`tests/monorepo-topology.test.mjs:104` "exactly the eleven games") and prose **wording** (`tests/df1-5-defender-roster-doc.test.mjs`), but **no guard derives the roster total numeral / raster-vector split in prose from `GAMES`** — so a future 12th game silently desyncs the docs again.

## Technical Approach
_Routing-level only; TEA owns the RED tests and the guard's exact form._
- Two doc reconciliations (README:127 comment → "eleven games"; CLAUDE.md table → add
  the `defender/` and `millipede/` rows in roster order).
- One **new mechanical guard**, derived from `tests/monorepo-topology.test.mjs` `GAMES`,
  that fails when the roster **total** numeral in prose (README/CLAUDE.md) diverges from
  `GAMES.length` — and, where the source classifies them, the raster/vector split. The
  intent is that adding a 12th game reddens this guard instead of shipping stale docs.
  Reuse the `GAMES` anti-vacuity anchor already at `monorepo-topology.test.mjs:100`;
  don't hardcode "11".

## Scope
- **In scope:** README.md:127 comment, CLAUDE.md R2 table rows (203–212), and the new
  roster-count guard. `GAMES` in `tests/monorepo-topology.test.mjs` is the single source
  of truth to derive from.
- **Out of scope:** touching the game roster itself, registry regeneration, or the
  wording guard in `df1-5-defender-roster-doc.test.mjs` beyond what the total-count guard
  requires.

## Acceptance Criteria
_No ACs in the sprint YAML (epic field is `null`); the following are derived from the
story title for TEA to firm up in RED — three deliverables, one per title clause:_
- **AC1 (README):** `README.md:127`'s `just build-all` comment no longer says "nine games";
  it states the true wired count (eleven).
- **AC2 (CLAUDE.md):** the R2 hosting table (~203–212) includes a `defender/` row and a
  `millipede/` row, in roster order, matching the existing row format.
- **AC3 (guard):** a test derives the roster total (and, where feasible, the raster/vector
  split) from `tests/monorepo-topology.test.mjs` `GAMES` and fails if a prose surface's
  count numeral disagrees — verified non-vacuous by a mutation (bump `GAMES` or the prose
  and watch it redden).

---
_Generated by `pf context create story df1-9` from the sprint YAML._
