# Story ml7-10 Context

## Title
Verify + correct the DDT-bomb and POISON-mushroom sprite tiles — ml7-6 fixed the playfield char->tile BIT-6 bank select (mushrooms now decode at $3C-$3F) but the DDT stamps ($6E/$6F -> tiles $2E/$2F, ddt.ts:38) and POISON mushrooms ($78-$7B -> $38-$3B, conway.ts:48) were only mapped mechanically, never eyeballed (no rocks/poison/live-DDT in the attract field). Stage a wave with rocks + poison mushrooms + a live DDT bomb and visually confirm each tile; correct any mismatch. Visual playtest.

## Metadata
- **Story ID:** ml7-10
- **Type:** story
- **Points:** 3
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Millipede — phase machine, runtime wiring, HUD, showcase (phase 6-7): where it becomes a cabinet, with the epilepsy accessibility gate baked in

## Problem
ml7-6 fixed the playfield char->tile BIT-6 bank select (mushrooms now decode at $3C-$3F) but the DDT bomb and POISON mushroom sprite tiles were only mapped mechanically, never visually verified. The current mappings ($6E/$6F → $2E/$2F for DDT, $78-$7B → $38-$3B for POISON, rocks $70) were guesses; no rocks/poison/live-DDT exist in the attract field to eyeball. Need to verify each tile against the sprite ROM decode from ml7-6 and correct any mismatches.

## Technical Approach
1. **Derive ground truth from sprite ROM:** Use the sprite decode from ml7-6 (gfx-rom.ts) to identify the correct tile values for DDT ($6E/$6F), cloud ($6F/$2F), rocks ($70), and POISON mushrooms ($78-$7B).
2. **Write verification tests:** Create determinable tests in RED phase that assert the correct stamp→tile mappings against the decoded ROM sprite data.
3. **Correct the mappings:** Update ddt.ts (line 38) and conway.ts (line 48) with verified tile values if they differ from current constants.
4. **Visual playtest:** Stage a millipede wave with rocks + poison mushrooms + a live DDT bomb and confirm the render matches the ROM. (See human smoke-test dependency below.)

## Scope
- In scope: verifying DDT, POISON, and rock sprite tile mappings against ROM sprite data; writing ROM-sourced tests; correcting the constants in ddt.ts and conway.ts if needed.
- Out of scope: other sprite tiles, playfield rendering, audio, state machine.

## Acceptance Criteria
1. **DDT bomb stamp→tile mapping verified:** The constants `DDT_STAMP` ($6E) and `CLOUD_STAMP` ($6F → $2F, or $6F/$2E?) at ddt.ts:38 are confirmed against ml7-6's sprite ROM decode; corrected if wrong.
2. **POISON mushroom stamp→tile mapping verified:** The picture-code band `POISON` ($78-$7B → $38-$3B) at conway.ts:48 is confirmed against the ROM sprite decode; corrected if wrong.
3. **Rock ($70) sprite tile verified:** Rock tile constant is confirmed against the ROM decode.
4. **ROM-sourced test asserts each mapping:** A determinable test (RED→GREEN cycle) in ml7/src/core/ asserts each corrected stamp→tile value against the sprite ROM data so the mapping is locked in and never regresses.
5. **Visual playtest confirms render:** (Owner: human) A live millipede wave with rocks, poison mushrooms, and a live DDT bomb is rendered on `/millipede/` and each sprite tile is eyeballed to match the ROM visual. (See human smoke-test dependency.)

## Human Smoke-Test Dependency
This story carries a **human smoke-test tail** (owner: the user) that the pipeline (Dev/Reviewer) cannot fully discharge. The automatable core — verifying mappings from sprite ROM data and writing ROM-sourced tests — completes within Dev/Reviewer. The title's literal ask ("Stage a wave with rocks + poison mushrooms + a live DDT bomb and visually confirm each tile") requires rendering `/millipede/` and eyeballing each sprite to match the ROM, because the attract field has no rocks/poison/live-DDT to verify. The pipeline delivers the corrected constants + tests; the human then runs `/millipede/`, stages a playable wave, and confirms each tile visually. **Do NOT wait for the human test to merge the PR — route it as a post-merge smoke test in the session archive.**

---
_Generated by `pf context create story ml7-10` from the sprint YAML._
