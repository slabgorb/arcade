# Story df1-10 Context

## Title
Retire centipede inline loadSoundClaims (sound-dossier.test.ts:399), adopt the hardened dossier-sweep loader — the centipede analog of df1-8: sound-dossier.test.ts keeps its OWN private JSON.parse(readFileSync(soundClaimsPath)) as Claim or Claim-array loader, a separate copy from centipede's hardened dossier-sweep.ts loadClaims (hardened in df1-6). Delete the inline copy and source the shape-checked loader that names the offending file. Found by df1-8 reviewer (rule-checker #24 fleet-residue sweep).

## Metadata
- **Story ID:** df1-10
- **Type:** chore
- **Points:** 2
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender — ROM source study + scaffold + fidelity harness (phase 1): the ground-truth dossier every later df* story cites

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach

Retire the inline unhardened `JSON.parse(readFileSync(...))` loader in
`plugins/centipede/tests/audit/sound-dossier.test.ts:399` and adopt the hardened
`loadClaims()` from `plugins/centipede/tests/audit/dossier-sweep.ts` (hardened
in df1-6).

The inline loader has two defects that the hardened loader already fixes:
1. Uncaught `SyntaxError` on malformed JSON (raw stack, no file named)
2. No runtime shape validation — a well-formed-JSON-wrong-shape file flows through unchecked

The hardened loader wraps `JSON.parse` in a try/catch that names the file, and
validates each entry's `source` field via `isValidClaimSource` (composed from
the same guard functions the citation checker uses).

Mirroring df1-8 (joust analog): delete the inline copy and call the shared
hardened loader from the sweep module. Ensure the AST retirement guard
(scanning for the old `as Claim | Claim[]` cast pattern) reddens when the
inline loader is removed.

## Scope
- In scope: retire the inline loader at sound-dossier.test.ts:399, adopt the hardened dossier-sweep loader
- Out of scope: other centipede loaders, unrelated changes

## Acceptance Criteria

**AC1:** The inline unhardened `JSON.parse(readFileSync(...)) as Claim | Claim[]`
loader in `plugins/centipede/tests/audit/sound-dossier.test.ts:399` is deleted.

**AC2:** `sound-dossier.test.ts` calls the hardened `loadClaims()` exported from
`plugins/centipede/tests/audit/dossier-sweep.ts` instead of its own private loader.

**AC3:** The tests pass with the adopted loader (load-claims-hardening.test.ts
or equivalent coverage proves the retired inline copy is gone).

**AC4:** Mutation proof: dropping the hardened loader call or the import statement
reddens the test suite (validates the loader is essential).

---
_Generated by `pf context create story df1-10` from the sprint YAML._
