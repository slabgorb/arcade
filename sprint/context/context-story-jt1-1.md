# Story jt1-1 Context

## Title
Scaffold — Vite/TS/Vitest on port 5279 + core/shell purity guard + CI caller + orchestrator registration

## Metadata
- **Story ID:** jt1-1
- **Type:** story
- **Points:** 2
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** joust
- **Epic:** Joust — foundation slice (scaffold, citations, image transcription, arena, flight, render)

## Problem
Bootstrap the joust subrepo scaffold per the design spec (joust/docs/superpowers/specs/2026-07-19-joust-clone-design.md). Mirror centipede's config (the newest raster scaffold): Vite 8, TypeScript, Vitest 4, strictPort 5279, base '/'. src/core and src/shell skeletons with the boundary guard: a source-text scan test forbidding shell globals and wall-clock (window/document/Date.now/Math.random and friends) in src/core/ — word test prose to avoid the comment-scanner trap. Ten-line CI deploy caller of slabgorb/arcade/.github/workflows/deploy-r2.yml@main targeting bucket arcade-joust. Orchestrator side: add joust to justfile games/subrepos lists and serve recipe, flip the CLAUDE.md port row from reserved to live, update repos.yaml notes past pre-implementation.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- npm install && npm run build && npm test green from a fresh checkout; npm run dev serves on 5279 and fails loudly if the port is taken (strictPort).
- Purity guard test exists, passes, and demonstrably fails when a forbidden global is introduced in src/core/ (prove with a temporary red run in the story notes).
- CI workflow caller committed; a push to main would deploy dist/ to arcade-joust (workflow-lints clean; no release cut in this story).
- just serve launches joust alongside the fleet; just install-all covers it; CLAUDE.md port table row 5279 is live, not reserved.

---
_Generated by `pf context create story jt1-1` from the sprint YAML._
