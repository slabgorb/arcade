# Story ml10-5 Context

## Title
Harden ml10-4 pointer-lock test coverage: add a behavioural/source pin that main.ts wires the onReject diagnostic sink (deleting the console.warn 4th-arg currently ships green — dead-wiring class); add a boot-harness behavioural test for the R5 onExit->mouse.reset() reset-on-exit (only source-regex pinned today); and repoint the stale (main.ts:100) citation in pointer-lock.test.ts:169 to shell/input.ts onMouseMove

## Metadata
- **Story ID:** ml10-5
- **Type:** chore
- **Points:** 1
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Millipede — unwired-code findings (wire the built-but-dead seams)

## Problem
ml10-4 wired the millipede pointer-lock seam but its test coverage has three gaps.
This 1-point chore hardens that coverage. See the story title for the three
deliverables; the verified source anchors are below.

## Verified Source Anchors (SM confirmed against the tree at setup — all three premises hold)
Game: **millipede** (`plugins/millipede/`).

1. **onReject diagnostic sink — dead-wiring class.** `src/main.ts:67` is the 4th arg
   to the pointer-lock wiring: `(reason) => console.warn('millipede: pointer lock
   request rejected', reason)`. Deleting this arg currently ships GREEN — no test
   pins that main.ts wires it. Add a behavioural/source pin so that deletion reddens.

2. **onExit → mouse.reset() (the "R5" reset-on-exit).** `src/main.ts:66` is
   `() => mouse.reset()`. Only a source-regex pins it today; add a boot-harness
   BEHAVIOURAL test that the exit callback resets the mouse.

3. **Stale citation.** `tests/pointer-lock.test.ts:169` prose cites `(main.ts:100)`
   for the `dv += movementY` vertical-sign behaviour, but that behaviour actually
   lives in `src/shell/input.ts:51` `onMouseMove`. Repoint the citation there.

Files in scope: `plugins/millipede/src/main.ts`, `plugins/millipede/src/shell/input.ts`,
`plugins/millipede/tests/pointer-lock.test.ts`.

## Technical Approach
_TDD. TEA writes RED pins for deliverables 1 & 2 (deletion of the console.warn 4th-arg
and a mutated exit callback must both redden) plus the citation repoint for #3, then
Dev makes them green. The story title + Verified Source Anchors above are the spec._

## Scope
- In scope: the three deliverables above (test hardening + one citation repoint).
- Out of scope: any behavioural change to millipede's pointer-lock wiring itself.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED
phase from the three deliverables in the title + Verified Source Anchors above._

---
_Generated by `pf context create story ml10-5` from the sprint YAML._
