# Story SH3-7 Context

## Title
joust — correct the README @shared-consumption paragraph that SH3-1 falsified: the mulberry32 is no longer 'lifted byte-for-byte into src/core/frame.ts rather than imported' (it now imports @shared/rng via src/core/rng.ts), and joust consumes several @shared subpaths, not 'exactly one: @shared/audio' (measure: grep -rhoE '@shared/[a-z0-9-]+' plugins/joust/src | sort -u). The paragraph is pinned by the jt9-28 AC2 guard in tests/audio-seam-scope.test.ts (anchor 'zero-consumption outlier' + indicative/measured stamp), so the guard prose must move with it. Adjacent, out-of-scope note: a THIRD inlined mulberry32 (0x6d2b79f5) still lives in tools/sample-bake/bake-samples.mjs:66.

## Metadata
- **Story ID:** SH3-7
- **Type:** chore
- **Points:** 2
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Shared-module adoption — retire the straggler cabinets' per-game re-implementations of extracted shared code

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria

> ⚠ **SCOPE RULING (user, 2026-08-11) + TEA measurement — read before implementing.**
> TEA found a THIRD stale claim in the same "What joust does NOT do" section: the
> persistence bullet (`README:145`, "persists no high scores … no `@shared/highscore`")
> and the shell "**no storage**, deliberately" (`:138`) are both falsified by jt10-7
> (`main.ts:369` persists to localStorage keyed `joust-high-scores`, imports
> `@shared/highscore`). Correcting only the `@shared`-consumption bullet would make the
> section self-contradictory. **The user ruled: fix the whole section coherently.** The
> RED tests (`plugins/joust/tests/audio-seam-scope.test.ts`, SH3-7 section) and the
> session file's **TEA Assessment + Design Deviations** are the scope of record — the
> story title and AC-1 below understate it. See especially the jt9-28 AC2 **interlock**
> note in the TEA Assessment before touching the paragraph.

**AC-1: Correct the README @shared-consumption paragraph**
- The paragraph at `plugins/joust/README.md:148-149` must be updated to reflect the measured truth:
  - Remove "consumes exactly one `@shared` subpath: `@shared/audio`"
  - Replace with accurate list: `@shared/audio`, `@shared/font`, `@shared/held-keys`, `@shared/highscore`, `@shared/host-helpers`, `@shared/loop`, `@shared/name-entry`, `@shared/rng`, `@shared/view`
  - Remove "lifted byte-for-byte into src/core/frame.ts rather than imported"
  - Clarify that the mulberry32 is imported from `@shared/rng` via `src/core/rng.ts` (per SH3-1)
  - Remove the phrase "ending joust's run as the fleet's zero-consumption outlier"

**AC-2: Update the jt9-28 AC2 guard in audio-seam-scope.test.ts**
- The AC2 guard (`plugins/joust/tests/audio-seam-scope.test.ts` near lines 638-645) currently anchors on "zero-consumption outlier" and requires a dated `indicative`/`measured 2026-08-NN` stamp
- Update the guard prose to pin the CORRECTED README paragraph instead
- Ensure the updated prose correctly describes the shared-module consumption post-correction
- The guard test must pass after correction (no test reddens)

**AC-3: Explicitly fence bake-samples.mjs out of scope**
- The inline mulberry32 (0x6d2b79f5) at `plugins/joust/tools/sample-bake/bake-samples.mjs:66` must NOT be touched
- Verify it remains in place and unmodified after story completion

**AC-4: Test coverage**
- `vitest run --project joust` runs green (all joust tests pass)
- `npm run test:orchestrator` runs green (cabinet wiring invariants pass)
- The story commits cleanly with no debug code or stale branches

---
_Generated by `pf context create story SH3-7` from the sprint YAML._
