# Epic ad1 Context

## Title
Attract demos — the self-play the cabinets owe the lobby

## Overview

> ⚠ **CORRECTION (2026-08-01, measured at ad1-2 setup).** One clause of the Overview below
> is refuted. It is reproduced verbatim and has not been edited, because it is accurate for
> the other game it names.
>
> **"star-wars and battlezone show rotating text pages"** — false for **battlezone**, true
> for **star-wars**. On the current tree battlezone already self-plays:
> `plugins/battlezone/src/core/sim.ts:10` reads "Attract is a SELF-PLAYING demo (story AC)";
> the attract branch of `stepGame` calls the same `stepBattle` real play uses, with an
> autopilot driver (`demoInput` — patrol arc plus pot-shots), running `stepEnemies` **and**
> `stepSaucer`; and `plugins/battlezone/src/main.ts:307-309` already states "the demo
> battlefield keeps playing behind the text (attract)". All 11 battlezone test files
> reference attract. That work shipped in the bz1-10 era.
>
> **Effect on the stories.** `ad1-2` was repurposed at setup by user ruling — 5pt `tdd` → 1pt
> `trivial`, scope reduced to the manifest opt-in; its title still asserts the refuted
> premise (repurpose-without-retitle), so read `sprint/archive/ad1-2-session.md` before
> trusting it. **`ad1-1` (star-wars) is unaffected** and its premise stands as filed. The
> other four stories were not measured — apply the same check to each before starting it.
>
> The rest of the Overview holds, including the migration note about `src/host/registry.ts`.

The lobby showcase carousel (uf1-6) frames each game's LIVE attract mode, so a game with no self-play demo contributes a text screen or an idle death instead of gameplay. Day one only tempest self-plays (demoActive, story 10-3) and centipede animates its attract world; star-wars and battlezone show rotating text pages, asteroids drifts rocks with no ship, and joust boots gover=GOVER_RUNNING with nobody at the controls. Each story here gives one game a real self-play attract demo and then flips 'showcase: true' in its own plugins/<id>/plugin.ts manifest. NOTE THE MIGRATION: the 2026-07-30 collapse to a monorepo DELETED lobby/src/core/registry.ts — the file this epic used to name — and generates src/host/registry.ts from the seven manifests instead, so a tile can no longer be opted in from the lobby side at all. This work is owed to the cabinets regardless of the lobby: a machine that does not demo itself is unfaithful to every one of these ROMs.

## Metadata
- **Epic ID:** ad1
- **Repo:** arcade

## Background
_Cross-story constraints and guardrails to be filled in as the epic
progresses._

---
_Generated by `pf context create epic ad1` from the sprint YAML._
