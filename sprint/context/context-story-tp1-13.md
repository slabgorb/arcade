# Story tp1-13 Context

## Title
AUDIO WIRING GAPS — the missing warp phase, the bonus chime, the bolt-collision cue, and the invented kzap

## Metadata
- **Story ID:** tp1-13
- **Type:** bug
- **Points:** 4
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** tempest
- **Epic:** Tempest — ROM fidelity against Theurer's original 1981 source

## Problem
Cluster C9. Subsumes S-011, S-013, S-014, S-015. Depends on tp1-2, which produces the corrected T3 bytes this story reuses.

## Technical Approach

Quarry pre-extracted by tp1-2 (see `sprint/archive/tp1-2-session.md`):

- **T3 reuse (AC-1):** tp1-2 landed the displaced T3 record (`$cc81`, ALSOUN slot `T3 ;THRUST IN SPACE`) as a baked cue named `thrust_space` in `tempest/tools/pokey-bake/sfx-data.mjs`. It is deliberately unwired — no `GameEvent` plays it and it is NOT in `audio.ts`'s SOUNDS manifest. This story adds the SOUNDS entry and the warp-dive second-phase wiring together (audit S-014). The tp1-2 archive calls the consumer story "tp1-9" — stale numbering; the consumer is this story.
- **R2 caveat:** the `.wav` files are gitignored and served from `arcade-assets.slabgorb.com/tempest/sfx/` (bucket named plain `arcade`). Wiring `thrust_space` into SOUNDS 404s at runtime unless the file is in the bucket — verify it was uploaded with tp1-2's release; if not: `node tools/pokey-bake/bake-sfx.mjs <outdir>` then `wrangler r2 object put arcade/tempest/sfx/thrust_space.wav --file=<path> --remote`.
- **Citation trap (AC-6):** audit findings pin line numbers in `sfx-data.mjs` and `audio.ts`; ANY edit there (comments included) shifts anchors and breaks `npm test -- citations`. Re-anchor after edits. Use `remediated_by: tp1-13` ONLY for defects this story actually removes (candidates: S-011, S-013, S-014, S-015).
- **Test authority:** extend `tempest/tests/audit/alsoun-cue-mapping.test.ts` (the cluster-C8 chain-of-proof: 13-slot ALSOUN table, event→cue→ROM chain) for the new wirings.
- **Out of scope:** `countdown_beep.wav` is baked from `$cc69` (SL ;SLAM, the tilt siren), not a countdown — known, inert, documented in the POKEY map. Do not touch it.

## Scope
- In scope: the four wiring gaps named in the title (warp phase-2 sound, bonus chime trigger, bolt-collision cue + explosion, kzap.wav deletion) and the audit/citation bookkeeping they require.
- Out of scope: `countdown_beep` renaming, any re-baking of cues other than what AC-1 needs, unrelated changes.

## Acceptance Criteria
- The warp dive's second phase has its sound, reusing tp1-2's corrected T3 bytes.
- The special-score chime fires on the end-of-wave bonus (the sample is already correct — only the trigger is missing).
- Bullet-on-bolt collisions get their sound AND their explosion.
- kzap.wav is DELETED. It has no basis anywhere in ALSOUN's 13-sound table — it was invented.
- Depends on tp1-2.
- npm test -- citations stays green.

---
_Generated by `pf context create story tp1-13` from the sprint YAML._
