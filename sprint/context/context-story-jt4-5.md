# Story jt4-5 Context

## Title
Demo — 'two knights, full loop': co-op spawn -> waves -> death -> extra man -> game-over on 5279, dev-overlay readout

## Metadata
- **Story ID:** jt4-5
- **Type:** story
- **Points:** 2
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** joust
- **Epic:** Joust — game structure (the game loop): 2P co-op ledgers, all six wave types, BCD scoring, extra men, game-over

## Problem
The epic's demo bar: two knights playing a FULL loop on 5279 — co-op spawn, waves advancing, mounts dying, an extra man awarded, and game-over — with a DEV-OVERLAY score/lives/wave readout (the authentic MESSAGE.SRC display is jt5's row, deliberately NOT stubbed here). Drives the identical stepGame the sim runs; no shell-side game logic. Render source-wiring test per the ?raw idiom (jt1-6); no invented colors (denylist scan green). Screenshot committed as the epic demo artifact, taken from THIS checkout with port-ownership proof (lsof or a spare port — strictPort alone is porous, td1-1; a screenshot predating the code it demos misrepresents the renderer, td1-3).

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- Playable in the browser on 5279: two knights (P1/P2 per the 2P contract) spawn, clear waves, lose lives, earn an extra man, and reach game-over — the whole loop, driven by stepGame with no shell-side game state.
- The dev-overlay shows each player's score + lives + the wave number (authentic text display is jt5, not stubbed); the values match the core registers exactly (a source-wiring test proves the overlay reads the sim, not a copy).
- Render source-wiring test green; no invented colors (denylist scan green); screenshot committed with port-ownership proof recorded in the story notes.
- npm test fully green including citations and purity guard.

## Forward-carried from jt4-4

Routed here by the jt4-4 Impact Summary (sprint/archive/jt4-4-session.md) — all five are in scope
for this story; a real played full loop is the natural home for each:

1. **Respawn wiring + natural full-play to game-over** — no respawn exists, so lives can never
   reach 0 in natural play; jt4-4 pinned game-over via settleGameOver + a constructed all-dead
   state. This story wires respawn so the demo's death → extra man → game-over arc happens by
   actual play.
2. **Coop/survival bounty LIVE award** — only the gladiator bounty awards live in the consolidated
   loop; coop/survival remain pure functions (jt4-3 suite). Wire them live WITHOUT double-awarding
   (jt4-4's forced-clear advances were the double-award trap).
3. **Egg-wave self-clear** — settled wave eggs don't hatch→remount, so an egg wave can't be cleared
   yet. Wire the hatch off a settled wave egg so wave progression survives an egg wave.
4. **[MEDIUM] [TEST] hardening (a):** the GOVER all-out condition is pinned only via constructed
   state — pin it through a real played game here.
5. **[MEDIUM] [TEST] hardening (b):** the wave-5 egg test asserts only hasEgg — strengthen to the
   complement size + absence of materialised ground enemies.

NOT this story: the waveRowAt BCD wave-10+ row seam (fleet backlog, recorded in the jt4-4 archive).

---
_Generated by `pf context create story jt4-5` from the sprint YAML; forward-carries added by SM._
