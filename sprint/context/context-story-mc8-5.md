# Story mc8-5 Context

## Title
Wire the cruise/Sputnik parametric drone LIVE TRIGGER (CMSNON/STSNON gated by CRMONS) — mc8-4 built the sweeping drone voice; this starts/stops it from on-screen cruise-missile & Sputnik threats. BLOCKED until mc5 adds those enemies to core.

## Metadata
- **Story ID:** mc8-5
- **Type:** story
- **Points:** 3
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Missile Command — authentic audio (W3SOUN + POKEY)

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

### SM-verified setup findings (blocker is RESOLVED)
The title says "BLOCKED until mc5 adds those enemies to core." That blocker is now
CLEARED — measured, not inferred:
- **Sputnik enemy** shipped in core (mc5-2): `core/sputnik.ts`, `state.sputniks`.
- **Cruise missiles** shipped in core (mc5-3): `icbm.kind === 'cruise'` on `state.icbms`.
- **Pure selector already exists** — `core/drone-trigger.ts::droneRequest(state)`
  (mc5-3, GREEN) returns `'both' | 'cruise' | 'sputnik' | null` from cruise-on-screen
  + sputnik-active presence. This is the CRMONS-gated projection the trigger needs.
- **Drone voice + sweep** shipped in mc8-4: `core/drone.ts` (`droneSweep`, `DroneKind`)
  and shell `audio.ts` (`startLoop`/`stopLoop`/`feedDrone`).

### The wiring seam (where the LIVE TRIGGER goes)
`shell/audio-dispatch.ts::updateSustainedSounds(audio, state)` — called once per render
frame from `main.ts:65`. It currently ONLY stops the drone on `state.phase === 'over'`.
mc8-5 makes it START/STOP the drone from `droneRequest(state)` (and feed the per-frame
sweep). Note the existing terminal-edge guard: a running threat at game-over must still
be silenced (the `'over'` stop must survive) — the "edge-driven-voices gotcha" comment
in that file documents the leak this avoids. `stopLoop`/`startLoop` idempotency is
relied upon.

### Key files
- `core/drone-trigger.ts` — `droneRequest` (pure selector; already tested)
- `core/drone.ts` — `droneSweep`, `DroneKind`
- `shell/audio.ts` — `startLoop`/`stopLoop`/`feedDrone` lifecycle
- `shell/audio-dispatch.ts` — `updateSustainedSounds` (the seam to wire)
- `main.ts:65` — per-frame caller
- Existing tests to extend/inform RED: `tests/drone-trigger.test.ts`,
  `tests/audio-dispatch.test.ts`, `tests/audio-engine.test.ts`,
  `tests/cruise-integration.test.ts`

## Scope
- In scope: wire the drone LIVE TRIGGER — start/stop the sustained drone from
  `droneRequest(state)` presence and drive its per-frame sweep, in the shell seam.
- Out of scope: the pure selector (already exists), the drone voice/sweep (mc8-4),
  SLAM (mc8-6), and any core enemy behavior (mc5).

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED
phase from the title + the SM-verified seam above._

---
_Generated by `pf context create story mc8-5` from the sprint YAML._
