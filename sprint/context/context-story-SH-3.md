# Story SH-3 Context

## Title
Extract RNG into @arcade/shared/rng — settle the single Rng contract (mutable vs immutable) and migrate all sims

## Metadata
- **Story ID:** SH-3
- **Type:** story
- **Points:** 5
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade-shared
- **Epic:** Shared arcade library — extract cross-repo duplication into @arcade/shared

## Problem
rng.ts is logic-identical (mulberry32) across star-wars/asteroids/battlezone (mutable) and tempest (immutable, rngNext -> {value, rng}). Pick ONE contract, extract to @arcade/shared/rng, and migrate consumers — tempest's call sites are the real work if the mutable form wins (or vice-versa). Determinism/replay behavior must be preserved exactly for already-shipped games; pin them by tag.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- A single Rng contract is chosen and documented in the extraction; the decision + rationale is recorded (deviation log or ADR note).
- @arcade/shared/rng exports the PRNG; star-wars/asteroids/battlezone/tempest all consume it at a pinned ref; local core/rng.ts removed.
- Seeded-sequence tests prove byte-identical output to each game's pre-extraction RNG (no determinism regression).

---
_Generated by `pf context create story SH-3` from the sprint YAML._
