# Story df7-5 Context

## Title
HUD + scanner render: draw the score, the men (lives) indicator and the wave number, and PAINT the df5-1 scanner projection (its blip list) into the shell — the scanner is projected in core (df5-1) but drawn NOWHERE today. Consumes df5-1 scanner.ts (the blip list, finally painted) + df5-3 score.ts (score/men) + df2 palette/charset. Cite the ROM scanner bezel (*SCANNER BEZEL defender/AMODE1.SRC:1225) and the HUD layout. Colour by df2 palette INDEX only.

## Metadata
- **Story ID:** df7-5
- **Type:** story
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender phase machine + attract + wiring + HUD + showcase (df7, phase 6-7): turn df1-df6's pure cores into a played-start-to-finish cabinet — the attract->setup->play->death->game-over phase machine (pure-first, pm4/mc6 model), the self-playing attract demo, the hall-of-fame name-entry flow, the HUD + scanner render, 2P alternating handoff, lobby showcase, and the full-lifecycle visual playtest

## Problem
df5-1 built the scanner as a pure projection (blip list + palette index) and explicitly left the DRAW to df7's HUD; today nothing paints it. df7-5 draws the scanner strip (bezel per AMODE1.SRC:1225) plus the score/men/wave HUD figures, reading df5-1 (scanner blips) and df5-3 (score/men) — the shell paints, the core still owns no shell state. This is the df5-1 consumer the projection was built for.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- AC1: the shell renders the df5-1 scanner blip list into the radar strip (bezel geometry per *SCANNER BEZEL defender/AMODE1.SRC:1225, cited, claims/*.json under the df1-1 gate) — the scanner, drawn nowhere today, now shows off-camera attackers at their radar positions; blip colour is a df2 palette INDEX, never a hex literal.
- AC2: the HUD renders the score, the men (lives) indicator and the current wave number, reading df5-3 score.ts (score/men) — the shell paints figures via df2 charset/palette; the core owns no HUD state.
- AC3: every HUD/scanner-bezel constant is cited under the df1-1 gate; no un-cited value; purity.test.ts stays green (the render is shell, the projection stays pure df5-1).
- AC4: no full-frame strobe (Decision B); the HUD/scanner draw is stable across frames (no per-frame flicker) and reads correctly against a synthetic populated scanner (an off-camera blip is visible at the right position).

---
_Generated by `pf context create story df7-5` from the sprint YAML._
