# Story mc11-2 Context

## Title
Emit the bonus count-up cue: in the 'between' bonus resolution (game.ts:401-420) emit N {type:'bonusTick'} events (one per bonus unit tallied) instead of the lump waveEndBonus with soundEvents:[], so the TK cue plays per unit. Update sound-events.ts:62 to strike the 'emitter is a filed follow-up' note. Assert the event count matches the units paid. REV-01 W3SOUN SUNABM

## Metadata
- **Story ID:** mc11-2
- **Type:** story
- **Points:** 5
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Missile Command — wire the post-mc5 unwired seams: sputnik cruise-borrow, bonus count-up cue, name-entry timeout; retire dead twins

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria

Derived from the story title and SM verified premise:

1. **Emission of bonusTick events:** The `between` phase branch (game.ts:401-420) emits `{type:'bonusTick'}` events into the returned `soundEvents` array during end-of-wave bonus resolution. Each event represents one bonus unit credited to the player.

2. **Event count correctness:** The number of emitted `bonusTick` events matches the number of bonus units actually paid. This ensures the audio dispatch layer (audio-dispatch.ts:51) plays one `bonus-tick` cue per unit, giving accurate acoustic feedback of the bonus accumulation.

3. **Note removal:** The explanatory comment in sound-events.ts:62 that states "Emitter is a filed follow-up; the map is wired" is struck (removed) once this story ships, since the emitter will no longer be a missing piece.

**Key design question for TEA/Dev to resolve:** `waveEndBonus()` returns a point total, not a unit count. The specification calls for "one event per bonus unit tallied," but the mapping from points to unit count must align with the ROM's end-of-wave bonus count-up loop (W3SOUN / SUNABM). This points-per-unit ratio is the load-bearing derivation for the implementation. Do not invent a constant; derive the ratio from the cited ROM accounting.

---
_Generated by `pf context create story mc11-2` from the sprint YAML._
