# Epic uf1 Context

## Title
Unwired features — ported-but-unconsumed mechanics found by the 2026-07-28 fleet sweep

## Overview
A fleet-wide sweep on 2026-07-28 (all nine subrepos on develop) hunted for features that are built, tested and cited against ROM source but never reach the running game. Mechanical module-graph scans were roughly 95 percent false positives — the fleet is well wired: no cold GameState fields, every arcade-shared subpath consumed, every event kind emitted AND handled, every .wav in every manifest returning 200 on R2, so the sw3-5 silent-404 class is clean. The real findings came from in-source confessions (the author who left the gap signed it) and from partially-consumed catalogues. Most stories here consume tested code rather than deriving new fidelity: the port already exists and only the wiring is missing. Two exceptions are called out in their own stories — uf1-6 is a promise the docs make that the code never kept, and uf1-7 is documentation drift. The sweep's remaining finding — centipede and joust both shipping with no audio module at all — was epic-scale rather than story-scale and now has its own epics, cp5 and jt5, each seeded with a seam story. Context that drove that split: centipede and joust both ship with no audio module at all. centipede names its own gap in src/core/bonus.ts:32 — the bonus-life sound is 'DEFERRED to cp5 with the rest of the audio, and deliberately not stubbed, an empty hook would be dead code pretending to be a seam' — so centipede audio already has a planned epic id. joust carries no equivalent note. Both need the multi-story treatment tempest, star-wars and asteroids each received, not a single story.

## Metadata
- **Epic ID:** uf1
- **Repo:** red-baron,joust,star-wars,lobby,.

## Background
_Cross-story constraints and guardrails to be filled in as the epic
progresses._

---
_Generated by `pf context create epic uf1` from the sprint YAML._
