# Story sw2-6 Context

## Title
Disassembly fidelity audit (spike) — findings doc + file follow-on stories, no behavior change

## Story Type
chore (spike)

## Metadata
- **Story ID:** sw2-6
- **Epic:** sw2
- **Points:** 3
- **Priority:** p2
- **Repo:** star-wars
- **Workflow:** trivial
- **Branch:** chore/sw2-6-disassembly-fidelity-audit

## Technical Approach

This is a **read-only audit spike** — no behavior change to src/core or src/shell.

1. **Audit the ported Star Wars core sim** (`star-wars/src/core/`) against the authentic Atari 1983 Star Wars disassembly (available in `star-wars/reference/`).

2. **Document fidelity gaps** in a findings doc (committed to the repo under `docs/` or `reference/`):
   - List each gap with ROM references where available
   - Distinguish between intentional design descopes and unintentional porting errors
   - Focus on sim behavior, not UI/render fidelity (unless render logic affects sim state)

3. **Known audit targets** (carry forward from sw2-1):
   - TIE collision model (broad ROM-accuracy deliberately deferred to this spike)
   - Any other gaps discovered during the audit

4. **File follow-on stories** via `/pf-sprint story add` for concrete, actionable gaps found:
   - Each story must be scoped and actionable (not vague)
   - Do NOT hand-edit sprint YAML; use the `pf` command

## Acceptance Criteria

1. **No behavior change:** Tests green before and after. No changes to src/core or src/shell sim behavior.
2. **Findings doc committed:** Audit results and gap list live in the repo (e.g. `docs/sw2-6-fidelity-audit.md`).
3. **Follow-on stories filed:** Concrete, actionable stories created via `/pf-sprint story add` for gaps worth fixing.

## Reference Materials

- **Disassembly:** `star-wars/reference/` (ROM disassembly + annotations)
- **Core Sim:** `star-wars/src/core/` (deterministic sim + Math Box / math3d)
- **Shell (render/input/loop):** `star-wars/src/shell/`
- **Prior Context:** sw2-1 session archive (scope note on TIE collision model)

---
_Generated by sm-setup for trivial workflow._
