# Story SH2-10 Context

## Title
Extract @arcade/shared/view (browser) — resizeToDisplay + pure letterbox — and fold asteroids margin.ts + battlezone viewport.ts

## Metadata
- **Story ID:** SH2-10
- **Type:** story
- **Points:** 5
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade-shared, tempest, asteroids, star-wars, battlezone
- **Epic:** Shared render surface — extract font/glow/view/compositor into @arcade/shared and converge the cabinet on one vector treatment

## Problem
Extract the DPR-resize + letterbox concern all four games hand-roll in main.ts. resizeToDisplay(canvas, cssW, cssH, dpr) sizes the backing store to DPR, sets the CSS size, returns the resolved ViewportSize (DOM). letterbox(canvasW, canvasH, aspect) is PURE math returning the aspect-preserving rect + scale. First reconcile asteroids' margin.ts (55 lines) and battlezone's viewport.ts (97 lines) to confirm they reduce to one letterbox contract (or add explicit modes); then migrate all four games. Pure letterbox math gets node unit tests; resizeToDisplay is shell-verified.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- @arcade/shared/view exports resizeToDisplay(canvas, cssW, cssH, dpr) -> ViewportSize and a pure letterbox(canvasW, canvasH, aspect) -> {x,y,width,height,scale}; letterbox has node unit tests for the aspect math.
- asteroids' margin.ts and battlezone's viewport.ts are folded into the shared module (reconciled to one letterbox contract, or documented distinct modes); all four games consume @arcade/shared/view at a pinned ref.
- Each game's resize/letterbox behaviour is unchanged (manual run at multiple window sizes); vitest + vite build green.

---
_Generated by `pf context create story SH2-10` from the sprint YAML._
