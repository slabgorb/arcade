# Story df3-5 Context

## Title
Laser fire: plugins/defender/src/core/laser.ts — LFIRE (max 4 concurrent via LFLG<4, defender/DEFA7.SRC:2763-2766), right/left spawn by facing (LASR/LASL, defender/DEFA7.SRC:2790,2839), the laser data structure (LFLG/LCOLRX, defender/PHR6.SRC:301-303), travel + off-screen death. Each laser is a (super)process. Collision AGAINST ENEMIES is df4 — df3 fires and travels only.

## Metadata
- **Story ID:** df3-5
- **Type:** story
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender — the ship + the scheduler (phase 4a): the cooperative process kernel, the player ship, and the scrolling wrap-around world — the first epic that steps a clock

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_The story title above is the spec. Approach hints below are coordination
pointers, not implementation — TEA/Dev own the design._

- **Builds on df3-1's scheduler.** `plugins/defender/src/core/scheduler.ts`
  already shipped (merged). "Each laser is a (super)process" means each fired
  laser runs as a scheduler process — model `laser.ts` on that kernel, don't
  invent a parallel stepping loop.
- **`laser.ts` is pure core.** New file `plugins/defender/src/core/laser.ts`
  lives under `src/core/`, which defender's purity test scans: no clock, no
  DOM, no render, no `Date`/`performance` — deterministic sim only.
- **ROM spec, from the title:** LFIRE fires; max 4 concurrent gated by LFLG<4
  (`defender/DEFA7.SRC:2763-2766`); spawn side chosen by ship facing via
  LASR/LASL (`DEFA7.SRC:2790` right, `:2839` left); laser record is LFLG/LCOLRX
  (`defender/PHR6.SRC:301-303`); lasers travel and die off-screen.

## Scope
- **In scope:** fire (LFIRE), the 4-concurrent cap, facing-based spawn side,
  travel, and off-screen death — as a scheduler process.
- **OUT of scope (df4):** collision AGAINST ENEMIES. df3-5 fires and travels
  ONLY. Do not spec or test enemy collision — that is a separate story.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story df3-5` from the sprint YAML._
