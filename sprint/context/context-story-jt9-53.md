# Story jt9-53 Context

## Title
pin tick() idle path: an idle tick must not release a sounding zero-window voice

## Metadata
- **Story ID:** jt9-53
- **Type:** bug
- **Points:** 2
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Joust — the remainder, re-ordered: apparatus, gameplay, geometry, brains, dossier

## Problem
Filed by jt9-35 Reviewer (2026-08-05), non-blocking MINOR, sibling to jt9-52. One adjacent mutant of src/shared/audio.ts survives the full 560-test shared suite: hoist the release out of tick()s countdown block so it fires whenever voiceFrames === 0 (on EVERY idle tick), not only on the countdown-to-zero pass. Site: tick() at src/shared/audio.ts:324-326 (`if (voiceFrames > 0) { voiceFrames -= 1; if (voiceFrames === 0) releaseVoice() }`). Non-equivalent in principle: a zero-length-window voice still SOUNDING would be released by the next idle tick(), so a subsequently accepted arbitrated cue would not cross-steal (:266) and TWO voices sound at once. LATENT today: the precondition is a manifest degrade path — an arbitrated priority with NO frameDurations entry (voiceFrames === 0 with voiceChannel set) — which no shipped cabinet has (joust/centipede declare a duration for every arbitrated cue; joust framesFor throws on frames <= 0). Same latent shape jt9-6/jt9-35/jt9-52 pin at the other release sites. SCOPE: add one engine test to src/shared/tests/audio-voice-release.test.ts — ZEROWIN manifest shape (a priority with no frameDurations entry) plus one tick() before the steal: an idle tick must NOT release a sounding zero-window voice; the next accepted arbitrated cue must still steal its channel. Re-measure the line anchors at setup (jt9-30: these audio.ts anchors drift by the hour).

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

### SM Setup Findings (measured 2026-08-07 against the current tree)
The story instructs "Re-measure the line anchors at setup." Done:

1. **Anchor is accurate — no drift today.** `src/shared/audio.ts:324-326` reads
   exactly `if (voiceFrames > 0) { voiceFrames -= 1; if (voiceFrames === 0) releaseVoice() }`.
   The mutant to pin: hoist the release so it fires whenever `voiceFrames === 0` on
   EVERY idle tick, not only on the countdown-to-zero pass. (Re-grep at RED time
   anyway — these anchors drift by the hour per jt9-30.)
2. **Target test file already exists:** `src/shared/tests/audio-voice-release.test.ts`.
3. **`ZEROWIN` already exists** in that file (≈line 336) and existing tests already
   use it to pin the `:222` and `:313` release sites. REUSE the existing `ZEROWIN`
   const — do NOT duplicate it. The genuinely-NEW coverage jt9-53 adds is the tick()
   IDLE path (`:324-326`), a distinct mutant site: an idle `tick()` must NOT release a
   sounding zero-window voice, and a subsequently accepted arbitrated cue must still
   cross-steal its channel (the `:266` steal).

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story jt9-53` from the sprint YAML._
