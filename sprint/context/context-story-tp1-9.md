# Story tp1-9 Context

## Title
THE CAMERA — the far ring is built about the projected vanishing point, and FAR_RATIO is per-well, not a single 0.2

## Metadata
- **Story ID:** tp1-9
- **Type:** bug
- **Points:** 6
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** tempest
- **Epic:** Tempest — ROM fidelity against Theurer's original 1981 source

## Problem
Cluster C5. Subsumes DB-006, DB-007, DB-008, DB-009, DB-016. Our perspective CURVE is algebraically correct; the CONSTANT is not. FAR_RATIO is a single module-level 0.2 for every level; the arcade's is per-well, R = (16+H)/(240+H), ranging 0.104-0.164. project, boundaryRail, laneWidth and flipPivot all close over the current module constant, so this is an API change, not a constant swap. Blocks tp1-10.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- perspectiveDepth() takes the tube and derives R = (16+H)/(240+H) per well — the module-level FAR_RATIO = 0.2 is deleted. A test pins the range at 0.104-0.164 across the 16 geometries.
- The far ring is built about the PROJECTED VANISHING POINT, not the ring centroid.
- The per-well screen-Z translation and its level-start slide exist.
- The eye is a movable parameter (tp1-10 needs to fly it into the well).
- project, boundaryRail, laneWidth and flipPivot take the tube rather than closing over a module constant.
- The starfield reuses the same hyperbolic law.
- npm test -- citations stays green.

---
_Generated by `pf context create story tp1-9` from the sprint YAML._
