# Story pm5-1 Context

> ⚠ **SM RULING (2026-08-16): the title's either/or is SETTLED — ROUTE-THROUGH, not delete.**
> The premise was verified against the current tree: `main.ts:232` (`!== null`, "draw the
> ghost at all?") and `render.ts:312` (`=== 'eyes'`, "render as eyes vs. regenerated body?")
> are two *legitimately distinct* predicates — the "disagreement" is not a bug. Route ONLY
> `main.ts:232` through `isReturningHome`; leave `render.ts:312`'s `=== 'eyes'` untouched
> (routing it through the helper would render a climbing-out regenerated body as eyes — a
> regression). The derived ACs and verified premises live in `.session/pm5-1-session.md`
> (Background + Acceptance Criteria) — TEA's primary input. Do NOT act on the "or delete the
> helper" half of the title below.

## Title
Single-source the eyes/return-home predicate: route the inline checks at main.ts:232 and render.ts:312 through isReturningHome (core/game.ts:278) — reconciling the !==null vs ==='eyes' disagreement to the intended semantics — or delete the helper as redundant. Assert the render mode matches the chosen predicate. Reference pm4-3

## Metadata
- **Story ID:** pm5-1
- **Type:** story
- **Points:** 2
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Pac-Man — groom the redundant dead twins: single-source isReturningHome, level-table mirror fields, high-score-qualified event

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story pm5-1` from the sprint YAML._
