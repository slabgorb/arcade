# Story df1-8 Context

## Title
Harden joust inline loadClaims (citations.test.ts:641): the same unhardened JSON.parse(readFileSync(...)) as Claim | Claim[] pattern df1-6 hardened in the four dossier-sweep loaders survives in joust's INLINE loadClaims at plugins/joust/tests/audit/citations.test.ts:641 (a separate copy from joust/tests/helpers/claims.ts, which is already hardened via asClaim). Wrap the parse per-file in a controlled error naming the file + shape-assert each entry's source, mirroring df1-6. Routed from df1-6 review (reviewer round 1). joust and missile-command helpers/claims.ts are already hardened; this is the last fleet residue.

## Metadata
- **Story ID:** df1-8
- **Type:** chore
- **Points:** 2
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender — ROM source study + scaffold + fidelity harness (phase 1): the ground-truth dossier every later df* story cites

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach

The unhardened inline loader at `plugins/joust/tests/audit/citations.test.ts:641-647` does a raw `JSON.parse(readFileSync(join(claimsDir, f), 'utf8')) as Claim | Claim[]` per file with NO per-file error naming and NO shape-assertion of `source`.

**Reference model to mirror:**
- `plugins/joust/tests/helpers/claims.ts:40` — exports `asClaim(value, file)` which throws a controlled error naming `claims/${file}` and shape-asserts `id`/`claim`/`source`.

**Precedent from df1-6:**
- `plugins/defender/tests/audit/dossier-sweep.ts:149` — the df1-6 implementation of `loadClaims(dir)` with hardened error naming and shape validation.

Mirror the df1-6 approach to wrap the inline parse in controlled per-file error handling and validate claim shape.

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story df1-8` from the sprint YAML._
