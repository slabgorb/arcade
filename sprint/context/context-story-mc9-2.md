# Story mc9-2 Context

## Title
Per-wave 8-colour palette and colour cycling (resolve open question O-5)

## Metadata
- **Story ID:** mc9-2
- **Type:** story
- **Points:** 5
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Missile Command — render fidelity (real graphics)

## Problem
Colour fidelity. Port SET UP COLORS FOR NEXT WAVE (W3DSUP.MAC:1583): the per-wave 8-colour palette applied to background, structures, missiles, and blasts, plus the colour-cycling the cabinet uses. This RESOLVES open question O-5 — the palette / 3rd-colour-bit / hardware address scramble that mc3 render explicitly did not reproduce; the resolution (how REV-01 maps palette indices to the 8 hardware colours) is documented in the dossier or the mc9 design doc and cited. Shell-only (a palette data module + render wiring). Depends on mc3; independent of mc9-1 but naturally lands after it.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- the field is drawn through a per-wave 8-colour palette sourced from SET UP COLORS FOR NEXT WAVE (W3DSUP.MAC:1583): the palette changes wave to wave and background/structures/missiles/blasts pull their colour from it, not from hard-coded hex.
- open question O-5 is resolved in writing (how REV-01 maps palette indices to the 8 hardware colours, incl. the 3rd-colour-bit/address scramble) and the resolution is cited; the dossier/design doc records it.
- colour cycling is reproduced where the cabinet cycles; render tests assert palette selection per wave; shell-only, purity.test.ts stays green.

---
_Generated by `pf context create story mc9-2` from the sprint YAML._
