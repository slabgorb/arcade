# Story jt13-13 Context

## Title
Give the joust attract-mode self-play demo a real player AI, so the demo bird actually plays (flap/steer/joust) instead of standing dead — and so live respawn-physics changes stop being frozen by the passive-demo fingerprints

## Metadata
- **Story ID:** jt13-13
- **Type:** feature
- **Points:** 5
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Joust gameplay & entry-screen bug fixes

## Problem
ENABLER for jt13-14. The attract 'self-play demo' isn't self-playing: main.ts:618 steps it with EMPTY inputs — `stepGame(cabinet.game, {})` — so the demo bird never flaps, never steers, dies on the first hazard and just re-materialises in place. main.ts even calls it the 'self-play demo' (comments :279-280,:330,:394) but no code drives the player. Two problems this causes: (1) the lobby demo looks broken (a dead bird, not a game); (2) the demo fingerprint tests (audio-events.test.ts, the audio-thud and dumb-wingbeat seeds) are pinned to the EXACT proc/score/lives/wave-advance frame sequence a PASSIVE player produces, which is why any change to respawn or flight physics cascades into them and why several seeds 'never advance waves' — a held or grounded respawn bird can't clear a wave when nobody flaps it (see memory: joust-attract-demo-is-passive-no-ai). Deliver a minimal deterministic demo player AI: feed `stepGame` a synthesised input each frame (flap toward survival / height, steer toward eggs or away from lava, joust from above) so the demo actually progresses and clears waves. It MUST be pure + seeded (no Date/Math.random) so the demo stays replay-deterministic and the fingerprint tests can be RE-BASELINED onto the new active-demo trajectory rather than deleted (preserve the coverage — re-sweep procs/scores/lives + wave-advance frames; rng staying constant is the tell it's an intended reshape, not a logic drift). Keep the `stepGame(` literal seam the demo-source / gameover-wiring tests pin (main.ts:384,:656-657). Verify VISUALLY (Playwright headless): the attract demo should be seen playing, not dying in place.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story jt13-13` from the sprint YAML._
