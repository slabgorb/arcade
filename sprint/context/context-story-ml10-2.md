# Story ml10-2 Context

## Title
Wire high-score persistence: load the ladder on boot, save it after name entry

## Metadata
- **Story ID:** ml10-2
- **Type:** feature
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Millipede — unwired-code findings (wire the built-but-dead seams)

## Problem
shell/highscore.ts (makeMilliHighScoreStorage, loadHighScores) is built and tested (high-score-persistence.test.ts) but imported by NOTHING in src/ — self-labelled "Deliberately UNWIRED (ml7-3)" at shell/highscore.ts:18. main.ts seeds from core/highscore DEFAULT_HIGH_SCORES only, so the ladder resets on every reload and a qualifying score is never persisted. Wire it: on boot, main.ts loads the table via loadHighScores(makeMilliHighScoreStorage()) (falls back to the seeded ROM ladder on an empty/first boot); after a game ends and name-entry commits, save the updated table through the storage. One-origin cabinet key (MILLI_HIGH_SCORE_GAME_ID = 'millipede') so the shared arcade-lobby localStorage persists it and the lobby can read it. Reuse the tested module and the existing name-entry primitives (core/highscore insertHighScore / qualifiesForHighScore / stepInitials) — no new logic. NOTE the "still attract-only" premise in the shell/highscore.ts:18 comment is STALE: main.ts already steps into play (stepGame) and has a name-entry phase — verify that seam before pinning. Seams: src/main.ts (boot load + post-entry save); shell/highscore.ts unchanged or minimal.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- On boot, main.ts loads the ladder via loadHighScores(makeMilliHighScoreStorage()) and renders it (attract showcase + HUD hi-score), falling back to DEFAULT_HIGH_SCORES on a first/empty boot.
- After a game ends and a qualifying score is entered, the updated ladder is saved via makeMilliHighScoreStorage().save under the one-origin cabinet key ('millipede') and survives a reload — a returning boot shows the persisted board.
- Persistence is proven end-to-end through main.ts wiring (boot → play → name-entry → reload), not only the existing module unit tests; no regression to the name-entry flow, the self-playing attract demo, or the HUD.

---
_Generated by `pf context create story ml10-2` from the sprint YAML._
