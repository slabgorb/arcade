# Story jt8-3 Context

## Title
Cliff look-ahead (B2XLEN/SHXLEN) plus shadow-lord SHLEP player-line tracking

## Metadata
- **Story ID:** jt8-3
- **Type:** story
- **Points:** 3
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Joust — playability: enemies hunt (full pursuit AI) and eggs can be caught

## Problem
The hunter/shadow navigation and the deadliest brain. B2DIR (:4104-4159) projects B2XLEN = 27+4 = 31 px ahead in the travel direction (scaled by PVELY), samples the arena solid-mask (BCKXTB plus/minus B2XLEN AND BCKYTB) — our arena.groundMaskAt is that BCKXTB/BCKYTB analog — and on a cliff ahead flips PFACE and slows (B2AV). The plain bounder lacks this look-ahead (B2XLEN :3969, SHXLEN :4228 = 31). Shadow lord (SHADOW :4230): SHDN free-fall on descent (NO FLAPING WINGS, :4246), SHLEP tracks the PLAYER line directly (:4277), flaps only to escape the lava below dollar-D3. Extend the hunter/shadow brains (enemy.ts b2undr/shadow) with the look-ahead turn and the shadow player-line track (bounder unchanged). BUILDS ON jt8-1/jt8-2. Radix-cited plus claims entries.

## Technical Approach

> ✅ The Problem paragraph above is the epic description **verbatim**, and the SM
> measured every citation in it against `reference/williams-source/joust/JOUSTRV4.SRC`
> on 2026-08-01: **all verified, no corrections needed.** The measured-facts block,
> two open questions for TEA (the `:4252` PVELX/"FALLING?" comment/code mismatch,
> and the `$D0`-vs-`$D3` lava-threshold split), and the inherited jt8-2 handoff
> items live in `.session/jt8-3-session.md` → Background. Read them before RED.

Measured pointers (facts, not design — TEA/Dev own the approach):
- ROM: `B2DIR` :4104-4159 (hunter look-ahead; PFACE writes :4122/:4141, `B2AV` slow
  :4142-4145); equates `B2XLEN EQU 27+4` :3969, `SHXLEN EQU 27+4` :4228; `SHDIR`
  :4330-4377 (the shadow's own copy — SHXLEN at :4350/:4368, `SHAV` slow :4373, plus
  a `$D0` lava pre-check :4330-4334 the hunter lacks); `SHADOW` :4230, `SHDN`
  :4246-4248, flap-escape :4249-4254 (`$D3`); `SHLEP` :4277-4284 stores the player's
  line AND velX on an 8+1-frame timer, `SHLEP1` :4286-4298 tracks.
- Code: `plugins/joust/src/core/enemy.ts` — look-ahead is "provenance-only" (:362);
  the steering is explicitly assigned to jt8-3 (:491-497); `shadow()`'s vertical
  flap logic (:471-487, from uf1-8) already exists. `arena.groundMaskAt` is the
  `BCKXTB`/`BCKYTB` analog.
- Tests to inherit: `plugins/joust/tests/homing-wiring.test.ts` AC-4 (repair its
  fixture overclaim first — jt8-2 Reviewer [MEDIUM]); extract the now-3×
  `loadClaims`/`claimCovers` duplication to `tests/helpers/` in this story.

## Scope
- In scope: hunter + shadow horizontal steering (look-ahead turn + slow), the
  shadow SHLEP player-line track, claims entries, the helper extraction above.
- Out of scope: the plain bounder (unchanged — AC-1 pins that it does NOT look
  ahead); the shadow's existing vertical flap logic except where SHLEP fidelity
  requires it.

## Acceptance Criteria
- The hunter/shadow look-ahead turns the enemy (flip PFACE plus slow) when a cliff is B2XLEN=31 px ahead in its travel direction, and does NOT turn in open air — both cases pinned against arena.groundMaskAt; the plain bounder does NOT look ahead. Claims entry (:4104-4159, B2XLEN :3969 / SHXLEN :4228).
- The shadow lord free-falls on descent (no brake) and tracks the targeted player line via SHLEP; lava-escape below dollar-D3 holds; pinned plus mutation-checked (:4230-4277).
- Radix-cited constants plus claims entries; the citations suite is green.
- Determinism: a seeded hunter navigating a cliff and a shadow hunting a player replay bit-for-bit; purity guard plus npm test green.

---
_Generated by `pf context create story jt8-3` from the sprint YAML._
