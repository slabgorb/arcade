# Story sw8-15 Context

## Title
music-bake comment accuracy: the sw8-14 double-play prose overstates history and three catalogue counts went stale. (1) audio.ts:83 + gen-music-data.mjs:270 say REB 'had been ringing twice'/'rang once per iteration AND again from that cue' — it never did: finish_ground.wav 404s in R2, so the cue was always silent and REB rang only from the towers loop (sw8-14 Dev finding recorded the correction; the source comments did not get it). (2) events.ts:217 'The five one-shot POKEY tunes' sits above a SEVEN-member TuneName union. (3) bake-music.test.mjs:201 banner 'the five one-shot TUNES' sits above a seven-entry list. (4) bake-music.mjs CLI: --only <unknown-track> silently bakes ZERO files and exits 0, bypassing bakeTrack's own 'unknown track' throw — the file already validates unknown FLAGS, so the value gap is inconsistent

## Metadata
- **Story ID:** sw8-15
- **Type:** chore
- **Points:** 1
- **Priority:** p3
- **Workflow:** trivial
- **Repo:** arcade
- **Epic:** Cabinet feel: the flight and combat loop — render/experiential fidelity vs the cabinet longplay

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
Wrong-prose corrections follow the project rule: grep every synonym/phrasing of
the corrected claim (not just the named lines — the story's line numbers had
drifted), fix every live present-tense mirror, and leave explicitly-dated
historical records (RED-contract headers, archived sessions, audit findings)
alone. Ground truth for item (1) is the sw8-14 Dev finding: the double-play was
LATENT, not live — finish_ground.wav was never uploaded to R2 during the
concatenation era, so the core's cue was silent and REB rang only from the
towers loop. (The asset is live NOW — curl 200 — so the past tense is the whole
correction.) Item (4) mirrors the CLI's existing unknown-flag throw, placed
before `mkdirSync` so a rejected invocation has no side effects.

## Scope
- In scope: the four items in the story title, plus every live mirror of the
  same wrong claims found by the synonym sweep (two beyond the named spots:
  `gen-music-data.mjs:91` "the five one-shot tunes", and the
  `music-data.test.mjs` sw8-14 block's "REB has been ringing TWICE… left live").
- Out of scope: historical/frozen prose that is dated on its face — the sw6-1
  and sw7-8 RED-contract headers ("the four .wav", "the five .wav files"),
  archived session records, audit `verbatim` quotes.

## Acceptance Criteria
- [ ] audio.ts and gen-music-data.mjs no longer claim REB rang twice; both state
  the double-play was latent (silent 404ing cue; only the towers loop rang), citing
  the sw8-14 Dev finding.
- [ ] music-data.test.mjs's sw8-14 comment block carries the same correction
  ("left live for REB" is gone).
- [ ] events.ts describes the TuneName union as seven tunes (five at sw7-8,
  themeB sw8-12, finishGround sw8-14); bake-music.test.mjs's banner and
  gen-music-data.mjs's FLATTENING comment no longer say "five".
- [ ] `bake-music.mjs --only <unknown>` exits nonzero naming the track, creates
  no outDir, and a vitest CLI test pins it (status > 0, stderr names the track).
- [ ] `--only <known>` still bakes exactly that track's file.
- [ ] Citation gate green after `reanchor-citations.mjs --write` (audio.ts
  citations U-009/U-024 moved, 0 lost); repo lint green.

---
_Generated by `pf context create story sw8-15` from the sprint YAML._
