# Story df4-4 Context

## Title
Mutants (SCZ/'SCHITZO') + the UFO pursuer: plugins/defender/src/core/ reducers for the SCZS0/SCZ0 schizoid process (START SCHITZOS, defender/DEFB6.SRC:585,592 — the mutant a lander becomes on reaching the top) and the UFOST/UFOLP process (UFO PROCESS START, defender/DEFB6.SRC:5,25 — velocity defender/DEFB6.SRC:48, shoot logic; the timeout pursuer). FIRST map both ROM labels to their arcade enemy names in the dossier, CITED (the UFO->baiter mapping in particular is a claim, not an assumption). Consumes df4-1 + df4-2; the lander->mutant transform trigger is set up by df4-3.

## Metadata
- **Story ID:** df4-4
- **Type:** story
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Defender — the menagerie (phase 4b): one cited reducer per enemy, the shared collision seam, and the first effects that kill — with the photosensitivity accessibility exception decided here

## Problem
Two Defender enemy processes are still unbuilt: the SCZS0/SCZ0 **schizoid** process
(the MUTANT a lander becomes on reaching the top, `defender/DEFB6.SRC:585,592`) and the
UFOST/UFOLP **UFO** process (the timeout pursuer, `defender/DEFB6.SRC:5,25`, velocity
`:48`, plus shoot logic). Both are `df3` scheduler processes; colour comes from the
`df2` palette index only.

## Verified premise (checked against the tree at setup)
- All cited ROM lines exist and are accurate in `reference/original-source/defender/DEFB6.SRC`:
  `:5` = `UFOST NEWP UFOLP,STYPE`, `:25` = `*UFO PROCESS`, `:48` = `*UFO VELOCITY`,
  `:585` = `*START SCHITZOS`, `:592` = `SCZS0 NEWP SCZ0,STYPE`.
- Dependencies landed: df4-1 (COLIDE box pre-test), df4-2 (appearance), df4-3
  (lander→mutant transform trigger + abduction mechanic, merged PR #499).
- The enemy-identity dossier to extend is
  `plugins/defender/docs/rom-study/claims/15-enemies.json` (defender claims live under
  `plugins/defender/docs/rom-study/claims/`, **not** a top-level `claims/`).

## Technical Approach
- **Reducers are PURE and live in `plugins/defender/src/core/`** (`mutants.ts`, `ufo.ts`) —
  no shell/clock/`Date`/`Math.random`/canvas imports; the `purity` core-boundary test scans
  this directory's source text. Consume df4-1's COLIDE box pre-test for collision queries.
- **FIRST deliverable is the CITED dossier mapping.** Map both ROM labels to their arcade
  enemy names in `15-enemies.json` with a source citation. **The UFO→BAITER mapping is a
  CLAIM to be sourced, not assumed** — cite it; do not hard-map it silently.
- **RED teeth (identity-glossary gate):** the RED phase must assert a per-row
  `expectPopulated` check (all required dossier fields present) BEFORE citation byte-checks,
  so coverage/byte sweeps cannot pass vacuously (prior df4 convention — see
  `[[defender-enemy-identity-glossary-gate]]`).

## Scope
- In scope: `15-enemies.json` mappings (SCZ→MUTANT, UFO→BAITER, both cited); pure
  `core/mutants.ts` and `core/ufo.ts` reducers; synthetic-process test coverage.
- Out of scope: scheduler integration of live enemies; scoring (that is df5); bombers/
  mines/pods/probes (df4-5); the visual playtest (df4-5's sibling df4-... playtest story).

## Acceptance Criteria
The authoritative, derived ACs live in `.session/df4-4-session.md` (5 ACs). Summary:
1. `15-enemies.json` extended with SCZ→MUTANT and UFO→BAITER, each fully cited; per-row
   `expectPopulated` passes.
2. Per-enemy RED populated check runs BEFORE citation byte-checks (anti-vacuous gate).
3. PURE `core/mutants.ts` schizoid reducer (lander→mutant transform, df4-3 trigger); purity green.
4. PURE `core/ufo.ts` UFO reducer (velocity `:48`, shoot logic, COLIDE queries); purity green.
5. Synthetic-process test coverage, critical behavior mutation-proven (timeout threshold,
   pursuit velocity, mutant appearance).

---
_Enriched by SM at setup from the derived session ACs; base generated by `pf context create story df4-4`._
