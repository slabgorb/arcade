# Epic ml10 Context

## Title
Millipede — unwired-code findings (wire the built-but-dead seams)

## Overview
Tenth Millipede epic. Architect unwired-code sweep 2026-08-15 (recorded in memory: millipede-unwired-code-audit): four places where millipede code is BUILT and TESTED but never reaches the running game. All four are WIRING, not new systems — reuse-first; the tested code and (for the dead-branch pair) the live state already exist. FALSE POSITIVE, do not refile: the seven enemy presence -start/-stop voices READ as unemitted to a literal grep but ARE emitted via a computed `${root}-start` template at sim.ts:229 (PRESENCE_VOICES, ml7-8 shipped). CLEARED, not dead: the top-level core/<enemy>.ts files (roster.ts orchestrates them). GROUPING RULE (inherit ml8/ml9): group stories by the FILES they edit, not by finding — the two dead-input-branch fixes both touch core/sim.ts and are one story (ml10-3) with two commits (they share the dead-feature-signature-unpopulated-input-field class). The dead-branch findings need a test that reaches the function through the REAL producers, not a hand-built input — a data test stays green while the branch is dead (exactly how joust's EGGVAL rungs stayed dead). Points are seed-only; expect the reviewer's mutation batteries to file follow-ups.

## Metadata
- **Epic ID:** ml10
- **Repo:** arcade

## Background
_Cross-story constraints and guardrails to be filled in as the epic
progresses._

---
_Generated by `pf context create epic ml10` from the sprint YAML._
