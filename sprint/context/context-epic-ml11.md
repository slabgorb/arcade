# Epic ml11 Context

## Title
Millipede — deferred visual-fidelity follow-ups (the render-surface loose ends ml7/ml9/ml10 left filed but homeless)

## Overview
Eleventh Millipede epic. The cabinet is complete and shipped (ml1-ml10 all archived); this epic collects the grounded, CITED visual-fidelity deferrals whose parent epics closed under them, so a filed Delivery Finding does not die in the archive (the star-wars sw6-1 lesson). Architect sweep 2026-08-16 (invocation: /pf-architect materialize the next epic for millipede) over every ml*-session.md; each seed below was VERIFIED against the live code before filing, and one candidate was DROPPED as already-fixed: the DDT blue-box + RED 'DDT' text shipped in ml9-3 (plugins/millipede/src/shell/ddt-glyph.ts, wired main.ts:236) — do NOT refile it (the 'a fixed finding's note looks like the defect' trap). These are shell/render + core-index gaps, NOT new subsystems: reuse-first — the pure per-length palette fn (playfieldPens, playfield-palette.ts:43) and the showcase render path (renderShowcase, main.ts:195) already exist; the gap is the LCOLOR gate + the wiring + a composition decision, plus one black-rect draw. GROUPING RULE (inherit ml8/ml9/ml10): group stories by the FILES they edit, not by which pixel looks wrong. ACCESSIBILITY GATE STILL BINDS (the standing exception to ROM-always-wins — owner photosensitive epilepsy): every recolour/panel change must stay steady-by-construction — no full-screen strobe/flash on any transition; a fixed-per-length index is a steady colour and holds ml7-4 by construction, but re-verify at the /millipede/ visual playtest. Seed points are seed-only; expect the reviewer's mutation batteries to file follow-ups (an ml8-class tail). Source findings, not a fresh spec doc: ml7-11 session (CENTIN/LCOLOR + ALPHANUMERIC provenance), ml9-3 session (showcase black panels), ml10-5 session (ml10-6 guard). Roadmap parent: docs/superpowers/specs/2026-08-11-millipede-cabinet-roadmap-and-ml1-design.md. Screenshots: sprint/planning/ml9-playthrough-refs/ (attract-mame-reference.png, ingame-mame-reference.png). OUT OF SCOPE: any NEW owner-observed playthrough finding (that reopens ml9's owner-observed model, not this one); core-sim behaviour; sound.

## Metadata
- **Epic ID:** ml11
- **Repo:** arcade

## Background
_Cross-story constraints and guardrails to be filled in as the epic
progresses._

---
_Generated by `pf context create epic ml11` from the sprint YAML._
