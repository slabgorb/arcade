# Story cp1-5 Context

## Title
Player + single shot — bottom-zone movement, TBLMT clamp, slot-14 fire, shot-vs-mushroom collision

## Metadata
- **Story ID:** cp1-5
- **Type:** story
- **Points:** 5
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** centipede
- **Epic:** Centipede — foundation slice (scaffold, citations, picture ROMs, playfield, player+shot, render)

## Problem
Player gun movement inside the ROM's bottom-zone rectangle (MOVE, CENTI4.MAC:1477), trackball movement limited per TBLMT (CENTI4.MAC:2514), single-shot firing (CHECK FIRE SWITCH, CENTI4.MAC:2099; slot 14 — ONE shot in flight is authentic), shot flight and grid-cell collision with mushrooms as the ROM computes it. Shell input: pointer-lock mouse deltas quantized to trackball counts + arrow/WASD synthesizing counts at the clamp rate; both paths feed the same core input contract. Trackball hardware context: counters read once per IRQ, 240x/s (centiped.cpp:514) — the shell aggregates per frame.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- Movement bounds, TBLMT clamp value, fire semantics, and shot speed transcribed with radix-cited comments + claims entries; citations suite green.
- Exactly one shot in flight ever; firing while the shot lives is ignored, matching the ROM's slot-14 model.
- Shot-vs-mushroom collision resolves on grid cells per the ROM's arithmetic (cited), covered by moving-shot tests (asteroids lesson: never test only stationary projectiles).
- Core input contract is device-agnostic counts; mouse and keyboard shells both drive it in a shell test.

---
_Generated by `pf context create story cp1-5` from the sprint YAML._
