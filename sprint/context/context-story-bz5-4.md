# Story bz5-4 Context

## Title
Enemy AI cross-check against MAME + BZONE.MAC together

## Metadata
- **Story ID:** bz5-4
- **Type:** story
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Battlezone presentation & MAME cross-check — hit-driven windshield, the periscope overlay, and a driver-level fidelity pass

## Problem
Re-validate the enemy AI (shipped across bz1-bz4: the goal-heading tank state machine — flank/wander/charge, ~22 deg/s turn, standoff distance, fire on barrel sweep; plus saucer, missile and supertank behaviors) against MAME as a second source. The bz* audits read behavior off the BZONE.MAC / dis65 disassembly; MAME EXECUTES the same ROM, so its running behavior and its sound-gating are a cross-check on our port. bzone_a.cpp documents the sound-enable bits that fire as AI acts — engine enable/rev (D7/D4), shell enable (D2), explosion enable (D0) (bzone_a.cpp:1-20) — which tie audible enemy actions (move, fire, die) to the AI state and give an observable hook for the cross-check. Confirm the aggression ramp, the fire-decision gating, the standoff/charge transitions and the missile/supertank spawn cadence match the executed ROM; file any divergence as a finding with a MAME citation before changing behavior. Verification-first: behavior changes only where BOTH sources agree the clone is wrong.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

Verification-first audit — the deliverable is recorded findings with MAME citations;
behavior changes ONLY where BOTH sources agree the clone diverged.

## Sources (both verified on disk 2026-08-05)
- **MAME driver — EXTERNAL, NOT vendored in this repo.** Absolute paths:
  - `/Users/slabgorb/Projects/mame/src/mame/atari/bzone.cpp`
  - `/Users/slabgorb/Projects/mame/src/mame/atari/bzone.h`
  - `/Users/slabgorb/Projects/mame/src/mame/atari/bzone_a.cpp` (sound-enable bits, header lines 1–20 — cite as `bzone_a.cpp:1-20`)
  - `/Users/slabgorb/Projects/mame/src/mame/layout/bzone.lay`
  MAME EXECUTES the ROM; its running behavior + sound-gating are the second source.
- **BZONE.MAC disassembly (vendored, first source, used by bz1–bz4):**
  `reference/atari-source/battlezone/BZONE.MAC` (+ `reference/va-battlezone/Battlezone.dis65`).

## Clone code under audit (plugins/battlezone/src/core/ — pure sim; do NOT compute AI in the shell)
`enemies.ts` (tank AI state machine), `movement.ts`, `firing.ts`, `saucer.ts`, `difficulty.ts` (aggression ramp).
Findings doc to append: `plugins/battlezone/docs/battlezone-1980-source-findings.md`.
Suites that MUST stay green (AC3): `enemies`, `enemies-aggression`, `enemies-maneuver`, `firing`.

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- The tank AI state machine (turn rate, standoff, flank/charge transitions, fire-on- sweep) is cross-checked against MAME's executed behavior and the disassembly together; agreements and any divergences are recorded in the findings doc with MAME citations.
- The saucer, missile and supertank spawn/aggression cadences are confirmed against the second source; the enemy sound-gating (bzone_a.cpp:1-20) is used as the observable hook tying AI actions to their audio.
- Any behavior change lands only where both sources agree the clone diverged, with the existing enemy suites (enemies, enemies-aggression, enemies-maneuver, firing) staying green.

---
_Generated by `pf context create story bz5-4` from the sprint YAML._
