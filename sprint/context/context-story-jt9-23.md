# Story jt9-23 Context

## Title
joust B2UP3/SHUP3 — the two climb-preparation states, and the four PJOYT sites no DYTBL row covers

## Metadata
- **Story ID:** jt9-23
- **Type:** story
- **Points:** 5
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Joust — the remainder, re-ordered: apparatus, gameplay, geometry, brains, dossier

## Problem
Found by TEA during uf1-9's RED, via a whole-file census rather than a spot check. PJOYT is armed at FOURTEEN sites across joust's enemy brains and only NINE are DYTBL rows; uf1-9 wired those nine. The other five are hardcoded immediates the 1982 authors never migrated, and two of them belong to STATES this port does not have at all: B2UP3 (JOUSTRV4.SRC:4195-4200) and SHUP3 (:4411-4416), both labelled 'LEVEL FLIGHT, READY TO GO UP', each storing the line to track into PDIST and then loading a hardcoded 'LDA #20+1' decision interval — the ROM's own comment on both is TIME UNTIL NEXT DECISION, which is why the census found SIX sites carrying that comment where the backlog claimed four. Their bodies (B2UP3A :4202-4210, SHUP3A :4418-4425) sample the background pair for a cliff and gate on PVELY. THE OTHER THREE FROZEN SITES ARE ALREADY MODELLED and must not be re-derived: the down-seek's 2-wake wing hold (LDA #2 at :3823 and :4008) is enemy.DOWN_SEEK_WING_HOLD, and the hunter's cliff dwell (LDA #8 at :4144) is enemy.HUNTER_CLIFF_DWELL — both deliberately NOT wave-scaled, because their shadow-lord counterpart SHCLTM is a row and these are not. THE POINT OF THIS STORY is the two missing STATES, not the constants: wiring a row into a state that does not exist is impossible, which is why uf1-9 stopped here. Their extents are pinned by the FROZEN group in plugins/joust/tests/cadence-source.test.ts, which passes today and will keep passing — it asserts the ROM shape, not the port's. Start by deciding whether the climb-preparation state is reachable in this port at all: uf1-9 measured that the up-seek path itself is entered on ZERO frames of natural play across three seeds and 6000 frames, because hunters first spawn at wave 4 and no test's play reaches it.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria

From the epic description, TEA/Dev must verify:

1. **Two missing STATES modelled:** B2UP3 (JOUSTRV4.SRC:4195-4200) and SHUP3 (:4411-4416) — "LEVEL FLIGHT, READY TO GO UP" — must be wired into the state machine
   - Each stores the tracked line into PDIST
   - Each loads a hardcoded `LDA #20+1` decision interval (TIME UNTIL NEXT DECISION)
   - Bodies B2UP3A (:4202-4210) and SHUP3A (:4418-4425) sample the background pair for a cliff and gate on PVELY
   
2. **Three frozen sites NOT re-derived:** The following must remain as modelled constants (deliberately NOT wave-scaled):
   - Down-seek 2-wake wing hold: `enemy.DOWN_SEEK_WING_HOLD` = 2 (at JOUSTRV4.SRC:3823 and :4008)
   - Hunter cliff dwell: `enemy.HUNTER_CLIFF_DWELL` = 8 (at :4144)
   - Verify these still exist at the cited lines in `plugins/joust/src/core/enemy.ts`

3. **FROZEN guard stays green:** The test `cadence-source.test.ts` FROZEN group (describes at ~:260, asserts at ~:273) must continue passing
   - It verifies the ROM shape via "arms PJOYT at nine ROW-backed sites and five FROZEN ones"
   - This is NOT a port assertion; it is the ROM truth — it must keep passing

## References

Prior story's archived session (ROM quarry for this work):
- `sprint/archive/uf1-9-session.md` — wired the nine DYTBL rows, stopped at these two missing states

## TEA Framing Note

⚠ **Critical: Start with a reachability harness, not code**

The story's first task is investigative, not implementation. uf1-9 measured that the up-seek path itself is entered on **ZERO frames of natural play** across three seeds and 6000 frames. This is because hunters first spawn at wave 4, and no test's play reaches that far.

**Do not let the RED test be vacuous on an unreachable state.** If natural play cannot reach the climb-preparation state, you MUST drive it directly (a dedicated test fixture or seed+frame harness) to prove its wiring is correct. This is the "wiring a dead core can kill a sibling's reachability" trap — probe the assembled state, not staged units alone.

---
_Generated by `pf context create story jt9-23` from the sprint YAML._
_Acceptance criteria and TEA framing derived from epic description and SM verification._
