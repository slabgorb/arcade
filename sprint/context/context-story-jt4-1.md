# Story jt4-1 Context

## Title
BCD scoring core — the game.ts session layer, per-player SCRHUN/SCRTEN registers, drain the kill/egg/ptero event stream

## Metadata
- **Story ID:** jt4-1
- **Type:** story
- **Points:** 3
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** joust
- **Epic:** Joust — game structure (the game loop): 2P co-op ledgers, all six wave types, BCD scoring, extra men, game-over

## Problem
The session layer above the sim, and the score registers it feeds. A new module game.ts holds GameState { players: [{score, lives, out}], gover, wave, sim } and a stepGame that WRAPS the existing stepDemo (joust/src/core/demo.ts) and DRAINS its already-emitted score events (joust.killScore, egg.eggScoreEvents, ptero.pteroScoreEvent) — today thrown away (the event log 'would otherwise grow unbounded; nothing drains it') — into per-player 6-digit BCD registers. The SCRHUN thousands/hundreds + SCRTEN tens/hundreds-BACKWARDS layout (JOUSTRV4.SRC:7340-7366) is transcribed and pinned; the DVALUE ladder (Bounder 500 / Hunter 750 / Shadow Lord 1500 / Ptero 1000 DERIVED, JOUSTRV4.SRC:5563-5577) is the value source; eggs 250-1000 + 500 air-catch are already event-carried (jt2-4/jt3). TWO INDEPENDENT LEDGERS from this first story (the ROM co-op shape). Closes the accumulation seeds (demo.ts event-log drain, egg.ts/dissolve.ts 'BCD scoring is jt4'); corrects the 'scoring DISPLAY is jt4' comments to 'accumulation jt4 / display jt5' (no glyph render here — dev-overlay is jt4-5, authentic display is jt5). Every constant radix-cited + claims entry; the purity guard sweeps game.ts.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- game.ts exists with a per-player BCD score register; stepGame wraps stepDemo and drains the existing kill/egg/ptero score events into the registers with NO second stepping path (the jt2-1 one-sim seam); the purity guard sweeps game.ts green.
- The SCRHUN/SCRTEN tens-BACKWARDS layout is transcribed and pinned by test (a value crossing the tens/hundreds boundary lands in the ROM's byte order); the DVALUE ladder drives kill values; claims entries for the layout + the ladder; citations suite green.
- Two players accumulate independently (a P1 kill never moves P2's score); the derived ptero-1000 and the SCRTEN quirk are pinned with their provenance flagged (pass-through, not re-derived).
- Determinism: a seeded run's per-player score total replays bit-for-bit; no wall-clock / Math.random in core; npm test fully green including citations and purity guard.

---
_Generated by `pf context create story jt4-1` from the sprint YAML._
