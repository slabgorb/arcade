# Story jt9-11 Context

## Title
joust troll GRIP is production-dead — beginGrip/stepGrip/escalateGrip have zero production callers, and LAVTIM/LAVGRA are the rows that feed them

## Metadata
- **Story ID:** jt9-11
- **Type:** story
- **Points:** 6
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Joust — the remainder, re-ordered: apparatus, gameplay, geometry, brains, dossier

## Problem
joust/src/core/troll.ts implements the lava troll's grip — the PATCH1/2/3 escalation the 1982 source added in 'VERSION 3 SOFTWARE' — and NONE of it runs. beginGrip, stepGrip, escalateGrip, escapeScoreEvent and the TrollGrip type have ZERO references in src/ outside troll.ts itself; only trollSpawnable is live (demo.ts:961). demo.ts:420 admits it in its own words: '(stepGrip) is a pure core exercised directly; wiring the live grab off the LNDB7 …'. So a troll spawns, and can never actually grab anyone: the grace timer (LAVKLL 30*60, :6393), the +1/frame pull escalation to the $500 cap (:6374-6386), the -$0180 break-free threshold (:6616), the 50-point escape award (:6668) and the pull into the lava are all unreachable. This is the same disease as uf1-2 one layer down — a faithful, tested, cited pure core with no caller — and it BLOCKS uf1-10 (now jt9-12)'s LAVGRA half, which has nothing to feed until the grab is live. The missing piece is the GRAB itself: the LNDB7 hand-out/hand-grab detection that repoints the victim's gravity vector from ADDGRA to ADDLAV (PADGRA, :1651-1652). Found by TEA during uf1-2 test design while tracing which DYTBL rows had a live consumer chain.

ADDED BY jt9-1 (TEA, 2026-08-02) — HOW THE TROLL KNOWS WHO IT GRABBED, which this story needs before beginGrip can have a caller. PJOY has a THIRD meaning nobody has catalogued: at :6781 the newly-created troll's own workspace gets `STU PJOY,Y  FINGER PRINT THIS PROCESS FOR THE LAVA TROLL` — the VICTIM's workspace ADDRESS, stored as data. That is neither an entry address (jt9-1's subject, `JSR [PJOY,U]` at :5830/:5951/:6456) nor a wing state (jt5-8/uf1-9's). It is the victim binding, and it is read back at :1712 (`LAVVI2 CMPY PJOY,U`). Our trollProcess() carries no victim reference at all, which is why the grip has nowhere to attach.

Related, and already shipped by jt9-1 so do not rebuild it: demo.ts's insertTroll() now places a spawned troll IMMEDIATELY BEFORE an enemy rather than appending it, per :6778 `LDU PPREV  AFTER PREVIOUS PROCESS (BEFORE THIS ONE)`. That is the placement half of the same binding; this story owns the identity half. Note the port currently picks "the first enemy" because there is no grabbed victim to pick — once this story gives the troll a real victim, insertTroll should place it in front of THAT process, and jt9-1's positional test in glide-prologue.test.ts is the guard to update.

=== MERGED 2026-08-03: THE TROLL HALF OF jt9-12 FOLDED IN (Architect grooming pass) ===

jt9-12 held four DYTBL rows across two unrelated mechanisms, and said so itself: its LAVGRA half was hard-blocked on THIS story ("DO uf1-11 (now jt9-11) FIRST or this half is theatre"). Rather than keep a story whose stated precondition is another story, its two troll rows move here and its two egg rows moved to jt9-9. Points 5 -> 6.

THE TWO ROWS THIS STORY NOW OWNS. LAVTIM (JOUSTRV4.SRC:1611, LDA LAVTIM RESET TIMER - the lava troll hand-animation frame timer) and LAVGRA (:6395, PATCH1 LDD LAVGRA GIVE CURRENT LAVA TROLL GRAVITY, seeding CLVGRA). LAVGRA has a designated consumer that ALREADY EXISTS - troll.beginGrip(baseGravity) - and that is exactly why it belongs here: this story is what gives beginGrip a caller, so wiring LAVGRA is a one-line addition here and was an unreachable no-op anywhere else.

NOT IN SCOPE and never will be: LAVLAV, the fifth lava row. Its label appears exactly ONCE in the whole of JOUSTRV4.SRC, on its own DYWORD line at :7306. Williams shipped a row nothing reads; uf1-2 recorded it as dead-in-rom in ROW_DISPOSITION so no future sweep re-files it as a porting gap. CAUTION, added by this grooming pass: jt9-1 found the SAME dead-in-rom reasoning to be WRONG for LNTLAV - the label appeared once but the DYTBL initialiser walks the table POSITIONALLY (:939-950) and never reads a name, so the row was live. Re-check LAVLAV positionally before trusting its disposition; jt9-1 emptied the dead-in-rom category and added a guard asserting it stays empty.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story jt9-11` from the sprint YAML._
