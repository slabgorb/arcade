# Story mc1-1 Context

## Title
Scaffold the missile-command plugin and boot it in the cabinet

## Metadata
- **Story ID:** mc1-1
- **Type:** chore
- **Points:** 3
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Missile Command — plugin skeleton: boot the cabinet on REV-01 ground truth

## Problem
Stand up plugins/missile-command/ as the eighth game and prove it boots in the cabinet. REUSE-FIRST: copy plugins/joust/ (the newest raster game) as the template and strip it to a minimal bootable shell — do NOT hand-type the four files (a missing one fails late, not at the step you were on). Design: plugins/missile-command/docs/design/mc1-skeleton.md. Provenance/ground-truth already staged by the Architect under plugins/missile-command/{reference,docs}.

SCOPE: scaffold + registration + a black canvas that boots. No field, cursor, or gameplay yet (those are mc1-2..mc1-4). What this story buys is the wiring: the four-file plugin shape, the three registrations, a regenerated registry, and the core/shell purity boundary in place so every later story writes against a clean skeleton.

WATCH THE SERVE TRAP (tests/canonical-serve.test.mjs): the one dev server answers 200 to EVERYTHING via the lobby SPA fallback, so an all-200 sweep proves nothing. Any boot check must compare /missile-command/ against a nonsense control path and assert they DIFFER.

plugin.ts meta per design: id 'missile-command', title 'MISSILE COMMAND', year 1980, order 7, listed true, showcase false, version from package.json.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- The four-file plugin shape exists under plugins/missile-command/, copied and adapted from plugins/joust/: index.html (Vite entry referencing /src/main.ts), plugin.ts (GameMeta per design — id 'missile-command', title 'MISSILE COMMAND', year 1980, order 7), package.json ({name,version,private}), tsconfig.json (extends ../../tsconfig.json). The orchestrator suite's 'adding a game' invariants pass for the new directory.
- All THREE registrations are done and no more: 'missile-command' appended to games in justfile and to GAMES in vitest.config.ts, and src/host/registry.ts regenerated via 'npm run gen:registry' (committed, NOT hand-edited). scripts/build-app.mjs and the deploy workflow read plugins/ directly and need no edit.
- The game boots on the one dev server: http://127.0.0.1:5270/missile-command/ serves the plugin's own index.html and its /src/main.ts mounts a canvas. Proven DISTINCT from the lobby fallback — the check compares /missile-command/ against a nonsense control path (e.g. /banana/) and asserts the responses DIFFER, not merely that both are 200.
- The core/shell boundary is present and enforced: src/core/ and src/shell/ exist, and a purity/core-boundary test (copied from joust's tests/helpers/purity-scanner.ts) scans src/core/ source text and passes on the skeleton's clean core (no Date, no Math.random, no canvas in core).
- Gates green: 'npx vitest run --project missile-command' passes, 'npm run lint' is clean repo-wide, and 'npm run test:orchestrator' passes (the new game's wiring invariants are satisfied).

---
_Generated by `pf context create story mc1-1` from the sprint YAML._
