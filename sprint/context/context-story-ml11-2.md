# Story ml11-2 Context

## Title
Attract enemy-showcase: draw the per-creature BLACK cell panels behind the sprites (attract-mame-reference.png shows solid black panels; renderShowcase currently blits sprites straight onto the blue background).

## Metadata
- **Story ID:** ml11-2
- **Type:** bug
- **Points:** 2
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Millipede — deferred visual-fidelity follow-ups (the render-surface loose ends ml7/ml9/ml10 left filed but homeless)

## Problem
Deferred from ml9-3 (Delivery Finding, Dev: 'the showcase black-box cell backgrounds are not implemented — attract-mame-reference.png shows solid black panels behind the creature sprites, but the showcase still renders them over the blue background'). The decision (black boxes, not blue show-through) is ALREADY PINNED in ml9-3's Design Deviations — this story implements it only, no re-decision. GROUND TRUTH (verified 2026-08-16 at develop=734ecf3f): renderShowcase (plugins/millipede/src/main.ts:195) fills the whole canvas with SHOWCASE_BACKGROUND (blue), draws the sectioned text stamps (white high-scores + red creature labels, ml9-3), then blits each creature sprite via drawSpritePx — with NO black rectangle behind any cell. THE STORY: before blitting each showcase sprite/cell, draw a black rect behind it (a per-creature panel), sized/placed to match attract-mame-reference.png. Single file surface: src/main.ts (renderShowcase). This is a pure render addition on the attract path; it must NOT regress the white high-scores + red labels (ml9-3) or the self-playing attract demo. ACCESSIBILITY: a static black panel is steady — no strobe. Confirm at the /millipede/ visual playtest (the attract screen cycles in every ATTRACT_CYCLE_FRAMES window).

## Technical Approach
Single-file surface: `plugins/millipede/src/main.ts`, `renderShowcase` (:195-211).

The black rect goes inside the `for (const s of showcaseSprites())` loop at :208-210, **drawn BEFORE** `drawSpritePx`. Use a black fill (colour index matching the ROM blackness or a calculated value from the core palette) and size/position the rect to match the geometry shown in `sprint/planning/ml9-playthrough-refs/attract-mame-reference.png`.

Must NOT regress the white high-scores + red labels (:203-205, ml9-3) or the self-playing attract demo.

## Scope
- **In scope:** a per-creature BLACK rect panel drawn behind each showcase cell inside `renderShowcase`'s `for (const s of showcaseSprites())` loop (`main.ts:208-210`), before `drawSpritePx`. Pure render addition on the attract path.
- **Out of scope:** any re-decision of black-vs-blue (already PINNED in ml9-3's Design Deviations — implement only); the text stamps / label colours (ml9-3); the self-playing attract demo; the in-game HUD; core simulation. Single-file surface: `main.ts`.

## Acceptance Criteria
- renderShowcase (plugins/millipede/src/main.ts) draws a solid BLACK panel behind each creature cell/sprite before its stamps, matching the black panels in sprint/planning/ml9-playthrough-refs/attract-mame-reference.png; the panel geometry (which cells, size) is pinned to the reference or an explicit owner call, not eyeballed silently.
- No regression to ml9-3: the white HIGH SCORES + footer and the RED creature labels still render (they now sit on/around the black panels as the reference shows), and the self-playing attract demo and in-game HUD are unchanged; pinned by the existing showcase/attract tests staying green.
- The panels are static fills (no strobe/flash — ml7-4 accessibility); confirmed at the /millipede/ visual playtest where the showcase screen cycles into view.

---
_Generated by `pf context create story ml11-2` from the sprint YAML._

_SM (Baldur) verified the GROUND TRUTH against the tree at develop=734ecf3f (2026-08-16): renderShowcase fills blue, draws text stamps (ml9-3), blits sprites with NO black rect behind cells — confirmed accurate, no corrections. **DO NOT REGENERATE this context file** — the Technical Approach, Scope and ACs are hand-verified._
