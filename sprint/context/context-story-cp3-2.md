# Story cp3-2 Context

## Title
The flea (ANT, slot 12) — ANTPC/ANTMV drop, mushroom seeding, multi-hit kill

## Metadata
- **Story ID:** cp3-2
- **Type:** story
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** centipede
- **Epic:** Centipede — the menagerie (spider, flea, scorpion) + poison goes live

## Problem
The flea, transcribed from rev-4. ANTPC (CENTI4.MAC:128) initializes the flea into slot 12 (ANTP =MOBJP+12., CENDEF.MAC:138); ANTMV (CENTI4.MAC:46) drops it down the screen — transcribe the spawn TRIGGER (the ROM ties it to the mushroom population in the player zone — transcribe the actual count/rows from code), the mushroom SEEDING it performs as it falls (which cells, what probability source — must draw from the seeded rng, determinism preserved), the descent speed(s), and the hit behaviour (the folklore is two hits with a speed-up after the first — transcribe what rev-4 actually does, cite it, and record any folklore divergence). Kill scores flea 200 (CENTIP.DOC:115, rev-1 prose — diff against code per open question 4). Where the spawn condition reads wave state not yet modelled, parameterize per the epic's wave-gating ruling; the story's tests force spawns via debug seeding.

## Technical Approach
_Refined by TEA/Dev. Constraints that are NOT negotiable:_

- **Ground truth is `centipede/docs/rom-study/`** — cite the dossier + claims,
  never re-derive. The **citation gate is live** in this repo; re-anchor pinned
  lines after any edit to a cited file (a comment-only edit still shifts them).
- **RADIX:** `CENTI4.MAC` inherits `.RADIX 16` via `CENDE4` — bare literals are
  **HEX** unless they carry a trailing period (decimal). Every transcribed
  constant needs a radix-cited comment + a claims entry.
- **Core/shell boundary:** flea spawn, descent, seeding and hit logic live in
  `src/core`. Seeding **must** draw from the seeded rng so determinism holds.
- **Wave-gating ruling:** spawn/behaviour conditions reading wave or score state
  not yet modelled (wave progression is cp4) are transcribed with that input
  **parameterized** and test-pinned; tests force spawns via debug seeding.
- **Scoring prose is rev-1** (open question 4): flea 200 (`CENTIP.DOC:115`) is
  diffed against rev-4 **code** before baking. Code wins; record any divergence
  in `open-questions.md`.
- **Folklore is a hypothesis, not a spec:** "two hits with a speed-up" must be
  confirmed against what rev-4 `ANTMV` actually does, and any divergence recorded.

## Scope
- In scope: the flea (slot 12) — `ANTPC` init, `ANTMV` descent, spawn trigger,
  mushroom seeding, descent speed(s), multi-hit kill + score.
- In scope (**carry-forward from cp3-1**): widening the spider's `OVRLAP` scan
  input to include the flea slot — see AC-5.
- Out of scope: wave progression (cp4), the scorpion + live poison creation
  (cp3-3), the ecosystem demo (cp3-3), audio channels (no audio subsystem yet).

## Acceptance Criteria
- Flea trigger condition, descent speed(s), seeding behaviour, and hit/kill semantics transcribed from ANTPC/ANTMV with radix-cited comments + claims entries; citations green.
- Falling flea seeds mushrooms from the seeded rng only — determinism test replays an identical fall; purity guard green.
- Hit behaviour matches rev-4 code (number of hits, any speed change between them), pinned by moving-shot tests; kill scores the rev-4 value with any CENTIP.DOC divergence recorded.
- Gun-vs-flea contact kills the player through the shared PLAY/EXPLOD/RESTOR chain.
- **AC-5 (carry-forward from cp3-1):** the spider's `OVRLAP` scan (`:1749 LDY I,12.`)
  scans slots 12..0 — which **includes** the flea slot (`ANTP =MOBJP+12.`). cp3-1
  scoped its `segs` input to the 12 centipede segments because the flea did not
  exist; cp3-2 must widen that input to include the flea and pin the interaction
  with a test that stages a live flea in slot 12. See `src/core/spider.ts`, and
  cp3-1's Delivery Findings / "Not done here" in `sprint/archive/cp3-1-session.md`.
  Note cp3-1's implemented divergence: vacant slots are skipped in addition to the
  ROM's `>= 0xF4` check (commented at `OVRLAP_DEAD_PIC`) — a live flea must not be
  read as vacant.

---
_Generated by `pf context create story cp3-2` from the sprint YAML._
