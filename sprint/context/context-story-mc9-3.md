# Story mc9-3 Context

## Title
Exact OLDRAD blast curve + EXPFRA cadence and authentic missile trails (retire the triangle blast)

## Metadata
- **Story ID:** mc9-3
- **Type:** story
- **Points:** 5
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Missile Command — render fidelity (real graphics)

## Problem
The blast, for real. mc1's blast is a symmetric-triangle placeholder from two cited constants; this ports the exact per-tick OLDRAD radius curve and the EXPFRA growth/collapse cadence from PROCESS EXPLOSIONS (W3MAIN.MAC:1811). This is the ONE core touch in mc9: blastRadius() in src/core/explosion.ts moves from the triangle approximation to the cited OLDRAD curve, backed by a new claim (the OLDRAD table / EXPFRA cadence, byte-exact REV-01) so citations.test.ts and the AC3 guard stay green. Also render authentic missile trails (DRAW MISSILE, W3DSUP.MAC:1221) — the ABM/ICBM trail styling the cabinet uses. Depends on mc3; coordinate with mc4 only if both edit explosion.ts.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- blastRadius() follows the exact OLDRAD per-tick curve with the EXPFRA growth/collapse cadence (not the mc1 symmetric triangle); the curve constants carry a byte-exact claim and the AC3 no-uncited-literal guard passes.
- missile trails render from the authentic DRAW MISSILE (W3DSUP.MAC:1221) styling; explosion.ts cites PROCESS EXPLOSIONS (W3MAIN.MAC:1811).
- citations.test.ts and purity.test.ts stay green after the core explosion change; existing blast/damage tests are updated to the real curve and pass.

---
_Generated by `pf context create story mc9-3` from the sprint YAML._
