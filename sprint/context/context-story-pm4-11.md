# Story pm4-11 Context

## Title
Authentic HUD icons (shell): render lives as Pac-life sprites bottom-left and the level indicator as fruit sprites bottom-right, replacing pm4-9's procedural LIVES/LEVEL text in the bottom HUD band. Reuse drawPacman/drawFruit; leave the top band (SCORE, HIGH SCORE) unchanged. Pin the ROM life-icon cap + fruit-per-level sequence at RED. Accessibility: no full-screen flash (pm4-1).

## Metadata
- **Story ID:** pm4-11
- **Type:** story
- **Points:** 3
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** arcade
- **Epic:** Pac-Man — cabinet lifecycle (attract + state machine + freeze pauses) + fidelity/accessibility fixes

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria

**Derived from story title** (not authored in epic YAML — TEA verifies at RED):

1. Bottom-left: lives render as Pac-life sprites via `drawPacman()` (not `LIVES` text).
2. Bottom-right: level indicator renders as fruit sprites via `drawFruit()` (not `LEVEL` text).
3. The number of life icons is capped at the ROM life-icon cap (TEA pins exact value from ROM at RED).
4. The fruit shown follows the ROM fruit-per-level sequence per `pacman.asm:2b23-2b31` (TEA pins from audit at RED).
5. Top band (SCORE, HIGH SCORE) is unchanged.
6. Accessibility: no full-screen flash introduced (pm4-1 constraint).

**Note:** ACs 3 & 4 name ROM facts that are TEA's to pin exactly at RED — the SM did not pin the numeric cap or sequence in the epic.

## Contention & Scope Notes

**Sibling checkout a-2:** has claimed pm4-10 (game-over→attract timeout) with a zero-commit branch. pm4-10 could eventually touch `render.ts`, but its scope is the sim-level game-over transition, orthogonal to pm4-11's bottom HUD band (`drawHud`). **Low collision risk.** Dev should expect a possible rebase against `render.ts` and keep the pm4-11 diff scoped tightly to `drawHud()` + any new sprite-row helpers.

**Boundary reminder:** This is a SHELL/render story — all changes in `plugins/pac-man/src/shell/`. Do NOT touch `src/core/`. The pac-man purity guard scans core source text.

---
_Generated by `pf context create story pm4-11` from the sprint YAML._
